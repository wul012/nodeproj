# Java v128 说明 - ResponseRecords 第二次拆分

本版按 Node v289 全局计划执行 Java v128：继续拆 `ReleaseApprovalRehearsalResponseRecords`，只做结构降膨胀，不改变接口 JSON/record 语义，不新增 fake harness runtime。

## 改动

- 新增 `ReleaseApprovalSandboxConnectionAdapterPreflightEchoRecords`。
- 从 `ReleaseApprovalRehearsalResponseRecords` 迁出 disabled adapter client precheck、fake transport dry-run packet、sandbox endpoint handle preflight 及其子 boundary/shape/review records。
- 同步更新 response、receipt chain、相关 builders、verification builders 和测试引用。

## 行数

- 主文件 `ReleaseApprovalRehearsalResponseRecords.java`：1680 行 -> 1299 行。
- 新文件 `ReleaseApprovalSandboxConnectionAdapterPreflightEchoRecords.java`：386 行。

## 验证

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsEvidenceServiceSandboxConnectionPacketEchoTests,OpsEvidenceServiceSandboxEndpointPreflightEchoTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests,OpsEvidenceServiceReleaseApprovalRehearsalOverviewTests" test
```

结果通过。未启动 Docker，未启动或保留 Java / mini-kv 后台进程。
