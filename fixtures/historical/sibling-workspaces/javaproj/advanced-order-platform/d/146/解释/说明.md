# Java v146 runtime note

## Scope

Java v146 adds the read-only echo for the Node v317 credential-handle approval contract intake.
It keeps credential value access, raw endpoint parsing, provider/client instantiation, external requests, ledger writes, SQL/schema migration, deployment, rollback, and auto-start behavior blocked.

## Verification

```powershell
mvn -q "-Dtest=OpsEvidenceServiceCredentialHandleApprovalContractEchoTests,ReleaseApprovalVerificationHintContributionCatalogTests,ReleaseApprovalVerificationWarningDigestLineCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q test
```

Result: passed. Testcontainers still logged Docker detection warnings, but this version did not require starting Docker Desktop.

## Screenshot

- `../图片/mcp-v146-credential-handle-approval-contract-echo.png`

## Result

The Java response schema is now `java-release-approval-rehearsal-response-schema.v47`, and the v146 receipt is ready for Node v318 credential-handle approval contract upstream echo verification after mini-kv v139 completes its parallel non-participation receipt.
