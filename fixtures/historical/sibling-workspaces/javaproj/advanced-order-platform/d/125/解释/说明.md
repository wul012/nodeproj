# Java v125 运行说明: Ops Overview 集成测试拆分

## 本版目标

按 Node `plans2/v287-post-test-only-fake-harness-precheck-roadmap.md` 推进 Java v125: Integration Tests 第四刀连拆。

本版只调整测试结构，不改生产代码；不新增 fake harness runtime；不读取 credential value；不解析 raw endpoint URL；不打开 managed audit connection；不写 approval ledger；不执行外部 SQL。

## 拆分结果

原主文件:

```text
src/test/java/com/codexdemo/orderplatform/OpsOverviewIntegrationTests.java
```

新增:

```text
src/test/java/com/codexdemo/orderplatform/OpsOverviewIntegrationTestSupport.java
src/test/java/com/codexdemo/orderplatform/OpsOverviewReadOnlyEvidenceIntegrationTests.java
src/test/java/com/codexdemo/orderplatform/OpsOverviewStaticReadOnlyEvidenceIntegrationTests.java
```

拆分方式:

- `OpsOverviewIntegrationTests` 只保留 `/api/v1/ops/overview` 基础只读业务信号。
- `OpsOverviewReadOnlyEvidenceIntegrationTests` 承接 `/api/v1/ops/evidence` 动态 read-only evidence 回归。
- `OpsOverviewStaticReadOnlyEvidenceIntegrationTests` 承接 `/contracts/ops-read-only-evidence.sample.json` 静态样本回归。
- `OpsOverviewIntegrationTestSupport` 收口 MockMvc、failed event 仓库注入和清理顺序，避免 overview/evidence 测试重复维护清理逻辑。

## 行数变化

```text
OpsOverviewIntegrationTests.java: 854 -> 57 行
OpsOverviewIntegrationTestSupport.java: 35 行
OpsOverviewReadOnlyEvidenceIntegrationTests.java: 621 行
OpsOverviewStaticReadOnlyEvidenceIntegrationTests.java: 163 行
```

## 验证

已通过:

```text
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsOverviewIntegrationTests,OpsOverviewReadOnlyEvidenceIntegrationTests,OpsOverviewStaticReadOnlyEvidenceIntegrationTests" test
```

## 清理

本版测试会生成 `target/`，收尾阶段删除。没有启动 Docker，也没有留下 Java 后台进程。
