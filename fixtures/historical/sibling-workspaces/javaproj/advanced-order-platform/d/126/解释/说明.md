# Java v126 运行说明: EvidenceService echo support catalog 止血

## 本版目标

按 Node `plans2/v287-post-test-only-fake-harness-precheck-roadmap.md` 推进 Java v126: EvidenceService catalog 化止血。

本版只做小范围重构，不改 receipt/response 结构，不新增 fake harness runtime；不读取 credential value；不解析 raw endpoint URL；不打开 managed audit connection；不写 approval ledger；不执行外部 SQL。

## 改动结果

调整文件:

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverBoundaryCatalog.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverApprovalRequiredImplementationReadinessEchoSupport.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverProductionReadinessBlockedDecisionEchoSupport.java
```

改动方式:

- `BoundaryCatalog` 新增 `ApprovalRequiredImplementationTemplate`，统一承载 approval-required boundary 的 owner、required artifacts、Java echo hint、mini-kv receipt hint、Node verification hint 和 prohibited runtime actions。
- `ApprovalRequiredImplementationReadinessEchoSupport` 改为从 catalog 取模板，删除本类内重复 switch。
- `ProductionReadinessBlockedDecisionEchoSupport` 的 missing requirement list 改为复用 catalog 的 requirement codes。

## 行数变化

```text
ReleaseApprovalSandboxEndpointCredentialResolverBoundaryCatalog.java: 119 -> 234 行
ReleaseApprovalSandboxEndpointCredentialResolverApprovalRequiredImplementationReadinessEchoSupport.java: 441 -> 334 行
ReleaseApprovalSandboxEndpointCredentialResolverProductionReadinessBlockedDecisionEchoSupport.java: 340 -> 330 行
```

净效果是把分散在 echo support 内的高风险常量收口到一个 catalog，整体主改动约为等量迁移，行为保持不变。

## 验证

已通过:

```text
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsEvidenceServiceCredentialResolverProductionReadinessBlockedDecisionEchoTests,OpsEvidenceServiceApprovalRequiredImplementationReadinessEchoTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
```

## 清理

本版测试会生成 `target/`，收尾阶段删除。没有启动 Docker，也没有留下 Java 后台进程。
