# Java v144 runtime note

## Scope

Java v144 adds the read-only echo for the Node v310 human approval artifact review post-echo decision gate.
It keeps runtime shell implementation, runtime invocation, credential value access, raw endpoint parsing, provider/client instantiation, external requests, ledger writes, SQL/schema migration, mini-kv write/authority commands, and auto-start behavior blocked.

## Verification

```powershell
mvn -q "-Dtest=OpsEvidenceServiceHumanApprovalArtifactReviewPostEchoDecisionGateEchoTests,ReleaseApprovalVerificationHintContributionCatalogTests,ReleaseApprovalVerificationWarningDigestLineCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q test
```

Result: passed. Testcontainers still logged Docker detection warnings, but this version did not require starting Docker Desktop.

## Screenshot

- `../图片/mcp-v144-human-approval-post-echo-decision-gate-echo.png`

## Result

The Java response schema is now `java-release-approval-rehearsal-response-schema.v45`, and the v144 receipt is ready for Node v311 post-echo decision upstream echo verification after mini-kv v137 completes its parallel read-only receipt.
