# Java v145 runtime note

## Scope

Java v145 adds the read-only echo for the Node v314 signed human approval artifact contract intake.
It keeps approval ledger writes, signed artifact storage/validation/authority, runtime shell behavior, credential value access, raw endpoint parsing, provider/client instantiation, external requests, SQL/schema migration, deployment, rollback, and auto-start behavior blocked.

## Verification

```powershell
mvn -q "-Dtest=OpsEvidenceServiceSignedHumanApprovalArtifactContractEchoTests,ReleaseApprovalVerificationHintContributionCatalogTests,ReleaseApprovalVerificationWarningDigestLineCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q test
```

Result: passed. Testcontainers still logged Docker detection warnings, but this version did not require starting Docker Desktop.

## Screenshot

- `../图片/mcp-v145-signed-human-approval-artifact-contract-echo.png`

## Result

The Java response schema is now `java-release-approval-rehearsal-response-schema.v46`, and the v145 receipt is ready for Node v315 signed human approval artifact contract upstream echo verification after mini-kv v138 completes its parallel non-participation receipt.
