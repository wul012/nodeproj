# Java v143 runtime note

## Scope

Java v143 adds the read-only echo for the Node v308 human approval artifact review packet.
It keeps runtime shell, credential value access, raw endpoint parsing, provider/client instantiation, external requests, ledger writes, schema migration, mini-kv writes, and auto-start behavior blocked.

## Verification

```powershell
mvn -q test
```

Result: passed. Testcontainers still logged Docker detection warnings, but this version did not require starting Docker Desktop.

## Screenshot

- `../图片/mcp-v143-human-approval-artifact-review-packet-echo.png`

## Result

The Java response schema is now `java-release-approval-rehearsal-response-schema.v44`, and the v143 receipt is ready for Node v309 upstream echo verification.
