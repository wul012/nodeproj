# Java v115 运行说明：credential resolver approval-required boundary echo refinement

## 计划依据

本次继续读取当前有效计划：

```text
D:\nodeproj\orderops-node\docs\plans\v274-post-disabled-candidate-echo-roadmap.md
```

计划要求 Java v115 只读强化 6 个 approval-required boundary 的解释，并继续复用 v113 的 echo workflow template：

```text
CREDENTIAL_HANDLE
ENDPOINT_HANDLE
OPERATOR_APPROVAL
ROLLBACK_BOUNDARY
SCHEMA_MIGRATION_POLICY
AUDIT_LEDGER_WRITE_POLICY
```

边界要求保持不变：不写 approval ledger，不执行 SQL，不读取 credential value，不解析 raw endpoint，不打开 managed audit connection，不启动真实 resolver。

## 合理性判断

合理。v113 已经把 4 个 candidate-ready boundary 和 6 个 approval-required boundary 回显出来，但 approval-required 一侧还缺少更细的只读解释。v115 只是在现有 disabled candidate receipt 上补齐每个 approval-required boundary 的解释字段，不进入真实执行路径。

本次同时做了一刀结构拆分：把 sandbox endpoint credential resolver decision echo 相关 nested records 从 `ReleaseApprovalRehearsalResponseRecords` 迁出，避免主 records 文件继续膨胀。

## 代码改动

新增：

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverDecisionEchoRecords.java
```

修改：

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalRehearsalResponseRecords.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoRecords.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoSupport.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalManagedAuditSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoReceiptBuilder.java
src/main/java/com/codexdemo/orderplatform/ops/OpsEvidenceService.java
src/test/java/com/codexdemo/orderplatform/ops/OpsEvidenceServiceTests.java
src/test/java/com/codexdemo/orderplatform/OpsOverviewIntegrationTests.java
```

`ReleaseApprovalSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoRecords` 新增：

```text
approvalRequiredBoundaryExplanationsEchoed
approvalRequiredBoundaryExplanations
RehearsalSandboxEndpointCredentialResolverApprovalRequiredBoundaryExplanation
```

每个 explanation 都明确：

```text
code
requirementFromV268
evidenceAllowed
approvalReason
prohibitedRuntimeActions
credentialValueReadAllowed
rawEndpointUrlParseAllowed
managedAuditConnectionAllowed
approvalLedgerWriteAllowed
sqlExecutionAllowed
rollbackExecutionAllowed
automaticUpstreamStartAllowed
```

## 六个边界

本版回显的 6 个 approval-required boundary：

```text
CREDENTIAL_HANDLE
ENDPOINT_HANDLE
OPERATOR_APPROVAL
ROLLBACK_BOUNDARY
SCHEMA_MIGRATION_POLICY
AUDIT_LEDGER_WRITE_POLICY
```

共同约束：

```text
evidenceAllowed=approval-required-read-only-evidence
credentialValueReadAllowed=false
rawEndpointUrlParseAllowed=false
managedAuditConnectionAllowed=false
approvalLedgerWriteAllowed=false
sqlExecutionAllowed=false
rollbackExecutionAllowed=false
automaticUpstreamStartAllowed=false
```

## 版本与 schema

```text
release approval rehearsal response schema: v33 -> v34
disabled implementation candidate echo receipt: v1 -> v2
candidate echo mode: java-v115-credential-resolver-approval-required-boundary-echo-refinement-only
```

新增 proof claim：

```text
managedAuditSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoReceipt.approvalRequiredBoundaryExplanations.size=6
```

新增 Node verification action：

```text
Verify managedAuditSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoReceipt.approvalRequiredBoundaryExplanations.size=6
```

## 行数结果

```text
ReleaseApprovalRehearsalResponseRecords.java: 1833 -> 1680 行
ReleaseApprovalSandboxEndpointCredentialResolverDecisionEchoRecords.java: 162 行
ReleaseApprovalSandboxEndpointCredentialResolverDisabledImplementationCandidateEchoRecords.java: 303 行
```

这次拆分幅度合理：主 records 文件减少 153 行，迁出的 decision echo records 是一组稳定数据载体；业务新增只集中在 disabled candidate receipt 的只读解释字段，没有新写 600-800 行重 support。

## 验证记录

已运行：

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsEvidenceServiceTests,OpsOverviewIntegrationTests" test
mvn -q test
```

覆盖点：

```text
Java 编译通过
OpsEvidenceServiceTests 通过
OpsOverviewIntegrationTests 通过
全量 Maven test 通过
6 个 approval-required boundary 精确回显
approvalRequiredBoundaryExplanations.size=6 proof claim 存在
Node verification action 存在
所有 runtime action boolean 保持 false
```

全量测试期间 Testcontainers 打印了本机 Docker 环境不可用的日志，但命令退出码为 0。本版不需要启动 Docker Desktop，也没有启动 Docker。

## 清理记录

Maven 测试会生成 `target/`；最终交付前删除。没有启动长期 Java 服务，没有启动 Docker Desktop，没有遗留后台进程。

## 一句话总结

Java v115 在不打开真实 resolver、不读 credential、不解析 raw endpoint、不连接 managed audit 的前提下，补齐了 6 个 approval-required boundary 的只读解释，并把 decision echo records 从主 records 文件中拆出，供后续 Node v275 做三方一致性验证。
