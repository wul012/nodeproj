# Java v93 说明：operator window checklist echo receipt

## 计划依据

最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\v237-post-readiness-gate-roadmap.md
```

计划要求 Java v93 只读回显 Node v238 operator window checklist 字段，给 Node v239 evidence verification 使用；不打开连接、不读取 credential value、不执行 SQL、不写 ledger。

## 合理性判断

这次继续合理拆分。`OpsEvidenceService` 只保留常量和入口接线，v93 主逻辑放进独立 builder：

```text
ReleaseApprovalManagedAuditSandboxConnectionOperatorWindowChecklistEchoReceiptBuilder
```

新增字段按四组收口：

```text
checklistFieldBoundary
approvalBoundary
credentialBoundary
javaExecutionBoundary
```

同时把 v92 builder 里已有的位置布尔构造改成命名 flags record，避免继续靠 `true/false` 顺序表达业务语义。

## 本版目标

新增响应字段：

```text
managedAuditSandboxConnectionOperatorWindowChecklistEchoReceipt
```

新增 receipt：

```text
java-release-approval-rehearsal-managed-audit-sandbox-connection-operator-window-checklist-echo-receipt.v1
```

承接：

```text
Node v238
managed-audit-manual-sandbox-connection-operator-window-checklist.v1
```

面向：

```text
Node v239
managed-audit-manual-sandbox-connection-operator-window-evidence-verification.v1
```

## 代码改动

核心新增：

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalManagedAuditSandboxConnectionOperatorWindowChecklistEchoReceiptBuilder.java
```

响应模型新增：

```text
RehearsalManagedAuditSandboxConnectionOperatorWindowChecklistEchoReceipt
RehearsalSandboxConnectionOperatorWindowChecklistFieldBoundary
RehearsalSandboxConnectionOperatorWindowApprovalBoundary
RehearsalSandboxConnectionOperatorWindowCredentialBoundary
RehearsalSandboxConnectionOperatorWindowExecutionBoundary
```

验证链路更新：

```text
ReleaseApprovalRehearsalResponseBuilder
ReleaseApprovalVerificationHintBuilder
ReleaseApprovalVerificationWarningDigestBuilder
```

schema 升级：

```text
java-release-approval-rehearsal-response-schema.v21
```

## 关键字段

v93 回显 Node v238 的 checklist 形态：

```text
requiredApprovalCount=3
checklistStepCount=8
pauseConditionCount=8
forbiddenOperationCount=6
timeoutBudgetMs=15000
windowDurationMinutes=30
```

approval item id：

```text
release-owner
security-reviewer
operations-owner
```

pause condition code：

```text
SOURCE_GATE_NOT_READY
OWNER_APPROVAL_MISSING
CREDENTIAL_VALUE_REQUESTED
SCHEMA_SQL_REQUESTED
ROLLBACK_PATH_MISSING
TIMEOUT_BUDGET_CHANGED
UPSTREAM_ACTIONS_ENABLED
MANUAL_ABORT_MARKED
```

## 边界

仍然保持：

```text
credentialValueIncludedInChecklist=false
credentialValueReadByJava=false
actualConnectionAttemptedByJava=false
schemaMigrationSqlExecutedByJava=false
approvalLedgerWrittenByJava=false
managedAuditStoreWrittenByJava=false
nodeAutoStartAllowed=false
productionWindowOpenedByJava=false
readyForManagedAuditSandboxAdapterConnection=false
```

## 验证记录

已执行：

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsEvidenceServiceTests,OpsOverviewIntegrationTests" test
```

后续收口前还会执行：

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsEvidenceServiceTests,OpsOverviewIntegrationTests" test
mvn -q -DskipTests package
git diff --check
```

## 清理记录

本说明生成时没有启动后台服务。Maven 验证产生的 `target/` 会在最终收口前删除。

## 一句话总结

v93 只读回显 Node v238 checklist，并把新增布尔边界放进命名 record/factory；Node v239 可以拿它做验证，但仍不能据此打开真实 sandbox connection。
