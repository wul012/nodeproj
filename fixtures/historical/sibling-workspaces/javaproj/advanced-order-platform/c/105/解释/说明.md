# Java v105 说明：sandbox endpoint credential resolver decision echo marker

## 计划依据

最新计划来自：
```text
D:\nodeproj\orderops-node\docs\plans\v260-post-credential-resolver-decision-roadmap.md
```

这版要求 Java 做的是只读回显 Node v260 的 credential resolver 决策记录，不是实现 resolver 本身。

## 合理性判断

合理。Node v260 已经把 sandbox endpoint credential resolver 收敛成 decision record，Java 这一版只需要把 handles、policy marker、approval marker、no-go 条件和 side-effect 边界固定下来，继续把真实凭据、raw endpoint、外部请求、managed-audit connection、SQL、ledger 和 auto-start 全部挡住。

## 本版内容

新增字段：
```text
managedAuditSandboxEndpointCredentialResolverDecisionEchoMarker
```

核心回显：
```text
Node v260 decision record endpoint / markdown endpoint / state
Node v259 upstream echo verification endpoint / state
8 个 required decision fields
9 个 explicit no-go conditions
read-only side-effect boundary
```

schema 口径：
```text
source schema: java-release-approval-rehearsal-response-schema.v26
response schema: java-release-approval-rehearsal-response-schema.v27
```

## 保持不做

```text
不读 credential value
不解析 raw endpoint URL
不打开 managed audit connection
不发送外部请求
不执行 schema migration
不写 approval ledger
不启动 Java / mini-kv
不把 mini-kv 变成 managed audit storage
```

## 下一步

Node v261 会拿 Java v105 的 decision echo marker 继续做下一轮只读校验，再往后才考虑更细的 disabled resolver 预检。
