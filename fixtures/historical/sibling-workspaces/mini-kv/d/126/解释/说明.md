# mini-kv v126 命令归档说明

本轮版本目标是补充 `credential_resolver_implementation_plan_non_participation_receipt`，承接 Node v283 resolver implementation plan draft，并明确 mini-kv 仍只提供只读、非执行、非凭据、非端点、非权威回执。mini-kv 不执行 LOAD/COMPACT/RESTORE/SETNXEX，不启动服务端，不参与 Java/Node 的 resolver 执行。

## 01-cmake-configure.png

- 命令：`cmake -S . -B cmake-build-v126 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
- 结果：配置和生成成功，构建文件写入 `cmake-build-v126`。
- 意义：证明新增 CMake target、源文件和测试文件能够被当前 CLion/MinGW 工具链识别。

## 02-cmake-build.png

- 命令：`cmake --build cmake-build-v126 --parallel 2`
- 结果：完整构建成功，包含新增的 `minikv_credential_resolver_implementation_plan_non_participation_receipt_tests`。
- 意义：证明 v126 新增 receipt 实现、命令 smoke formatter、fixture 校验测试与既有目标可以一起编译。

## 03-targeted-tests.png

- 命令：`ctest --test-dir cmake-build-v126 --output-on-failure -R "credential_resolver_implementation_plan_non_participation_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests"`
- 结果：4/4 通过。
- 意义：定向验证 v126 receipt、runtime smoke evidence、release verification manifest、SMOKEJSON 输出都包含并保护新的 Node v283 plan 非参与回执。

## 04-full-ctest.png

- 命令：`ctest --test-dir cmake-build-v126 --output-on-failure`
- 结果：53/53 通过。
- 意义：证明 v126 新增内容没有破坏 WAL、snapshot、restore evidence、runtime receipts、credential resolver receipt 链和既有 smoke 测试。

## 05-cli-smoke.png

- 命令：`SMOKEJSON`、`CHECKJSON SMOKEJSON`、`GET restore:real-read-token`、`QUIT`
- 结果：`SMOKEJSON` 包含 `credential_resolver_implementation_plan_non_participation_receipt`；`CHECKJSON SMOKEJSON` 仍是 read-only/no-write；`GET restore:real-read-token` 返回 `(nil)`。
- 意义：证明真实 CLI 运行下，v126 只暴露计划回执和边界证据，不泄露凭据值，不暴露 raw endpoint，也不写入恢复 token。

## 06-line-counts.png

- 命令：统计 `src/`、`include/`、`tests/` 下 C++/header 文件行数前 15。
- 结果：当前最大文件为 `src/command.cpp` 800 行，`tests/command_tests.cpp` 792 行，新增 `src/runtime_credential_resolver_implementation_plan_receipts.cpp` 477 行。
- 意义：本版没有继续制造 1000 行以上的新大文件；新增 receipt 文件仍在可维护范围内。后续若继续增长，优先按 receipt family 或 fixture/test helper 再拆。

## 进程与清理记录

- 本轮未启动 `minikv_server.exe` 或其他长期后台服务。
- CLI smoke 使用 `minikv_cli.exe` 短进程，命令完成后自动退出。
- `cmake-build-v126` 是本轮临时验证目录，最终提交前会删除。
