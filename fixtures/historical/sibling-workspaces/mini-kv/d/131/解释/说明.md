# mini-kv v131 command evidence

This archive records the final verification pass for mini-kv v131, the credential resolver disabled runtime shell candidate gate non-participation receipt.

## Screenshots

1. `../图片/01-cmake-configure.png`
   - Command: `cmake -S . -B cmake-build-v131 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
   - Result: configure and generation succeeded.
   - Meaning: the v131 source, test target, and fixture paths are accepted by CMake.

2. `../图片/02-cmake-build.png`
   - Command: `cmake --build cmake-build-v131 --parallel 2`
   - Result: build succeeded and produced `minikv_disabled_runtime_shell_candidate_gate_receipt_tests`.
   - Meaning: the new v131 receipt source and updated aggregation tests compile with the project.

3. `../图片/03-targeted-ctest.png`
   - Command: targeted CTest for `disabled_runtime_shell_candidate_gate_receipt_tests`, `disabled_runtime_shell_receipt_tests`, `cred_res_exec_denied_retention_check_tests`, `release_verification_manifest_tests`, `runtime_smoke_evidence_tests`, and `smokejson_command_receipt_tests`.
   - Result: 6/6 tests passed.
   - Meaning: v131 is visible in standalone fixture, runtime `SMOKEJSON`, release manifest, rolling diagnostics, and older receipt-history tests.

4. `../图片/04-full-ctest.png`
   - Command: `ctest --test-dir cmake-build-v131 --output-on-failure`
   - Result: 58/58 tests passed.
   - Meaning: the full mini-kv regression suite remains green after adding the v131 candidate gate receipt.

5. `../图片/05-cli-smoke.png`
   - Command: `SMOKEJSON`, `CHECKJSON SMOKEJSON`, `GET restore:real-read-token`, `QUIT`.
   - Result: `SMOKEJSON` exposes `credential_resolver_disabled_runtime_shell_candidate_gate_non_participation_receipt`, receipt digest `fnv1a64:0557867ad4f6ed7e`, Node v297 gate digest `651383bcd175bdaff2691c026135a1cebbcf30de91be7709cbc7843866684e22`, and closed runtime flags. `CHECKJSON SMOKEJSON` stays read-only and `GET restore:real-read-token` returns `(nil)`.
   - Meaning: the real CLI smoke remains a read-only evidence check and does not execute writes, restore, compact, load, SETNXEX, runtime shell, credential reads, raw endpoint parsing, or external requests.

6. `../图片/06-line-counts.png`
   - Command: line count summary for the updated large files.
   - Result: largest touched files are `tests/release_verification_manifest_tests.cpp` at 829 lines, `tests/runtime_smoke_evidence_tests.cpp` at 759 lines, `src/runtime_credential_resolver_disabled_runtime_shell_candidate_gate_receipts.cpp` at 423 lines, and `src/command_smoke_formatters.cpp` at 381 lines.
   - Meaning: no production source or test file crossed the 1000-line split threshold during v131.

## Cleanup

No server or long-running process was started for this archive. The temporary build directory `cmake-build-v131` and intermediate `_logs` directory are removed during the final cleanup pass; final screenshots and this explanation remain under `d/131/`.
