# mini-kv v130 Command Archive

This version adds the `credential_resolver_disabled_runtime_shell_non_participation_receipt` for the Node v295 disabled runtime shell design review. It is a read-only non-participation receipt for Node v296 upstream echo verification. It does not implement, enable, or invoke a disabled runtime shell; does not instantiate providers or resolver clients; does not read credential values; does not parse raw endpoint URLs; does not send HTTP/TCP; does not connect managed audit; does not write storage, approval ledger, or schema state; and does not execute LOAD, COMPACT, RESTORE, or SETNXEX.

## 01-cmake-configure.png

- Command: `cmake -S . -B cmake-build-v130 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
- Result: configure and generation succeeded; build files were written to `cmake-build-v130`.
- Meaning: the new v130 source file, test target, and CMake registration are accepted by the local CLion/MinGW toolchain.

## 02-cmake-build.png

- Command: `cmake --build cmake-build-v130 --parallel 2`
- Result: full build succeeded, including `minikv_disabled_runtime_shell_receipt_tests`.
- Meaning: the v130 receipt implementation, SMOKEJSON formatter exposure, fixtures, release manifest, and tests compile together with the existing mini-kv targets.

## 03-targeted-tests.png

- Command: `ctest --test-dir cmake-build-v130 --output-on-failure -R "release_verification_manifest_tests|runtime_smoke_evidence_tests|smokejson_command_receipt_tests|cred_res_exec_denied_retention_check_tests|disabled_runtime_shell_receipt_tests"`
- Result: 5/5 targeted tests passed.
- Meaning: the standalone receipt fixture, runtime smoke exposure, release manifest exposure, rolling diagnostics, and retained v129 receipt history stayed aligned.

## 04-full-ctest.png

- Command: `ctest --test-dir cmake-build-v130 --output-on-failure`
- Result: 57/57 tests passed.
- Meaning: v130 did not regress existing WAL, snapshot, command, TCP, restore evidence, runtime evidence, release manifest, or credential resolver receipt behavior.

## 05-cli-smoke.png

- Command: `SMOKEJSON`, `CHECKJSON SMOKEJSON`, `GET restore:real-read-token`, `QUIT`
- Result: `SMOKEJSON` exposes `credential_resolver_disabled_runtime_shell_non_participation_receipt`; `CHECKJSON SMOKEJSON` remains read-only and non-write; `GET restore:real-read-token` returns `(nil)`.
- Meaning: real CLI execution exposes the v130 receipt with `receipt_digest=fnv1a64:80181b2752099581`, the Node v296 consumption hint, the Node v295 design review source, the Node v294 pre-plan source, and the retained v129 receipt history.

## 06-line-counts.png

- Command: count the largest C++ and test files after the v130 change.
- Result: `tests/release_verification_manifest_tests.cpp` is 783 lines, `tests/runtime_smoke_evidence_tests.cpp` is 721 lines, `src/runtime_evidence_receipts.cpp` is 706 lines, `src/command.cpp` is 661 lines, `tests/command_tests.cpp` is 662 lines, and `src/tcp_server.cpp` is 606 lines. The new v130 receipt source is 366 lines and the new v130 test is 242 lines.
- Meaning: no file is close to the earlier 1000-line split threshold after this version. The focused v130 receipt did not require another maintenance split.

## Process And Cleanup Notes

- No `minikv_server.exe` or other long-running service was started.
- CLI smoke used a short-lived `minikv_cli.exe` process and exited with `QUIT`.
- `cmake-build-v130`, `d/130/_logs`, and `v130-receipt-body.tmp.json` are temporary verification artifacts and should be removed before final handoff.
