# v139 归档说明

1. `01-cmake-configure.png`：`cmake -S . -B cmake-build-v139 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug` 配置成功。
2. `02-cmake-build.png`：`cmake --build cmake-build-v139 --parallel 2` 完整构建成功，包含 v139 receipt、fixture、测试和全量目标。
3. `03-targeted-ctest.png`：定向测试通过，覆盖 v139 新 receipt、signed artifact 历史 receipt、runtime smoke、manifest 和 rolling guard。
4. `04-full-ctest.png`：全量 CTest 66/66 通过，证明 v139 没有破坏既有回归面。
5. `05-cli-smoke.png`：CLI 真实执行 `SMOKEJSON` / `CHECKJSON SMOKEJSON` / `GET restore:real-read-token`，确认 v139 receipt 进入运行时输出，且 restore token 仍为 `(nil)`。
6. `06-tcp-smoke.png`：TCP server/client 真实 smoke 通过，确认服务端可读回 v139 receipt，且服务进程已在验证后停止。

补充：本次临时 HTML 证据页仅用于截图，截图完成后会清理 `d/139/_logs`、临时 HTTP 服务和 `cmake-build-v139`。
