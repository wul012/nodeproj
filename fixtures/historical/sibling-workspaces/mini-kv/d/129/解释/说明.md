# mini-kv v129 Command Archive

This version adds the `credential_resolver_disabled_fake_harness_execution_denied_receipt_verification_retention_check` for the Node v292 blocked fake-harness readiness decision. It is a read-only retention check for Node v293 review. It does not implement or run a fake harness runtime, does not instantiate real or fake providers or clients, does not read credential values, does not parse raw endpoint URLs, does not send HTTP/TCP, does not connect managed audit, does not write storage, approval ledger, or schema state, and does not execute LOAD, COMPACT, RESTORE, or SETNXEX.

## 01-cmake-configure.png

- Command: `cmake -S . -B cmake-build-v129 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
- Result: configure and generation succeeded; build files were written to `cmake-build-v129`.
- Meaning: the new v129 source file, test target, and CMake registration are accepted by the local CLion/MinGW toolchain.

## 02-cmake-build.png

- Command: `cmake --build cmake-build-v129 --parallel 2`
- Result: full build succeeded, including `minikv_cred_res_exec_denied_retention_check_tests`.
- Meaning: the v129 retention check implementation, SMOKEJSON formatter exposure, fixtures, and tests compile together with the existing mini-kv targets.

## 03-targeted-tests.png

- Command: `ctest --test-dir cmake-build-v129 --output-on-failure -R "release_verification_manifest_tests|runtime_smoke_evidence_tests|current_runtime_fixture_rolling_guard_tests|runtime_no_start_no_write_follow_up_tests|operator_window_no_start_no_write_receipt_tests|cred_res_exec_denied_retention_check_tests"`
- Result: 6/6 targeted tests passed.
- Meaning: the retention fixture, runtime smoke exposure, release manifest exposure, and closed-boundary checks all stayed aligned.

## 04-full-ctest.png

- Command: `ctest --test-dir cmake-build-v129 --output-on-failure`
- Result: 56/56 tests passed.
- Meaning: v129 did not regress existing WAL, snapshot, command, TCP, restore evidence, runtime evidence, release manifest, or credential resolver receipt behavior.

## 05-cli-smoke.png

- Command: `SMOKEJSON`, `CHECKJSON SMOKEJSON`, `GET restore:real-read-token`, `QUIT`
- Result: `SMOKEJSON` exposes `credential_resolver_disabled_fake_harness_execution_denied_receipt_verification_retention_check`; `CHECKJSON SMOKEJSON` remains read-only and non-write; `GET restore:real-read-token` returns `(nil)`.
- Meaning: real CLI execution exposes the v129 receipt with `receipt_digest=fnv1a64:9d433e000a555c11`, the Node v293 sentence, the Node v292 blocked readiness record, and the retained v128 receipt history.

## 06-line-counts.png

- Command: count C++ and header lines under `src/`, `include/`, and `tests/`.
- Result: the largest files are `src/command.cpp` at 800 lines, `tests/command_tests.cpp` at 792 lines, `tests/release_verification_manifest_tests.cpp` at 779 lines, `src/runtime_evidence_receipts.cpp` at 752 lines, and `tests/runtime_smoke_evidence_tests.cpp` at 734 lines. The new v129 source file is 368 lines and the new v129 test file is 277 lines.
- Meaning: no file crossed 1000 lines in this version. The biggest files are still below the earlier split threshold, so this release only needed one focused retention-check split rather than a wider refactor.

## Process And Cleanup Notes

- No `minikv_server.exe` or other long-running service was started.
- CLI smoke used a short-lived `minikv_cli.exe` process and exited with `QUIT`.
- `cmake-build-v129`, `d/129/_logs`, and `d129_smokejson.json` are temporary verification artifacts and should be removed before final handoff.
