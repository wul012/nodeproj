# mini-kv v133 命令归档说明

本目录记录 mini-kv v133 的最终验证截图。截图使用 Playwright MCP 生成；由于 MCP 阻止直接访问 `file://`，本次临时用 `127.0.0.1:6133` 只读服务展示 `d/133/_logs/` 中的日志页面，截图完成后已停止该服务。没有使用文本渲染 PNG 回退。

## 01-cmake-configure.png

命令：

```powershell
cmake -S . -B cmake-build-v133 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug
```

结果：CMake 配置成功，构建文件写入 `cmake-build-v133`。

意义：证明 v133 的 CMake 目标、源文件、测试注册可以被当前 CLion/MinGW 工具链识别。

## 02-cmake-build.png

命令：

```powershell
cmake --build cmake-build-v133 --parallel 2
```

结果：所有目标构建成功，包括新增的 `minikv_runtime_shell_post_decision_plan_intake_receipt_tests`。

意义：证明 v133 新增 receipt 实现、公共声明、`SMOKEJSON` 挂载和测试代码都能通过编译链接。

## 03-targeted-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v133 -R "runtime_shell_post_decision_plan_intake_receipt_tests|runtime_shell_decision_record_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests|runtime_no_start_no_write_follow_up_tests|operator_window_no_start_no_write_receipt_tests|manual_sandbox_dry_run_command_non_participation_receipt_tests|manual_sandbox_connection_precheck_non_participation_receipt_tests|cred_res_exec_denied_retention_check_tests|disabled_runtime_shell_receipt_tests|disabled_runtime_shell_candidate_gate_receipt_tests" --output-on-failure
```

结果：12/12 通过。

意义：覆盖 v133 新回执、v132 历史回执、滚动 `node_consumption` 字段、runtime smoke fixture、release manifest，以及早期手工 sandbox 回执的历史语义。旧测试已改为检查历史语义仍存在，不再把旧版本句子误当成永久首句。

## 04-full-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v133 --output-on-failure
```

结果：60/60 通过。

意义：证明 v133 没有破坏现有 store、WAL、snapshot、TCP、fixture、release manifest、credential resolver receipt 链和 runtime evidence 聚合测试。

## 05-cli-smoke.png

命令：

```powershell
SMOKEJSON
CHECKJSON SMOKEJSON
GET restore:real-read-token
QUIT
```

结果摘要：

- `SMOKEJSON` 暴露 `credential_resolver_runtime_shell_post_decision_plan_intake_non_participation_receipt`。
- v133 digest 为 `fnv1a64:d2739fd08da1653e`。
- `selected_continuation_decision=continue-blocked-planning`。
- `ready_for_node_v302_post_decision_plan_intake_upstream_echo_verification=true`。
- `ready_for_node_v302_runtime_shell_implementation=false`。
- credential、raw endpoint、HTTP/TCP、ledger/schema、LOAD/COMPACT/RESTORE/SETNXEX、audit/order authority 边界均保持关闭。
- `CHECKJSON SMOKEJSON` 仍是只读元数据检查。
- `GET restore:real-read-token` 返回 `(nil)`。

意义：证明真实 CLI 输出与 fixture/manifest/test 预期一致，没有写入或 restore 痕迹。

## 06-line-counts.png

命令：统计当前 tracked 与 v133 新增 C++/头文件的前 25 个最大文件行数。

结果：最大文件为 `tests/release_verification_manifest_tests.cpp`，841 行；`tests/runtime_smoke_evidence_tests.cpp` 为 764 行；最大生产源码 `src/runtime_evidence_receipts.cpp` 为 706 行；v133 新增实现 `src/runtime_credential_resolver_runtime_shell_post_decision_plan_receipts.cpp` 为 384 行。

意义：当前没有超过 1000 行的 C++/头文件；v133 选择新建独立 receipt 源文件而不是继续膨胀旧文件，符合可维护拆分要求。

## 后台进程

本次归档截图期间临时启动了一个只读 Python HTTP 服务：

```text
127.0.0.1:6133
```

该服务仅用于向 Playwright MCP 展示 `d/133/_logs/` 中的 HTML 日志页面；截图完成后已停止，没有保留后台进程。
