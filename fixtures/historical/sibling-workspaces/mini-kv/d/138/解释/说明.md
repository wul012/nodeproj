# v138 归档说明

本次截图都来自 `d/138/_logs/html/` 下的临时证据页，页面内容由配置、构建、测试和 smoke 日志整理而来。临时 HTTP 服务仅用于截图，已在截图完成后停止。

## 截图对应关系

- `01-cmake-configure.png`：`cmake -S . -B cmake-build-v138 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`，确认 v138 构建目录重新生成成功。
- `02-cmake-build.png`：`cmake --build cmake-build-v138 --parallel 2`，确认全量目标构建完成。
- `03-targeted-ctest.png`：定向 CTest 4 个关键测试，确认新回执测试、runtime smoke、manifest 和 SMOKEJSON 入口都通过。
- `04-full-ctest.png`：全量 CTest 65/65 通过，确认这版没有拖坏其他回归测试。
- `05-cli-smoke.png`：CLI smoke，展示 `SMOKEJSON`、`CHECKJSON SMOKEJSON` 和 `GET restore:real-read-token` 的关键结果，以及 `EXIT_CODE=0`。
- `06-tcp-smoke.png`：TCP smoke，展示 `minikv_server` / `minikv_client` 连接后的 `SMOKEJSON`、`INFOJSON`、`HEALTH`、`GET restore:real-read-token` 和 `EXIT_CODE=0`。

## 保留与清理

- 保留：`d/138/图片/`、`d/138/解释/说明.md`
- 清理：`d/138/_logs/`、临时 HTTP 服务

