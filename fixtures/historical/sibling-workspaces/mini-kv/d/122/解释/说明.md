# mini-kv v122 命令结果归档说明

## 本版范围

本轮先只读查看了 Node 计划入口 `D:\nodeproj\orderops-node\docs\plans\README.md`、计划索引 `D:\nodeproj\orderops-node\docs\plans2\README.md`，以及当前计划 `D:\nodeproj\orderops-node\docs\plans2\v280-post-status-routes-quality-roadmap.md`。

计划判断合理：Node v281 已完成 credential resolver approval-required implementation readiness review，mini-kv v122 的合理职责是补充 approval-required implementation readiness non-participation receipt，供后续 Node v282 与 Java v116 一起验证。mini-kv 本版只提供只读证据，不实现 resolver、不读取 credential、不解析 raw endpoint、不连接 managed audit、不执行 LOAD/COMPACT/RESTORE/SETNXEX、不写 storage/approval ledger/schema。

## 截图说明

| 截图 | 命令 | 结果与意义 |
|---|---|---|
| `01-cmake-configure.png` | `cmake -S . -B cmake-build-v122 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug` | 配置成功，生成 v122 构建目录。 |
| `02-cmake-build.png` | `cmake --build cmake-build-v122 --parallel 2` | 构建成功，新测试目标 `minikv_credential_resolver_impl_readiness_receipt_tests` 生成。 |
| `03-targeted-tests.png` | `ctest --test-dir cmake-build-v122 --output-on-failure -R 'credential_resolver_approval_required_implementation_readiness_non_participation_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|command_tests'` | 本版相关 4 个目标测试全部通过，覆盖新回执、SMOKEJSON、release manifest 和命令输出。 |
| `04-full-ctest.png` | `ctest --test-dir cmake-build-v122 --output-on-failure` | 全量 51/51 通过，确认 v122 没有破坏既有 WAL、TCP、fixture、receipt 和 runtime evidence 测试。 |
| `05-tcp-smoke.png` | 启动 `minikv_server` 后用 `minikv_client` 执行 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT` | 真实 TCP 只读 smoke 通过；输出包含 v122 新回执、digest `fnv1a64:b6e1efa8878b64d6`，`GET restore:real-read-token` 仍为 `(nil)`。 |
| `06-line-counts.png` | 统计 v122 相关源码、测试、fixture 行数 | 记录拆分后的文件规模，确认新增实现文件约 391 行，`command.cpp` 保持 800 行。 |

## 进程与清理

真实 TCP smoke 使用本地 `minikv_server` 监听 `127.0.0.1:6532`。验证结束后已停止该进程，没有保留后台服务。

构建目录 `cmake-build-v122` 只作为本版本地构建产物存在；关键命令结果已转成 PNG 放入 `d/122/图片/`。
