# mini-kv v135 运行截图说明

本版归档使用 Playwright MCP 截图。MCP 已验证可用；直接打开 `file://` 页面被 MCP 安全策略阻止，因此本次把命令日志放到临时本地 HTTP 页面后截图。该 HTTP 服务只用于截图，截图完成后已停止。

## 01-cmake-configure.png

命令：

```powershell
$cmake -S . -B cmake-build-v135 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug
```

结果：CMake configure/generate 成功，构建目录为 `cmake-build-v135`。

意义：确认 v135 新增 receipt 源文件、测试目标和 CMake target 可以被当前 CLion MinGW 工具链识别。

## 02-cmake-build.png

命令：

```powershell
$cmake --build cmake-build-v135 --parallel 2
```

结果：全量构建成功，包括 `minikv_cli`、`minikv_server`、`minikv_client` 和新增的 `minikv_approval_prerequisite_artifact_intake_receipt_tests`。

意义：确认 v135 新增实现没有破坏现有 C++ 编译和链接。

## 03-targeted-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v135 -R "approval_prerequisite_artifact_intake_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests" --output-on-failure
```

结果：4/4 targeted tests passed。

意义：确认 v135 回执、runtime smoke fixture、release manifest 和 `SMOKEJSON` 命令入口的关键契约都稳定。

## 04-full-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v135 --output-on-failure
```

结果：62/62 tests passed。

意义：确认新增 v135 回执没有破坏既有 store、WAL、TCP、restore evidence、runtime evidence 和历史 credential resolver receipt 测试。

## 05-cli-smoke.png

命令：

```text
SMOKEJSON
INFOJSON
STORAGEJSON
HEALTH
CHECKJSON SMOKEJSON
CHECKJSON LOAD data/v135.snap
CHECKJSON COMPACT
CHECKJSON SETNXEX restore:real-read-token 30 value
GET restore:real-read-token
QUIT
```

结果：CLI 输出包含 `credential_resolver_approval_prerequisite_artifact_intake_non_participation_receipt`、receipt digest `fnv1a64:19c30848a77ec251`、Node v306 artifact digest `72f3e90606e40a978611fa4b8596c76c3ebc468124c4ead7bb9c4833130ee9c2`，并保持 `write_commands_executed=false`、`runtime_write_observed=false`。`CHECKJSON SETNXEX` 只返回风险解释，随后 `GET restore:real-read-token` 返回 `(nil)`。

意义：确认真实 CLI 入口能读到 v135 回执，且危险命令只被解释、不被执行。

## 06-tcp-smoke.png

命令：

```text
minikv_server.exe 6535 127.0.0.1
minikv_client.exe 127.0.0.1 6535 5000 --connect-retries 10 --retry-delay-ms 100
```

客户端执行的命令与 CLI smoke 相同。

结果：TCP 客户端连接 `127.0.0.1:6535` 成功，读到同一份 v135 receipt/digest/artifact digest，`CHECKJSON SETNXEX` 仍只输出风险解释，`GET restore:real-read-token` 返回 `(nil)`。验证结束后 mini-kv server 已停止。

意义：确认真实 TCP 服务端/客户端路径也能消费 v135 只读证据，并保持 no-write/no-restore 边界。

## 清理记录

- 已停止截图用临时 HTTP 服务。
- 已停止 TCP smoke 用 `minikv_server.exe`。
- 最终交付应保留 `d/135/图片/` 和本说明文档。
- `d/135/_logs/`、`cmake-build-v135/` 和 `.playwright-mcp/` 仅为本轮中间验证材料，最终清理时删除。
