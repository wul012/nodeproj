# mini-kv v140 命令归档说明

本目录记录 mini-kv v140 endpoint-handle allowlist approval contract 非参与回执的关键命令结果。截图使用 Playwright MCP 生成；因 MCP 浏览器阻止 `file://` 页面，截图阶段临时启动了本地只读 HTTP 服务，仅用于渲染 `d/140/_logs` 下的临时证据页，截图完成后需要停止并清理。

## 01-cmake-configure.png

- 命令：`cmake -S . -B cmake-build-v140 -G Ninja -DCMAKE_MAKE_PROGRAM=... -DCMAKE_CXX_COMPILER=...`
- 结果：配置和生成成功，构建目录写入 `D:/C/mini-kv/cmake-build-v140`。
- 意义：证明 v140 使用本机 CLion/MinGW/CMake 工具链可以生成新的 Ninja 构建树。

## 02-cmake-build.png

- 命令：`cmake --build cmake-build-v140 --clean-first`
- 结果：先清理 184 个构建产物，再完整构建 184/184 个目标，包含新增 `minikv_endpoint_handle_allowlist_approval_contract_receipt_tests.exe`。
- 意义：证明新增 v140 源文件、测试目标、SMOKEJSON 聚合入口和既有目标可以从干净状态完整编译。

## 03-targeted-tests.png

- 命令：`ctest --test-dir cmake-build-v140 -R "endpoint_handle_allowlist_approval_contract_receipt_tests|credential_handle_approval_contract_receipt_tests|smokejson_command_receipt_tests" --output-on-failure`
- 结果：3/3 通过。
- 意义：定向覆盖新 v140 receipt、旧 v139 rolling diagnostics 兼容性，以及 `SMOKEJSON` 聚合入口。

## 04-full-ctest.png

- 命令：`ctest --test-dir cmake-build-v140 --output-on-failure`
- 结果：67/67 通过。
- 意义：证明 v140 新增回执没有破坏 store、WAL、TCP、runtime fixture、release manifest、历史 credential resolver receipt 等既有测试链。

## 05-cli-smoke.png

- 命令：`SMOKEJSON`、`CHECKJSON SMOKEJSON`、`GET restore:real-read-token`、`QUIT`
- 结果：`SMOKEJSON` 暴露 `credential_resolver_endpoint_handle_allowlist_approval_contract_non_participation_receipt`，回执 digest 为 `fnv1a64:c8b6521b3211cb8a`，Node v320 contract digest 为 `4657f89caa6866bad87db284dc98efd8d09a6538d8d735535e6a7e6d4d6c33e5`；`CHECKJSON SMOKEJSON` 保持 `read_only=true`、`execution_allowed=false`、`write_command=false`；`GET restore:real-read-token` 返回 `(nil)`。
- 意义：真实 CLI smoke 证明 v140 是只读证据输出，没有写 restore token，没有执行 LOAD/COMPACT/RESTORE/SETNXEX，也没有打开 endpoint/raw URL/network/write 权限。
