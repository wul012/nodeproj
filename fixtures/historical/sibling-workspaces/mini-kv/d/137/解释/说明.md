# mini-kv v137 命令归档说明

本目录记录 v137 的最终验证截图。截图通过 Playwright MCP 对本地临时 HTML 日志页截取生成；临时日志页来自 `d/137/_logs`，验证结束后会清理。

## 01-cmake-configure.png

命令：`cmake -S . -B cmake-build-v137 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`

结果：配置成功，生成 v137 的 CMake build files。

意义：证明新增 v137 receipt 源文件、测试目标和现有 CMake 工程结构可以被当前 MinGW/CMake 工具链识别。

## 02-cmake-build.png

命令：`cmake --build cmake-build-v137 --parallel 2`

结果：构建成功，包括 `minikv`、`minikv_cli`、`minikv_server`、`minikv_client` 和新增 `minikv_human_approval_post_echo_decision_receipt_tests`。

意义：证明新增 C++ 回执实现、SMOKEJSON 挂载和测试代码均可编译链接。

## 03-ctest-targeted.png

命令：`ctest --test-dir cmake-build-v137 -R "smokejson_command_receipt_tests|release_verification_manifest_tests|runtime_smoke_evidence_tests|human_approval_artifact_review_receipt_tests|human_approval_post_echo_decision_receipt_tests|approval_prerequisite_artifact_intake_receipt_tests|runtime_shell_chain_stop_prerequisite_receipt_tests|runtime_shell_post_decision_plan_intake_receipt_tests|runtime_shell_decision_record_receipt_tests" --output-on-failure`

结果：9/9 targeted tests passed。

意义：重点验证 v137 回执、runtime smoke fixture、release manifest、SMOKEJSON 滚动消费语义，以及 v132-v137 相关 credential resolver receipt 链没有回归。

## 04-ctest-full.png

命令：`ctest --test-dir cmake-build-v137 --output-on-failure`

结果：64/64 tests passed。

意义：证明 v137 新增 receipt 没有破坏既有 WAL、TCP、restore、runtime evidence、fixture、manifest 和 command behavior 测试。

## 05-cli-smoke.png

命令：`minikv_cli` 依次执行 `SMOKEJSON`、`GET restore:real-read-token`、`CHECKJSON SMOKEJSON`、`CHECKJSON SETNXEX`、`QUIT`。

结果：`SMOKEJSON` 暴露 `credential_resolver_human_approval_artifact_review_post_echo_decision_gate_non_participation_receipt`，receipt digest 为 `fnv1a64:ab2ef6306f362184`；`GET restore:real-read-token` 返回 `(nil)`；`CHECKJSON SETNXEX` 只返回风险说明和用法，不执行写入。

意义：证明 CLI 真实运行路径可读取 v137 回执，同时没有写入 restore token、没有执行 SETNXEX、没有打开 runtime shell 或 credential/endpoint/network 路径。

## 06-tcp-smoke.png

命令：启动 `minikv_server 6537 127.0.0.1`，再用 `minikv_client 127.0.0.1 6537 5000 --connect-retries 10 --retry-delay-ms 100` 执行 `SMOKEJSON`、`GET restore:real-read-token`、`CHECKJSON SMOKEJSON`、`QUIT`。

结果：TCP 路径同样暴露 v137 receipt、digest `fnv1a64:ab2ef6306f362184`、Node v310 decision gate state，并保持 `write_commands_executed=false` 和 `runtime_write_observed=false`。验证结束后服务端已停止。

意义：证明真实 TCP 读路径也能交付 v137 只读证据，且没有留下后台服务或写路径副作用。
