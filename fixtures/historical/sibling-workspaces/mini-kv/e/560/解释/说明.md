# mini-kv v560 command archive explanation

route-preview-import-preflight-release-package

The v560 archive records read-only downstream consumption evidence. The TCP import preflight smoke was stopped after verification, and no operator evidence value, manual entry, runtime payload, synthetic evidence, credential, secret, raw endpoint, active router, write routing, WAL, restore, load, compact, service start, or live read execution was added.
