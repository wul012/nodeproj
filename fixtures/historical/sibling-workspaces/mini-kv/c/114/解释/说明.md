# mini-kv v114 command archive

## 01-cmake-configure.png
Command: cmake configure with CLion bundled CMake/Ninja into cmake-build-v114.
Result: configure completed and generated build files.
Meaning: validates the new v114 credential resolver receipt test target is registered in CMake.

## 02-cmake-build.png
Command: cmake --build cmake-build-v114 --parallel 2.
Result: all binaries and tests built successfully.
Meaning: confirms the credential resolver non-participation receipt, SMOKEJSON formatter wiring, fixtures, and tests compile together.

## 03-targeted-tests.png
Command: targeted CTest run for credential_resolver_non_participation_receipt_tests, runtime_smoke_evidence_tests, release_verification_manifest_tests, and command_tests.
Result: all targeted tests passed.
Meaning: protects the new v114 standalone fixture, live SMOKEJSON surface, release manifest wiring, and command diagnostics without expanding into unrelated runtime behavior.

## 04-full-ctest.png
Command: full ctest --test-dir cmake-build-v114 --output-on-failure.
Result: all registered tests passed.
Meaning: confirms the v114 receipt preserves historical mini-kv behavior across the complete local test suite.

## 05-real-tcp-smoke.png
Command: started minikv_server 6515 127.0.0.1, then used minikv_client to run SMOKEJSON, INFOJSON, STORAGEJSON, HEALTH, GET restore:real-read-token, and QUIT.
Result: read-only runtime smoke completed; SMOKEJSON exposed credential_resolver_non_participation_receipt and Node v261 diagnostics; GET restore:real-read-token remained (nil).
Meaning: proves the receipt is visible from a real server/client path without credential resolution, secret provider instantiation, raw endpoint parsing, external requests, writes, restore, compact, load, SETNXEX, or managed-audit participation.

## 06-line-counts.png
Command: counted changed large/source files after v114.
Result: src/runtime_evidence_receipts.cpp is 752 lines, src/runtime_sandbox_receipts.cpp is 915 lines, src/command_response_formatters.cpp is 730 lines, and the new v114 test is 244 lines.
Meaning: records that the existing sandbox receipt module can absorb v114 while staying below the 1000-line refactor threshold.

Background process cleanup: the v114 smoke server was stopped after validation.
