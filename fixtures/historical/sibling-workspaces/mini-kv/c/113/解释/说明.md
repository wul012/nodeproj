# mini-kv v113 command archive

## 01-cmake-configure.png
Command: cmake configure with CLion bundled CMake/Ninja into cmake-build-v113.
Result: configure completed and generated build files.
Meaning: validates the new runtime sandbox receipt source and v113 test target are registered in CMake.

## 02-cmake-build.png
Command: cmake --build cmake-build-v113 --parallel 2.
Result: all binaries and tests built successfully.
Meaning: confirms the sandbox endpoint handle non-participation receipt, SMOKEJSON formatter wiring, and runtime sandbox receipt split compile together.

## 03-targeted-tests.png
Command: targeted CTest run for sandbox_endpoint_handle_non_participation_receipt_tests, runtime_smoke_evidence_tests, release_verification_manifest_tests, and command_tests.
Result: all targeted tests passed.
Meaning: protects the new v113 standalone fixture, live SMOKEJSON surface, release manifest wiring, and command diagnostics without expanding into unrelated runtime behavior.

## 04-full-ctest.png
Command: full ctest --test-dir cmake-build-v113 --output-on-failure.
Result: all registered tests passed.
Meaning: confirms the v113 receipt and refactor preserve historical mini-kv behavior across the complete local test suite.

## 05-real-tcp-smoke.png
Command: started minikv_server 6513 127.0.0.1, then used minikv_client to run SMOKEJSON, INFOJSON, STORAGEJSON, HEALTH, GET restore:real-read-token, and QUIT.
Result: read-only runtime smoke completed; SMOKEJSON exposed sandbox_endpoint_handle_non_participation_receipt and Node v259 diagnostics; GET restore:real-read-token remained (nil).
Meaning: proves the receipt is visible from a real server/client path without write, restore, compact, load, SETNXEX, raw endpoint parsing, credential reading, external requests, or managed-audit participation.

## 06-line-counts.png
Command: counted changed large/source files after the split.
Result: src/runtime_evidence_receipts.cpp is 752 lines, src/runtime_sandbox_receipts.cpp is 665 lines, src/command_response_formatters.cpp is 726 lines, and the new v113 test is 230 lines.
Meaning: records that the runtime receipt split reduced the former large receipt file below the 1000-line target while keeping the sandbox-specific receipts in a focused companion source file.

Background process cleanup: the v113 smoke server was stopped after validation.
