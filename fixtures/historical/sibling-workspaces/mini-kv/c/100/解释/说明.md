# mini-kv v100 Archive Notes

## Plan Read And Scope

This round first read `D:\nodeproj\orderops-node\docs\plans\README.md` and `D:\nodeproj\orderops-node\docs\plans\v234-post-blocked-execution-rehearsal-roadmap.md` in read-only mode. The plan asks mini-kv v100 to add a `current runtime fixture rolling evidence guard` for Node v235 manual sandbox connection precondition intake.

Scope judgment: reasonable. v100 only records and tests the rule that current runtime evidence may roll with the release, while historical consumed digest/receipt anchors must remain stable. It does not do a command dispatch table refactor, does not touch WAL/snapshot/restore behavior, does not execute LOAD/COMPACT/RESTORE/SETNXEX, and does not make mini-kv a managed audit storage backend.

## Screenshots And Commands

- `01-cmake-configure.png`: `cmake -S . -B cmake-build-v100 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`; CMake configure succeeded for v100.
- `02-cmake-build.png`: `cmake --build cmake-build-v100 --parallel 2`; all targets built, including `minikv_current_runtime_fixture_rolling_guard_tests`.
- `03-ctest.png`: `ctest --test-dir cmake-build-v100 --output-on-failure`; final result was 33/33 passed.
- `04-real-tcp-smoke.png`: started `minikv_server 6500 127.0.0.1`, then used `minikv_client` to run `SMOKEJSON`, `INFOJSON`, `STORAGEJSON`, `HEALTH`, `GET restore:real-read-token`, and `QUIT`. The response showed `0.100.0`, `mini-kv-live-read-v100`, and `(nil)` for the restore token. The server was stopped after verification.

## Main Changes

- Added `fixtures/release/current-runtime-fixture-rolling-guard.json` for v100 current version, artifact path, live-read session echo, current receipt digests, and historical consumed digest anchors from v84, v85, v86, v87, v88, v89, v90, v95, and v96.
- Added `tests/current_runtime_fixture_rolling_guard_tests.cpp` and registered it in CMake. The test verifies the guard fixture, runtime smoke fixture, release manifest, and real `SMOKEJSON` rolling/stability rule.
- Updated `fixtures/release/runtime-smoke-evidence.json` and `fixtures/release/verification-manifest.json` to publish the v100 guard, targeted test, and Node v235 consumption hint.
- Rolled current evidence to `0.100.0`, `v100`, `c/100/`, `mini-kv-live-read-v100`, and the v100 current digest set.

## Large File Line Counts

- `src/command.cpp`: 683 lines.
- `src/managed_audit_receipts.cpp`: 971 lines.
- `src/runtime_evidence_receipts.cpp`: 450 lines.
- `tests/command_tests.cpp`: 990 lines.
- `tests/runtime_smoke_evidence_tests.cpp`: 644 lines.
- `tests/release_verification_manifest_tests.cpp`: 593 lines.
- `tests/current_runtime_fixture_rolling_guard_tests.cpp`: 155 lines.

Conclusion: the main C++ large files are now around or below 1000 lines. This version did not expand into another large refactor; it stayed as a v100 single-topic guard.

## Cleanup Record

- The `minikv_server` process started for real TCP smoke was stopped.
- No background mini-kv process was left running.
- Removed intermediate validation outputs: `cmake-build-v100` and `c/100/.tmp`.
- Kept the four PNG screenshots under `c/100/images-directory` and this note under `c/100/explanation-directory` as the version archive.
