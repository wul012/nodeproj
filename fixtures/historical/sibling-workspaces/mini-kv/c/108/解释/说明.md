# mini-kv v108 运行归档说明

本目录记录 v108 `manual_sandbox_connection_precheck_non_participation_receipt` 的关键验证输出。v108 只做 Node v245 precheck packet 的只读非参与回执，不打开 managed audit connection，不读取 credential value，不执行 LOAD/COMPACT/RESTORE/SETNXEX，不写 storage 或 managed audit state。

## 图片说明

- `01-cmake-configure.png`：执行 `cmake -S . -B cmake-build-v108 -G Ninja`，确认 CMake 配置和生成成功。
- `02-cmake-build.png`：执行 `cmake --build cmake-build-v108 --parallel 2`，确认库、CLI/server/client 和 37 个测试程序构建成功。
- `03-ctest.png`：执行 `ctest --test-dir cmake-build-v108 --output-on-failure`，确认 37/37 测试通过。
- `04-runtime-smoke.png`：启动 `minikv_server 6508 127.0.0.1` 后用 `minikv_client 127.0.0.1 6508 5000 --connect-retries 10 --retry-delay-ms 100` 发送 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。验证 `manual_sandbox_connection_precheck_non_participation_receipt` 存在，`current_artifact_path_hint="c/108/"`，`source_precheck_item_count=7`，`source_required_operator_field_count=6`，并且 `mini_kv_auto_start_allowed=false`、`storage_write_allowed=false`、`credential_value_read_allowed=false`、`load_restore_compact_executed=false`、`setnxex_execution_allowed=false`。`GET restore:real-read-token` 返回 `(nil)`。

## 进程

本次 runtime smoke 临时启动 `minikv_server.exe`，验证结束后已停止，没有保留后台进程。
