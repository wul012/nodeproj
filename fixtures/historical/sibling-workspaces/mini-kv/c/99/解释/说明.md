# mini-kv v99 验证说明

## 计划读取与范围判断

本轮推进前已只读查看：

- `D:\nodeproj\orderops-node\docs\plans\README.md`
- `D:\nodeproj\orderops-node\docs\plans\v231-post-preflight-verification-roadmap.md`

计划判断合理。mini-kv v99 的合适范围是继续 `execute-with-wal` helper 的低风险覆盖或回归测试；本版实际执行为补齐 WAL helper 回归护栏，并同步 `0.99.0` / `v99` / `c/99/` 当前证据。没有做 dispatch table 大重构，没有触碰 snapshot / restore 核心，没有扩大 Node / Java / managed audit 边界。

## 截图说明

- `01-cmake-configure.png`：执行 `cmake -S . -B cmake-build-v99 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`，结果为配置成功并生成 v99 Debug 构建目录。
- `02-cmake-build.png`：执行 `cmake --build cmake-build-v99 --parallel 2`，结果为所有目标构建成功。
- `03-ctest.png`：执行 `ctest --test-dir cmake-build-v99 --output-on-failure`，结果为 `32/32` 测试通过。
- `04-smoke.png`：启动 `minikv_server 6499 127.0.0.1`，再用 `minikv_client 127.0.0.1 6499 5000 --connect-retries 10 --retry-delay-ms 100` 执行 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。结果显示版本为 `0.99.0`，`read_only=true`，`execution_allowed=false`，`restore_execution_allowed=false`，`order_authoritative=false`，`GET restore:real-read-token` 返回 `(nil)`，服务端验证后已停止。

## 本版行为证据

`tests/wal_tests.cpp` 新增了 `SET`、`SETNXEX`、`DEL`、`EXPIRE` 的 usage error、missing key、expired key miss 回归断言。测试通过 WAL record 数量确认这些失败或 no-op 分支不会写入空 WAL 记录，同时成功写路径仍保持 append-before-mutation 顺序。

release fixture 和 runtime evidence 已滚到 `0.99.0` / `v99`，供 Node v234 blocked execution rehearsal 只读消费。v99 仍保持 no auto-start、no credential value read、no schema rehearsal、no managed audit write、no restore/load/compact、no Java order authority。

## 大文件行数

当前主要 C++ 源码/测试大文件行数如下：

- `src/managed_audit_receipts.cpp`：929 行
- `tests/command_tests.cpp`：860 行
- `tests/runtime_smoke_evidence_tests.cpp`：624 行
- `src/tcp_server.cpp`：606 行
- `tests/release_verification_manifest_tests.cpp`：572 行
- `src/command.cpp`：557 行
- `src/line_editor.cpp`：545 行
- `src/command_response_formatters.cpp`：538 行
- `tests/tcp_server_tests.cpp`：520 行
- `src/client_main.cpp`：485 行
- `src/wal.cpp`：476 行
- `tests/wal_tests.cpp`：418 行

1000 行左右的控制要求合理；本轮后主要 C++ 源码和测试文件都低于 1000 行。`src/managed_audit_receipts.cpp` 和 `tests/command_tests.cpp` 已接近上限，后续如果继续增长，优先按 receipt family 或 fixture/test helper 再拆，不建议在 v99 里硬做中风险大拆。

## 清理记录

验证中启动过 `minikv_server`，smoke 结束后已停止。提交前已删除 `cmake-build-v99` 和 `c/99/.tmp`，保留正式归档目录 `c/99/图片/` 与 `c/99/解释/`。
