# mini-kv v112 command archive

## 01-cmake-configure.png
Command: cmake configure with CLion bundled CMake/Ninja into cmake-build-v112.
Result: configure completed and generated build files.
Meaning: validates the new v112 receipt target is registered in CMake.

## 02-cmake-build.png
Command: cmake --build cmake-build-v112 --parallel 2.
Result: all binaries and tests built successfully.
Meaning: confirms the fake transport dry-run packet receipt implementation, SMOKEJSON formatter, and new CTest target compile together.

## 03-ctest-all.png
Command: full ctest --test-dir cmake-build-v112 --output-on-failure.
Result: all registered tests passed.
Meaning: protects historical mini-kv behavior plus the new fake transport packet non-participation fixture and SMOKEJSON contract.

## 04-real-server-client-smoke.png
Command: started minikv_server 6512 127.0.0.1, then used minikv_client to run SMOKEJSON, INFOJSON, STORAGEJSON, HEALTH, GET restore:real-read-token, and QUIT.
Result: read-only runtime smoke completed; GET restore:real-read-token remained (nil).
Meaning: proves the receipt is visible from a real server/client path without write, restore, compact, load, SETNXEX, credential, external request, cleanup artifact, or managed-audit side effects.

## 05-line-counts.png
Command: counted changed large/source files.
Result: records the post-v112 file sizes for refactor monitoring.
Meaning: src/runtime_evidence_receipts.cpp is now near the next split threshold.

Background process cleanup: the v112 smoke server was stopped after validation.
