# Java v97 release approval rehearsal builder chain refactor

This fixture preserves the key Node-consumed explanation when the sibling Java workspace is not present in CI.

- release approval rehearsal builder chain refactor
- 外部契约和 digest 不变
- read-only/no-ledger/no-SQL/no-connection 边界
