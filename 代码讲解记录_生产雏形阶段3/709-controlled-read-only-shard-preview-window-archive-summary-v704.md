# Node v704 code walkthrough: window archive summary

`WINDOW_ARCHIVE_SUMMARY` records that future handoff should use compact pass/blocker lines.

The stage prevents later implementations from duplicating full evidence into multiple summaries.

Focused tests verify the ledger digest covers this stage.
