# v2003 code walkthrough

`operationApprovalEvidence.ts` is now a compatibility layer.

It exports the same public service, report, verification, renderer, and type names while the implementation lives in smaller files.
