# v2013 code walkthrough

mini-kv CHECK readers extract contract fields used by evidence verification.

The verification layer still blocks invalid available evidence if read-only, execution-allowed, digest, or side-effect count checks drift.
