# v2004 code walkthrough

`operationApprovalEvidenceTypes.ts` contains the report and verification contracts.

Downstream modules such as `operationApprovalHandoffBundle.ts` still receive the same exported type names from the public entrypoint.
