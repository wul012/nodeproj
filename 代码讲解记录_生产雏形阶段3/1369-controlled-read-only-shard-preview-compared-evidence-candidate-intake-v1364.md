# v1364 code walkthrough

The operator-provenance intake slot makes provenance a separate intake concern. The slot carries the source section code, blocker code, source node version, and required fields from the v1361 blueprint.

The corresponding guard blocks approval grant and signed approval. This is important because the presence of provenance requirements must not imply that an operator has already approved the material.
