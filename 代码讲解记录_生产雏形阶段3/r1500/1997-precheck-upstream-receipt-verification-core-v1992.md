# v1992 code walkthrough

The core module builds the final profile from source references, finalized checks, and policy messages.

It computes the verification digest, receipt verification object, summary counters, evidence endpoints, and next actions.
