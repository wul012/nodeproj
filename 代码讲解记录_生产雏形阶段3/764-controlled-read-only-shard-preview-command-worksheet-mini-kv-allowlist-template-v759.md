# Node v759 code walkthrough: command worksheet mini-kv allowlist template

v759 adds the mini-kv allowlist template.

Key code:

- `WORKSHEET_MINI_KV_ALLOWLIST_TEMPLATE` maps to `REHEARSAL_COMMAND_ALLOWLIST_CHECK`.
- The worksheet target coverage includes `policy`.

The command worksheet keeps mutating mini-kv commands outside the manual window.

Verification: covered by the command worksheet focused test.
