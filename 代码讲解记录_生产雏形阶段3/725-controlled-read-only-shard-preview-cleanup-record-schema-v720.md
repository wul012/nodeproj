# Node v720 code walkthrough: cleanup record schema

`CLEANUP_RECORD_SCHEMA` requires `stopped=true` or a concrete cleanup blocker for every owned process.

This mirrors the stage ledger cleanup gate in operator-readable form.
