# v1476 code walkthrough

v1476 records that no live integration startup is needed for the refactor. The only server process used later is the short-lived Node smoke server after build.

That process is stopped during cleanup.
