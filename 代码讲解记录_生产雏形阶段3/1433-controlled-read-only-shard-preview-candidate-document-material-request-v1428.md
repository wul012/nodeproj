# v1428 code walkthrough

The redaction-secret-boundary item requests redaction and no-secret evidence. The aggregate continues to expose `acceptsSyntheticEvidence=false` and `containsSecretValue=false`.

The route profile renders those flags without material content.
