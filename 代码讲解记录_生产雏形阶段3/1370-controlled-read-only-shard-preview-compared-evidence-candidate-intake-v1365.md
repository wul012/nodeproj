# v1365 code walkthrough

The comparison-result intake slot connects the future real candidate document to the v1351 evaluation preflight lineage through the v1361 candidate blueprint. It is still a read-only contract, not an evaluator.

The gates include `candidateEvaluationStillBlocked`, `allGuardsBlockCandidateEvaluation`, and `nextStepRequiresRealComparedPackageEvidenceCandidateDocument`, so the aggregate can be ready while evaluation remains false.
