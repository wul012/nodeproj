# v2028 code walkthrough

`createMiniKvV101Reference` reads the follow-up JSON and derives runtime no-start/no-write readiness.

The accepted follow-up list remains explicit because historical consumer hints shifted across later Node versions.
