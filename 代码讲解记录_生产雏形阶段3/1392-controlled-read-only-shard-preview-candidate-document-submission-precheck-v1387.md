# v1387 code walkthrough

The submission precheck type layer introduces the `Node v1387` through `Node v1411` checkpoint sequence. The source-lineage checkpoint maps the v1386 source-lineage request item and check into a submission precheck record.

Readiness requires the source request package to be ready while real documents and payloads remain absent.
