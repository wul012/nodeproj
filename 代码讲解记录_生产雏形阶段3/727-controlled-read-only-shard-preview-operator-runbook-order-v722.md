# Node v722 code walkthrough: operator runbook order

`OPERATOR_RUNBOOK_ORDER` makes the future live read-only window follow the package's section order.

This avoids ad hoc command ordering during the manual run.
