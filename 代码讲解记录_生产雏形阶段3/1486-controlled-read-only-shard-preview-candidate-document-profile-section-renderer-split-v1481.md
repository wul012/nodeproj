# v1481 code walkthrough

v1481 closes the range with the catalog at `Node v1481` and 210 modules. The candidate document profile section renderer is cataloged before the stable profile entry point.

The next candidate document feature can now build on a smaller local renderer boundary.
