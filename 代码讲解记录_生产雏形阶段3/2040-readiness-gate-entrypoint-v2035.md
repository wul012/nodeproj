# v2035 code walkthrough

The entrypoint orchestrates the batch: load source, create evidence files, create snippets, read mini-kv follow-up, create references, build gate, checks, messages, and final profile.

The public exports are stable.
