# v1989 code walkthrough

The references module keeps helper functions for evidence files, snippets, JSON object reads, typed field extraction, snippet matching, and array equality.

These helpers are private because their only job is to support this report's Java and mini-kv evidence readers.
