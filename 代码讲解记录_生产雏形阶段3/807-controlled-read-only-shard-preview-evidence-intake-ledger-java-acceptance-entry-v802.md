# Node v802 code walkthrough: evidence intake ledger Java acceptance entry

v802 adds Java acceptance intake.

`INTAKE_JAVA_ACCEPTANCE_ENTRY` maps to `EVIDENCE_JAVA_ACCEPTANCE_RULE` and keeps Java evidence acceptance tied to an explicit read-only status.

Verification: covered by the evidence intake ledger focused test.
