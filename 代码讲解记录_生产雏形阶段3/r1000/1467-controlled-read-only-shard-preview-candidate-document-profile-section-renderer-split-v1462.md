# v1462 code walkthrough

v1462 moves `renderComparedEvidenceCandidateDocumentMaterialRequestPackageProfileEntries`. The function still prints v1446 material request counts and disabled material intake status.

This is a direct migration with no data shape change.
