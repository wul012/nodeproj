# v1442 code walkthrough

The runtime-payload-absence-attestation item asks for proof that material contains no runtime payload. Runtime payload import stays blocked.

The package digest includes gates, items, and checks only.
