# v1389 code walkthrough

The operator-provenance checkpoint carries operator provenance into submission precheck without changing approval flags. The validator blocks approval grant and signed approval.

The profile summary exposes readiness separately from approval readiness.
