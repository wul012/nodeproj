# v1377 code walkthrough

The signature-envelope request item is rendered in archive Markdown and exposed through the stable review-artifact barrel. The renderer lists the source slots, source guards, request instruction, acceptance criterion, and blocking checks.

The route profile only shows summary counters and permission flags; detailed per-item output stays in the dedicated renderer.
