# v1479 code walkthrough

v1479 covers build and HTTP smoke expectations. Smoke checks the profile route still exposes the material submission precheck section and keeps disabled upstream probes fail-closed.

The smoke check does not require Java or mini-kv live services.
