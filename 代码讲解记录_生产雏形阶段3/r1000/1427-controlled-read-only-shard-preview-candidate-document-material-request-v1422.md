# v1422 code walkthrough

The material request type layer starts at `Node v1422` and defines request items, acceptance checks, and gates. The source-precheck item maps the v1421 source-precheck intake slot and guard into external material request wording.

Readiness requires the source intake packet to be ready while reviewed material remains absent.
