# v1390 code walkthrough

The comparison-result checkpoint carries comparison fields into the aggregate submission field count. The artifact gates compare the submitted field count to the v1386 requested field count.

This proves twenty fields are carried while evaluation remains blocked.
