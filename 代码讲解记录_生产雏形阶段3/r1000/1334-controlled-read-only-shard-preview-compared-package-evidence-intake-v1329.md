# Code walkthrough v1329

The policy and execution lock slot joins policy and lock checkpoint families because a compared package cannot be reviewable if it weakens no-runtime or no-write constraints.

The validator exposes lock drift as gate failures instead of letting later approval code discover it indirectly.
