# v1402 code walkthrough

The request-instruction coverage checkpoint checks that the future submission has explicit request instructions. It is cross-cutting and sources all request acceptance checks.

This protects maintainers from adding a real intake path without operator-facing instructions.
