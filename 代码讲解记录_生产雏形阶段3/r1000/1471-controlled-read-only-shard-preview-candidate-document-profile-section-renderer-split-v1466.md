# v1466 code walkthrough

v1466 test coverage checks `candidateDocumentMaterialRequestPackageVersion: Node v1446`. This locks the material request package section after the migration.

The assertion protects the route-facing field that later material submission work consumes.
