# v1463 code walkthrough

v1463 moves `renderComparedEvidenceCandidateDocumentMaterialSubmissionPrecheckProfileEntries`. It preserves the v1456 precheck state, source digest, gate counts, and false material submission/intake flags.

The extracted renderer keeps the safety boundary visible in the same route section.
