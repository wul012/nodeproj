# v1470 code walkthrough

v1470 updates catalog and validation literal versions from `Node v1456` to `Node v1481`.

The catalog test also moves expected module count, unique ID count, unique path count, and stable re-export count from 209 to 210.
