# v1460 code walkthrough

v1460 moves `renderComparedEvidenceCandidateDocumentSubmissionPrecheckProfileEntries`. The migrated function still prints checkpoint counts, validator counts, disabled flags, and the submission precheck digest.

No artifact construction code changes in this step.
