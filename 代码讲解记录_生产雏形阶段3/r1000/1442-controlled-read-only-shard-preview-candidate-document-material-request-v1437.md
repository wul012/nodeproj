# v1437 code walkthrough

The comparison-binding-table item asks for future rows that bind submitted fields to compared package evidence. Evaluation remains blocked, so the binding is not interpreted yet.

This keeps the next stage explicit without executing it.
