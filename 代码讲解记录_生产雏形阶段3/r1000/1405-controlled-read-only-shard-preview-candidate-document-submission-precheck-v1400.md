# v1400 code walkthrough

The import-evaluation-freeze checkpoint keeps payload import and evaluation frozen. The artifact gates expose import, evaluation, and acceptance blocking separately.

Focused tests assert all payload counters stay zero.
