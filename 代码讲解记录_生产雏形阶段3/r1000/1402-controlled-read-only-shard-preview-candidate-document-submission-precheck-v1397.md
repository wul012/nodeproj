# v1397 code walkthrough

The missing-document checkpoint maps missing-document rejection from the request package into submission validation.

Blocked source packages report `CANDIDATE_DOCUMENT_SUBMISSION_PRECHECK_SOURCE_REQUEST_PACKAGE_NOT_READY` and fail closed.
