# v1412 code walkthrough

The intake packet type layer starts at `Node v1412` and defines the first slot/guard contract. The source-precheck slot groups lineage, request instruction coverage, and acceptance criterion coverage from the v1411 submission precheck.

Readiness depends on the source submission precheck being ready while reviewed real document material remains absent.
