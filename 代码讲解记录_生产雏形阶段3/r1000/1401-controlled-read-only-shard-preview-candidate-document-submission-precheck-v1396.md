# v1396 code walkthrough

The archive-closeout checkpoint completes the ten slot-backed submission checkpoints. Those first ten checkpoints carry all twenty candidate fields.

The remaining checkpoints focus on cross-cutting validator behavior.
