# Code walkthrough v1341

The operator value handle rule reuses the source evidence handle slot but narrows the expected field to operator value handles. This keeps raw values and secret values out of the candidate.
