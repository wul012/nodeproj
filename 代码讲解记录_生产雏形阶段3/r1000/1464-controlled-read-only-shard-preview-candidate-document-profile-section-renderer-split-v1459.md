# v1459 code walkthrough

v1459 moves `renderComparedEvidenceCandidateDocumentRequestPackageProfileEntries` into the new renderer file. The function body keeps the same field names and order.

The main renderer now delegates this section through the group renderer.
