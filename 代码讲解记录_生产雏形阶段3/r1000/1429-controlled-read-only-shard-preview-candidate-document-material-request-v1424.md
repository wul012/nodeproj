# v1424 code walkthrough

The field-completeness item carries v1421 field coverage into request wording. The aggregate deduplicates requested material fields before comparing them with the source intake field count.

Tests assert the package requests twenty fields.
