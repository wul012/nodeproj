# v1398 code walkthrough

The synthetic-document checkpoint keeps synthetic evidence rejected at submission time. This is distinct from missing-document rejection and unreviewed quarantine.

The validator checks preserve those three boundaries independently.
