# v1426 code walkthrough

The identity-signature item asks for identity and signature envelope evidence. The renderer exposes source slot and guard codes so the request can be traced back to v1421.

No signature material is accepted.
