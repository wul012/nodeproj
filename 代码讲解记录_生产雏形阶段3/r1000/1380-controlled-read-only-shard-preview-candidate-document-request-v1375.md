# v1375 code walkthrough

The comparison-result request item contributes its candidate fields to the aggregate requested-field count. The artifact gates compare requested fields against v1371 `requiredCandidateFieldCount`.

The package can therefore prove all twenty fields are requested without importing any compared evidence candidate payload.
