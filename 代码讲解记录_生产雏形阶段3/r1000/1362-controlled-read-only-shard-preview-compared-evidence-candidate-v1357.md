# v1357 code walkthrough

The signature-envelope section keeps detached signature metadata descriptive. It is paired with runtime-payload exclusion so a signature envelope cannot become executable material in Node.

The candidate renderer prints blocker state, including runtime-payload, write, and sibling-mutation blocks, for archive review.
