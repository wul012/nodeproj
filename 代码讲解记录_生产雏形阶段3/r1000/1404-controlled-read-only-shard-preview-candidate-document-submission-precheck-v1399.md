# v1399 code walkthrough

The unreviewed-document checkpoint requires quarantine before any import or evaluation. The checkpoint is sourced from all request acceptance checks.

This makes quarantine a package-wide precondition instead of a local slot behavior.
