# v1393 code walkthrough

The evidence-handle checkpoint maps durable handle requirements from the request package. It does not create or synthesize handles.

Forced historical fallback tests still expect zero real documents and no synthetic evidence acceptance.
