# v1383 code walkthrough

The synthetic-document rejection request item reinforces that historical fixture fallback is not real evidence. Tests force `ORDEROPS_FORCE_HISTORICAL_FIXTURE_FALLBACK=true` and still expect zero real documents and synthetic evidence rejection.

This keeps GitHub-runner compatibility without weakening the real-document boundary.
