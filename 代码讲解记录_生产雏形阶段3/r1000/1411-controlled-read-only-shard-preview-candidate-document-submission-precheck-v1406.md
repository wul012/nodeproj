# v1406 code walkthrough

The redaction-boundary checkpoint keeps redaction as an explicit future submission requirement. It does not inspect or store secret values.

The route profile exposes `containsSecretValue: false`.
