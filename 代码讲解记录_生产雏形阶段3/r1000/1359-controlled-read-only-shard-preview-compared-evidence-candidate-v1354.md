# v1354 code walkthrough

The operator-provenance section maps operator provenance and manual submission reference rules into a single candidate section. This keeps the future real candidate tied to human submission context without letting Node create that submission.

The profile renderer exposes the section through aggregate counts and digest references rather than raw evidence values.
