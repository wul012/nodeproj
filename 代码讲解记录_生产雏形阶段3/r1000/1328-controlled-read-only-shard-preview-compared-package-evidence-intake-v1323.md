# Code walkthrough v1323

The manual submission reference slot records the field a future real evidence artifact must provide. It deliberately avoids creating a submitted package in Node, because submission and evidence citation are separate responsibilities.

The guard text is generated from the slot field name and blocks missing or synthetic evidence while keeping acceptance and runtime actions closed.
