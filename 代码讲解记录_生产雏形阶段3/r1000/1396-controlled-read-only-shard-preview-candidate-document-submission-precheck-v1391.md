# v1391 code walkthrough

The identity-digest checkpoint keeps digest lineage visible and keeps secret values out of the precheck. The top artifact reports `containsSecretValue: false`.

The digest includes checkpoint codes, source request coverage, validators, and gates.
