# v1469 code walkthrough

v1469 moves `profile-entry-types` to order 210. The validation logic still requires the profile entry point to remain the final catalog entry.

This keeps public type imports stable while allowing the new renderer module to be cataloged.
