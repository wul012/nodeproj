# v1395 code walkthrough

The approval-separation checkpoint keeps reviewed document submission separate from approval grant and signed approval capture.

The package can be ready as a precheck while `readyForReviewedRealCandidateDocumentSubmission` remains false.
