# v1461 code walkthrough

v1461 moves `renderComparedEvidenceCandidateDocumentIntakePacketProfileEntries`. The intake packet renderer remains a field projection over the existing profile object.

This keeps the request, submission precheck, and intake packet sections together.
