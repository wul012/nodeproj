# v2026 code walkthrough

`createSourceNodeV236` maps the dry-run request envelope into the readiness gate source reference.

It keeps the source facts compact for the gate and downstream operator checklist.
