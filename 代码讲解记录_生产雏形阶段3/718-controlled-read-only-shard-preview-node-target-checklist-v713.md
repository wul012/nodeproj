# Node v713 code walkthrough: Node target checklist

`NODE_TARGET_CHECKLIST` maps to `NODE_READ_TARGET_SPEC`.

It limits Node reads to health plus controlled preview JSON/Markdown and keeps the package planning-only.
