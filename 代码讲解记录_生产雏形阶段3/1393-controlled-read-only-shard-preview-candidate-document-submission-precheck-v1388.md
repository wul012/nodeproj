# v1388 code walkthrough

The artifact-shape checkpoint is generated from the catalog template and resolved through the builder against request item and check kinds.

This keeps the new layer from copying field definitions manually and preserves source request ownership.
