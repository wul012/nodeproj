# Node v773 code walkthrough: evidence packet operator context record

v773 adds operator context capture shape.

`EVIDENCE_OPERATOR_CONTEXT_RECORD` maps to `WORKSHEET_ENVIRONMENT_LOCK_CHECK` and requires operator role, approval reference, and read-only environment flags.

Verification: covered by the evidence packet focused test.
