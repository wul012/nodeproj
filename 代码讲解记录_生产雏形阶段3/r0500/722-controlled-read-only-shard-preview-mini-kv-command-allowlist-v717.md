# Node v717 code walkthrough: mini-kv command allowlist

`MINI_KV_COMMAND_ALLOWLIST` restricts the future window to `SHARDJSON` and `SHARDROUTEVERIFYREPORTJSON`.

The runbook package gates keep automatic service start and writes disabled.
