# Node v728 code walkthrough: receipt archive boundary

`RECEIPT_ARCHIVE_BOUNDARY` restricts archive content to non-secret receipt metadata.

This keeps runtime payload and credential data outside the package.
