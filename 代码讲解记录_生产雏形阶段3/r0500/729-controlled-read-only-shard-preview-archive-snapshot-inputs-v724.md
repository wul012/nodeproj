# Node v724 code walkthrough: archive snapshot inputs

`ARCHIVE_SNAPSHOT_INPUTS` defines non-secret archive snapshot inputs.

It allows ledger digest, package digest, target sections, and metadata only.
