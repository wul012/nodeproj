# Node v707 code walkthrough: window receipt archive verification

`WINDOW_RECEIPT_ARCHIVE_VERIFICATION` records the future non-secret receipt verification stage.

The stage keeps credential and runtime payload boundaries explicit before the live read-only window.

Focused tests verify v707 ordering.
