# Node v779 code walkthrough: evidence packet mini-kv allowlist record

v779 adds mini-kv allowlist evidence shape.

`EVIDENCE_MINI_KV_ALLOWLIST_RECORD` maps to the allowlist worksheet step. The target is `policy`, so no mini-kv command is sent.

Verification: covered by the evidence packet focused test.
