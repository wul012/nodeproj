# Node v716 code walkthrough: operator header policy

`OPERATOR_HEADER_POLICY` records guarded Node route header requirements.

The section stores header names and non-secret correlation expectations, not secret values.
