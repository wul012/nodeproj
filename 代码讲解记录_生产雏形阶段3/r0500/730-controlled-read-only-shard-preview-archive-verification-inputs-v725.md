# Node v725 code walkthrough: archive verification inputs

`ARCHIVE_VERIFICATION_INPUTS` records the future archive verification requirements.

The package expects digest and section-count recomputation after the live read-only window.
