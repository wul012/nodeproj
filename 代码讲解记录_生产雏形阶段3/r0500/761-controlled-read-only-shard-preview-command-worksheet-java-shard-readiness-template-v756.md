# Node v756 code walkthrough: command worksheet Java shard readiness template

v756 adds the Java shard readiness command template.

Key code:

- `WORKSHEET_JAVA_SHARD_READINESS_TEMPLATE` targets `java-http`.
- The template states that Java must be operator-started, not started by Node.

No fresh Java evidence is consumed by this version.

Verification: covered by the command worksheet focused test.
