# Node v782 code walkthrough: evidence packet Java acceptance rule

v782 adds Java acceptance rule shape.

`EVIDENCE_JAVA_ACCEPTANCE_RULE` maps to Java evidence capture. The target remains `archive`, not a live Java client.

Verification: covered by the evidence packet focused test.
