# Node v719 code walkthrough: evidence record schema

`EVIDENCE_RECORD_SCHEMA` records the fields expected after a future live read-only window.

The package still does not claim fresh runtime evidence exists.
