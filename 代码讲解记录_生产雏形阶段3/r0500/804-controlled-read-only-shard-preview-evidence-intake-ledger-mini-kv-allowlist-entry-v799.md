# Node v799 code walkthrough: evidence intake ledger mini-kv allowlist entry

v799 adds mini-kv allowlist intake.

`INTAKE_MINI_KV_ALLOWLIST_ENTRY` maps to `EVIDENCE_MINI_KV_ALLOWLIST_RECORD` and keeps mutating commands out of the intake path.

Verification: covered by the evidence intake ledger focused test.
