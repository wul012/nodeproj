# Node v755 code walkthrough: command worksheet Node Markdown read template

v755 adds the controlled shard preview Markdown read template.

Key code:

- `WORKSHEET_NODE_MARKDOWN_READ_TEMPLATE` targets `node-http`.
- Profile Markdown now includes a `Live Read-Only Window Command Worksheet` summary.

This keeps future operator review tied to the existing read-only route surface.

Verification: covered by the command worksheet focused test and route test.
