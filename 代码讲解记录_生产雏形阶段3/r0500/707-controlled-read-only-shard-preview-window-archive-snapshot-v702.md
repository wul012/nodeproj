# Node v702 code walkthrough: window archive snapshot

`WINDOW_ARCHIVE_SNAPSHOT` records the archive snapshot requirement.

The snapshot is represented as planning metadata inside the ledger and remains non-secret.

Focused tests verify the stage remains read-only and non-executing.
