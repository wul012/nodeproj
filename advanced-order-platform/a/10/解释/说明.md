# 第十版运行调试记录

本次版本目标：

```text
把数据库结构管理从 Hibernate ddl-auto update 升级为 Flyway 迁移脚本。
默认 H2 继续快速启动，PostgreSQL profile 预留真实数据库运行。
新增 Testcontainers PostgreSQL 集成测试，在 Docker 可用时验证真实数据库兼容性。
```

---

# 1. 本次新增能力

新增依赖：

```text
flyway-core
flyway-database-postgresql
testcontainers-junit-jupiter
testcontainers-postgresql
```

新增配置：

```text
spring.flyway.locations = classpath:db/migration/{vendor}
spring.jpa.hibernate.ddl-auto = validate
```

新增文件：

```text
src/main/resources/application-postgres.yml
docker-compose.yml
src/main/resources/db/migration/h2/V1__initial_schema.sql
src/main/resources/db/migration/postgresql/V1__initial_schema.sql
src/test/java/com/codexdemo/orderplatform/PostgresMigrationIntegrationTests.java
代码讲解记录/14-version-10-postgres-flyway-testcontainers.md
```

对应图片：

```text
../图片/01-version-10-change-summary.png
```

---

# 2. 关键代码记录

Maven 依赖：

```xml
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-core</artifactId>
</dependency>
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-database-postgresql</artifactId>
</dependency>
<dependency>
    <groupId>org.testcontainers</groupId>
    <artifactId>junit-jupiter</artifactId>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.testcontainers</groupId>
    <artifactId>postgresql</artifactId>
    <scope>test</scope>
</dependency>
```

默认 H2 配置：

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:order-platform;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
  flyway:
    locations: "classpath:db/migration/{vendor}"
  jpa:
    hibernate:
      ddl-auto: validate
```

PostgreSQL profile：

```yaml
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/order_platform}
    username: ${DB_USERNAME:order_app}
    password: ${DB_PASSWORD:order_app}
```

Testcontainers 容器：

```java
@Container
static final PostgreSQLContainer<?> POSTGRES = new PostgreSQLContainer<>(
        DockerImageName.parse("postgres:16-alpine"))
        .withDatabaseName("order_platform_test")
        .withUsername("order_app")
        .withPassword("order_app");
```

动态数据源：

```java
@DynamicPropertySource
static void registerPostgresProperties(DynamicPropertyRegistry registry) {
    registry.add("spring.datasource.url", POSTGRES::getJdbcUrl);
    registry.add("spring.datasource.username", POSTGRES::getUsername);
    registry.add("spring.datasource.password", POSTGRES::getPassword);
}
```

---

# 3. 单元测试记录

执行命令：

```powershell
mvn test
```

第一次结果：

```text
BUILD SUCCESS
OrderApplicationServiceTests: 18 passed
PostgresMigrationIntegrationTests: 1 skipped
Tests run: 19, Failures: 0, Errors: 0, Skipped: 1
Finished at: 2026-05-10T12:35:08+08:00
```

跳过原因：

```text
当前环境没有可用 Docker。
@Testcontainers(disabledWithoutDocker = true) 让 PostgreSQL 容器测试自动跳过。
```

启动 Docker Desktop 后再次执行：

```powershell
mvn test
```

第二次结果：

```text
BUILD SUCCESS
Testcontainers found Docker environment with local Npipe socket
Pulled testcontainers/ryuk:0.12.0
Pulled postgres:16-alpine
PostgreSQL container JDBC URL: jdbc:postgresql://localhost:60936/order_platform_test
PostgresMigrationIntegrationTests: 1 passed
Tests run: 19, Failures: 0, Errors: 0, Skipped: 0
Finished at: 2026-05-10T13:40:11+08:00
```

对应图片：

```text
../图片/02-maven-test-v10.png
```

---

# 4. 打包记录

执行命令：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
Built target\advanced-order-platform-0.1.0-SNAPSHOT.jar
Spring Boot repackage completed
Finished at: 2026-05-10T12:43:59+08:00
```

对应图片：

```text
../图片/03-maven-package-v10.png
```

---

# 5. HTTP 运行调试记录

临时启动命令等价于：

```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18090 `
  --order.expiration.enabled=false `
  --outbox.publisher.scan-delay-ms=1000
```

调试进程：

```text
PID: 25616
Port: 18090
Health: UP
```

启动日志关键点：

```text
Successfully applied 1 migration to schema "public", now at version v1
Initialized JPA EntityManagerFactory for persistence unit 'default'
```

接口调试结果：

```text
GET /api/v1/products
 -> ProductCount = 3

POST /api/v1/orders
 -> OrderCreated

POST /api/v1/orders/1/pay
 -> OrderPaid

GET /api/v1/inventory/products/1/movements
 -> RESERVE,COMMIT_RESERVED

GET /api/v1/outbox/events
 -> OrderPaid,OrderCreated
```

对应图片：

```text
../图片/04-http-flyway-smoke.png
```

---

# 6. PostgreSQL 配合点

本轮后续已启动 Docker Desktop，并完成真实 PostgreSQLContainer 验证。

具体操作：

```text
打开 Docker Desktop
确认左下角/底部没有 Starting 提示
不用登录 Docker Hub
不用手动创建 volume
在 PowerShell 进入 D:\javaproj\advanced-order-platform
执行 mvn test
```

`PostgresMigrationIntegrationTests` 已自动：

```text
拉起 postgres:16-alpine
执行 db/migration/postgresql/V1__initial_schema.sql
校验 flyway_schema_history
校验 8 张核心业务表
跑创建订单 -> 支付 -> 退款链路
断言库存流水为 RESERVE,COMMIT_RESERVED,RETURN_COMMITTED
```

对应图片：

```text
../图片/05-docker-and-cleanup-v10.png
```

---

# 7. 调试问题记录

本轮遇到的关键问题：

```text
Flyway 已经执行建表，
但 Hibernate validate 报 missing table [inventory_items]。
```

原因：

```text
H2 标识符大小写和 Hibernate 小写表名不一致。
```

修复：

```text
DATABASE_TO_UPPER=false
 -> DATABASE_TO_LOWER=TRUE
```

这让 H2 更贴近 PostgreSQL 的未加引号标识符行为。

---

# 8. 清理记录

本轮清理结果：

```text
HTTP 调试进程 PID 25616
 -> 已停止

临时 stdout/stderr 日志目录
 -> 已删除

target 构建目录
 -> 最终清理阶段删除

a/10/图片
 -> 保留，作为第十版运行调试截图记录

a/10/解释/说明.md
 -> 保留，作为第十版运行调试文字记录
```

---

# 9. 当前结论

第十版完成后，项目数据库底座从：

```text
Hibernate 自动更新表结构
```

升级为：

```text
Flyway 迁移建表
Hibernate validate 校验
H2 快速开发
PostgreSQL profile 真实数据库运行
Testcontainers 真实数据库集成测试入口
```

一句话总结：第十版把项目从“业务功能雏形”推进到了“具备生产化数据库管理意识”的阶段。
