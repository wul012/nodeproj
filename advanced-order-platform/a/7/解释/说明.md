# 第七版运行调试记录

本次版本目标：

```text
新增支付交易流水。

订单支付成功后，不只把订单状态改成 PAID，
还要落一条可查询的支付交易记录。
```

---

# 1. 本次新增能力

新增业务包：

```text
src/main/java/com/codexdemo/orderplatform/payment
```

新增支付交易表：

```text
payment_transactions
```

新增查询接口：

```text
GET /api/v1/orders/{orderId}/payments
```

支付成功后会记录：

```text
orderId
amount
status = SUCCEEDED
provider = SIMULATED
providerTransactionId = SIM-...
createdAt
completedAt
```

对应图片：

```text
../图片/01-version-7-change-summary.png
```

---

# 2. 关键代码记录

支付状态：

```text
src/main/java/com/codexdemo/orderplatform/payment/PaymentStatus.java
```

```java
public enum PaymentStatus {
    SUCCEEDED
}
```

支付交易实体：

```text
src/main/java/com/codexdemo/orderplatform/payment/PaymentTransaction.java
```

核心字段：

```java
private Long orderId;
private BigDecimal amount;
private PaymentStatus status;
private String provider;
private String providerTransactionId;
private Instant createdAt;
private Instant completedAt;
```

创建成功支付交易：

```java
public static PaymentTransaction succeeded(Long orderId, BigDecimal amount) {
    return new PaymentTransaction(orderId, amount, "SIMULATED");
}
```

支付服务：

```text
src/main/java/com/codexdemo/orderplatform/payment/PaymentService.java
```

核心逻辑：

```java
public PaymentTransaction recordSucceededPayment(SalesOrder order) {
    return paymentTransactionRepository.findFirstByOrderIdAndStatusOrderByCreatedAtAscIdAsc(
                    order.getId(),
                    PaymentStatus.SUCCEEDED
            )
            .orElseGet(() -> paymentTransactionRepository.save(
                    PaymentTransaction.succeeded(order.getId(), order.getTotalAmount())
            ));
}
```

它保证：

```text
同一订单已有 SUCCEEDED 支付交易时，不重复创建。
```

订单支付编排：

```java
order.markPaid();
inventoryService.commitReserved(order.quantitiesByProductId());
paymentService.recordSucceededPayment(order);
outboxRepository.save(OutboxEvent.orderPaid(order));
recordHistory(order, fromStatus, "ORDER_PAID");
```

这说明一次支付成功会同时完成：

```text
订单状态变更
库存确认扣减
支付流水落库
Outbox 事件落库
状态历史落库
```

---

# 3. 单元测试记录

执行命令：

```powershell
mvn test
```

结果：

```text
BUILD SUCCESS
Compiling 41 source files
Found 6 JPA repository interfaces
Tests run: 15, Failures: 0, Errors: 0, Skipped: 0
Finished at: 2026-05-10T11:49:55+08:00
```

第七版新增测试：

```text
payOrderCreatesSingleSucceededPaymentTransaction
```

覆盖点：

```text
创建订单
支付订单
重复支付同一订单
查询支付流水
确认只有一条 SUCCEEDED 交易
确认金额等于订单金额
确认 provider = SIMULATED
确认 providerTransactionId 以 SIM- 开头
确认 createdAt/completedAt 不为空
```

同时增强取消订单测试：

```text
取消订单不能支付
取消订单不能产生支付流水
```

对应图片：

```text
../图片/02-maven-test-v7.png
```

---

# 4. 打包记录

执行命令：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
Built target\advanced-order-platform-0.1.0-SNAPSHOT.jar
Spring Boot repackage completed
Finished at: 2026-05-10T11:52:25+08:00
```

对应图片：

```text
../图片/03-maven-package-v7.png
```

---

# 5. HTTP 运行调试记录

临时启动命令等价于：

```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18087 `
  --order.expiration.enabled=false `
  --outbox.publisher.scan-delay-ms=1000
```

调试进程：

```text
PID: 5624
Port: 18087
Health: UP
```

调试链路：

```text
GET /actuator/health
 -> UP

GET /api/v1/products
 -> 返回 3 个演示商品

POST /api/v1/orders
 -> 创建订单 id=1

GET /api/v1/orders/1/payments
 -> 0 条

POST /api/v1/orders/1/pay
 -> PAID

重复 POST /api/v1/orders/1/pay
 -> PAID

GET /api/v1/orders/1/payments
 -> 1 条
```

最终支付流水：

```text
PaymentsBeforePayCount:
0

PaymentsAfterReplayCount:
1

PaymentAmount:
499.00

PaymentStatus:
SUCCEEDED

PaymentProvider:
SIMULATED

ProviderTransactionIdPrefix:
SIM-

PaymentCreatedAtPresent:
true

PaymentCompletedAtPresent:
true
```

对应图片：

```text
../图片/04-http-payment-smoke.png
```

---

# 6. 状态历史和 Outbox 验证

状态历史：

```text
HistoryActions:
ORDER_CREATED,ORDER_PAID
```

Outbox：

```text
TargetEventTypes:
OrderCreated,OrderPaid

TargetPublishedCount:
2

Publisher log:
event=outbox_publish published=2
```

这说明第七版没有破坏前面几版的两个关键能力：

```text
订单状态历史仍然写入
Outbox 发布标记仍然正常
```

对应图片：

```text
../图片/05-history-outbox-cleanup-v7.png
```

---

# 7. 代码讲解记录

第七版新增讲解文件：

```text
代码讲解记录/11-version-7-payment-transactions.md
```

同时更新：

```text
README.md
代码讲解记录/README.md
代码讲解记录/03-order-domain-api.md
代码讲解记录/04-order-application-flow.md
```

---

# 8. 清理记录

本轮清理结果：

```text
HTTP 调试进程 PID 5624
 -> 已停止

临时 stdout/stderr 日志
 -> 已删除

target 构建目录
 -> 已删除

a/7/图片
 -> 保留，作为第七版运行调试截图记录

a/7/解释/说明.md
 -> 保留，作为第七版运行调试文字记录
```

---

# 9. 当前结论

第七版完成后，支付链路从：

```text
订单状态 CREATED -> PAID
```

升级为：

```text
订单状态 CREATED -> PAID
库存 reserved 确认扣减
payment_transactions 写入 SUCCEEDED 交易
order_status_history 写入 ORDER_PAID
outbox_events 写入 OrderPaid
```

一句话总结：第七版让项目从“订单已支付”推进到“支付有交易凭证”，更接近真实订单支付系统。
