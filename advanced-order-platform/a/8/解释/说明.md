# 第八版运行调试记录

本次版本目标：

```text
新增支付后退款。

PAID 订单在未发货前可以退款，
退款后要回补库存、写退款交易流水、写状态历史、写 Outbox 事件。
```

---

# 1. 本次新增能力

新增订单状态：

```text
REFUNDED
```

新增接口：

```text
POST /api/v1/orders/{orderId}/refund
```

退款成功后：

```text
orders.status = REFUNDED
orders.refundedAt != null
inventory_items.available 回补
payment_transactions 新增 REFUNDED 交易
order_status_history 新增 ORDER_REFUNDED
outbox_events 新增 OrderRefunded
```

对应图片：

```text
../图片/01-version-8-change-summary.png
```

---

# 2. 关键代码记录

订单领域规则：

```java
public boolean refund() {
    if (status == OrderStatus.REFUNDED) {
        return false;
    }
    if (status != OrderStatus.PAID) {
        throw new BusinessException(HttpStatus.CONFLICT, "ORDER_STATUS_INVALID",
                "Only PAID orders can be refunded");
    }
    status = OrderStatus.REFUNDED;
    refundedAt = Instant.now();
    return true;
}
```

库存回补：

```java
public void returnCommitted(int quantity) {
    if (quantity <= 0) {
        throw new BusinessException(HttpStatus.BAD_REQUEST, "INVALID_QUANTITY", "Quantity must be greater than zero");
    }
    available += quantity;
}
```

退款交易流水：

```java
public static PaymentTransaction refunded(Long orderId, BigDecimal amount) {
    return new PaymentTransaction(orderId, amount, PaymentStatus.REFUNDED, "SIMULATED", "REF-");
}
```

退款事务编排：

```java
@Transactional
public OrderResponse refund(Long orderId) {
    SalesOrder order = findOrder(orderId);
    OrderStatus fromStatus = order.getStatus();
    if (order.refund()) {
        inventoryService.returnCommitted(order.quantitiesByProductId());
        paymentService.recordRefundedPayment(order);
        outboxRepository.save(OutboxEvent.orderRefunded(order));
        recordHistory(order, fromStatus, "ORDER_REFUNDED");
    }
    return OrderResponse.from(order);
}
```

---

# 3. 单元测试记录

执行命令：

```powershell
mvn test
```

结果：

```text
BUILD SUCCESS
Compiling 41 source files
Found 6 JPA repository interfaces
Tests run: 17, Failures: 0, Errors: 0, Skipped: 0
Finished at: 2026-05-10T11:59:24+08:00
```

第八版新增测试：

```text
paidOrderCanBeRefundedAndRestocksInventoryIdempotently
 -> 支付后退款
 -> 库存 available 回补到支付前
 -> 支付流水包含 SUCCEEDED、REFUNDED
 -> 重复退款不重复写 REFUNDED
 -> 历史包含 ORDER_REFUNDED
 -> Outbox 包含 OrderRefunded

shippedOrderCannotBeRefunded
 -> 已发货订单不能退款
 -> 不写 REFUNDED 支付流水
```

对应图片：

```text
../图片/02-maven-test-v8.png
```

---

# 4. 打包记录

执行命令：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
Built target\advanced-order-platform-0.1.0-SNAPSHOT.jar
Spring Boot repackage completed
Finished at: 2026-05-10T12:05:15+08:00
```

对应图片：

```text
../图片/03-maven-package-v8.png
```

---

# 5. HTTP 运行调试记录

临时启动命令等价于：

```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18088 `
  --order.expiration.enabled=false `
  --outbox.publisher.scan-delay-ms=1000
```

调试进程：

```text
PID: 21632
Port: 18088
Health: UP
```

主链路：

```text
POST /api/v1/orders
 -> 创建订单 id=1

POST /api/v1/orders/1/pay
 -> PAID

POST /api/v1/orders/1/refund
 -> REFUNDED

重复 POST /api/v1/orders/1/refund
 -> REFUNDED

GET /api/v1/orders/1/payments
 -> 2 条
```

支付流水：

```text
PaymentStatuses:
SUCCEEDED,REFUNDED

PaymentProviders:
SIMULATED,SIMULATED

PaymentPrefixes:
SIM-,REF-
```

对应图片：

```text
../图片/04-http-refund-smoke.png
```

---

# 6. 状态历史、Outbox 和非法流

状态历史：

```text
HistoryActions:
ORDER_CREATED,ORDER_PAID,ORDER_REFUNDED

HistoryToStatuses:
CREATED,PAID,REFUNDED
```

Outbox：

```text
TargetEventTypes:
OrderCreated,OrderPaid,OrderRefunded

TargetPublishedCount:
3
```

非法流：

```text
已发货订单退款:
HTTP_409
```

发布器日志：

```text
event=outbox_publish published=6
```

说明：

```text
published=6 包含主退款订单的 3 条事件，
以及非法流验证中第二张订单创建、支付、发货产生的 3 条事件。
```

对应图片：

```text
../图片/05-history-outbox-invalid-v8.png
```

---

# 7. 代码讲解记录

第八版新增讲解文件：

```text
代码讲解记录/12-version-8-refund-flow.md
```

同时更新：

```text
README.md
代码讲解记录/README.md
代码讲解记录/03-order-domain-api.md
代码讲解记录/04-order-application-flow.md
代码讲解记录/05-outbox-exception-tests.md
代码讲解记录/11-version-7-payment-transactions.md
```

---

# 8. 清理记录

本轮清理结果：

```text
HTTP 调试进程 PID 21632
 -> 已停止

临时 stdout/stderr 日志
 -> 已删除

target 构建目录
 -> 已删除

a/8/图片
 -> 保留，作为第八版运行调试截图记录

a/8/解释/说明.md
 -> 保留，作为第八版运行调试文字记录
```

---

# 9. 当前结论

第八版完成后，订单支付退款链路变成：

```text
CREATED -> PAID -> REFUNDED
```

同一次退款会同步完成：

```text
库存回补
退款交易流水
订单状态历史
Outbox 退款事件
```

一句话总结：第八版把项目从“支付有交易凭证”推进到“退款也有完整业务闭环”。
