# 第九版运行调试记录

本次版本目标：

```text
新增库存变更流水。
订单创建、支付、退款、取消会改变库存，
第九版把每次库存变化的前后快照都记录下来，
方便后续排查“库存为什么少了/多了/被占用了”。
```

---

# 1. 本次新增能力

新增库存流水表：

```text
inventory_movements
```

新增库存流水类型：

```text
RESERVE
COMMIT_RESERVED
RELEASE_RESERVED
RETURN_COMMITTED
```

新增查询接口：

```text
GET /api/v1/inventory/products/{productId}/movements
```

每条流水记录：

```text
productId
type
quantity
availableBefore
reservedBefore
availableAfter
reservedAfter
createdAt
```

对应图片：

```text
../图片/01-version-9-change-summary.png
```

---

# 2. 关键代码记录

库存流水类型：

```java
public enum InventoryMovementType {
    RESERVE,
    COMMIT_RESERVED,
    RELEASE_RESERVED,
    RETURN_COMMITTED
}
```

库存流水实体：

```java
@Entity
@Table(name = "inventory_movements",
        indexes = @Index(name = "idx_inventory_movements_product_created", columnList = "product_id, created_at"))
public class InventoryMovement {
    @Enumerated(EnumType.STRING)
    @Column(nullable = false, length = 40)
    private InventoryMovementType type;

    @Column(nullable = false)
    private int availableBefore;

    @Column(nullable = false)
    private int reservedBefore;

    @Column(nullable = false)
    private int availableAfter;

    @Column(nullable = false)
    private int reservedAfter;
}
```

库存服务统一记录变化：

```java
private void applyAndRecord(Long productId, int quantity, InventoryMovementType type, Consumer<InventoryItem> operation) {
    InventoryItem item = findLocked(productId);
    int availableBefore = item.getAvailable();
    int reservedBefore = item.getReserved();

    operation.accept(item);

    inventoryMovementRepository.save(InventoryMovement.record(
            item, type, quantity, availableBefore, reservedBefore));
}
```

查询接口：

```java
@GetMapping("/products/{productId}/movements")
public List<InventoryMovementResponse> listProductMovements(@PathVariable Long productId) {
    return inventoryService.listProductMovements(productId);
}
```

---

# 3. 单元测试记录

执行命令：

```powershell
mvn test
```

结果：

```text
BUILD SUCCESS
Compiling 46 source files
Found 7 JPA repository interfaces
Tests run: 18, Failures: 0, Errors: 0, Skipped: 0
Finished at: 2026-05-10T12:14:58+08:00
```

第九版新增/增强测试：

```text
inventoryMovementsTrackReserveCommitAndReturn
 -> 创建订单产生 RESERVE
 -> 支付订单产生 COMMIT_RESERVED
 -> 退款订单产生 RETURN_COMMITTED
 -> 校验 available/reserved before/after 快照

cancelOrderReleasesReservedInventoryAndIsIdempotent
 -> 创建订单产生 RESERVE
 -> 取消订单产生 RELEASE_RESERVED
 -> 重复取消不重复写释放流水
```

对应图片：

```text
../图片/02-maven-test-v9.png
```

---

# 4. 打包记录

执行命令：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
Built target\advanced-order-platform-0.1.0-SNAPSHOT.jar
Spring Boot repackage completed
Finished at: 2026-05-10T12:17:06+08:00
```

对应图片：

```text
../图片/03-maven-package-v9.png
```

---

# 5. HTTP 运行调试记录

临时启动命令等价于：

```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18089 `
  --order.expiration.enabled=false `
  --outbox.publisher.scan-delay-ms=1000
```

调试进程：

```text
PID: 22356
Port: 18089
Health: UP
```

主链路：

```text
POST /api/v1/orders
 -> 创建退款链路订单
 -> 库存流水 RESERVE

POST /api/v1/orders/1/pay
 -> 订单 PAID
 -> 库存流水 COMMIT_RESERVED

POST /api/v1/orders/1/refund
 -> 订单 REFUNDED
 -> 库存流水 RETURN_COMMITTED

POST /api/v1/orders
 -> 创建取消链路订单
 -> 库存流水 RESERVE

POST /api/v1/orders/2/cancel
 -> 订单 CANCELLED
 -> 库存流水 RELEASE_RESERVED
```

最终查询：

```text
GET /api/v1/inventory/products/1/movements
```

结果摘要：

```text
FinalMovementCount: 5
MovementTypes: RESERVE,COMMIT_RESERVED,RETURN_COMMITTED,RESERVE,RELEASE_RESERVED

ReserveDelta: 100/0 -> 98/2
CommitDelta: 98/2 -> 98/0
ReturnDelta: 98/0 -> 100/0
ReleaseDelta: 99/1 -> 100/0
```

对应图片：

```text
../图片/04-http-inventory-movements-smoke.png
```

---

# 6. Outbox 联动记录

本轮 HTTP 冒烟同时触发 Outbox 发布器：

```text
event=outbox_publish published=5
```

对应业务事件：

```text
OrderCreated
OrderPaid
OrderRefunded
OrderCreated
OrderCancelled
```

说明：

```text
库存流水不是事件总线，而是库存侧审计表；
Outbox 是跨系统事件出口。
两者都记录“发生过什么”，但面向的问题不同。
```

---

# 7. 代码讲解记录

第九版新增讲解文件：

```text
代码讲解记录/13-version-9-inventory-movements.md
```

同时更新：

```text
README.md
代码讲解记录/README.md
代码讲解记录/02-catalog-inventory.md
代码讲解记录/04-order-application-flow.md
```

本版讲解重点：

```text
多代码引用串起库存流水：
InventoryMovementType
InventoryMovement
InventoryMovementRepository
InventoryMovementResponse
InventoryService
InventoryController
OrderApplicationServiceTests
```

---

# 8. 调试注意点

PowerShell 调试接口时要注意：

```text
Invoke-RestMethod 对 JSON 空数组、单元素数组、多元素数组的 Count 行为不完全一致。
本轮最终验证使用原始 JSON 响应再读取字段，避免数组 Count 被 PowerShell 包装误导。
```

这个点属于运行调试经验，不影响 Java 项目代码。

---

# 9. 清理记录

本轮清理结果：

```text
HTTP 调试进程 PID 22356
 -> 已停止

临时 stdout/stderr 日志目录
 -> 已删除

target 构建目录
 -> 最终清理阶段删除

a/9/图片
 -> 保留，作为第九版运行调试截图记录

a/9/解释/说明.md
 -> 保留，作为第九版运行调试文字记录
```

对应图片：

```text
../图片/05-cleanup-v9.png
```

---

# 10. 当前结论

第九版完成后，库存模块从“只保存当前库存数值”升级为：

```text
当前库存 + 库存变化流水
```

一句话总结：第九版把库存从一个容易看不出原因的结果数值，推进到可审计、可解释、可回放排查的业务记录。
