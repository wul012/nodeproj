# 第六版运行调试记录

本次版本目标：

```text
新增订单状态历史/操作流水。

订单不只保存“当前状态”，还要能查到：
从什么状态变来
变成了什么状态
由什么动作触发
什么时候发生
```

---

# 1. 本次新增能力

新增状态历史表：

```text
order_status_history
```

新增核心字段：

```text
orderId
fromStatus
toStatus
action
changedAt
```

新增查询接口：

```text
GET /api/v1/orders/{orderId}/history
```

完整履约订单的历史会类似：

```text
ORDER_CREATED: null -> CREATED
ORDER_PAID: CREATED -> PAID
ORDER_SHIPPED: PAID -> SHIPPED
ORDER_COMPLETED: SHIPPED -> COMPLETED
```

对应图片：

```text
../图片/01-version-6-change-summary.png
```

---

# 2. 关键代码记录

新增实体：

```text
src/main/java/com/codexdemo/orderplatform/order/OrderStatusHistory.java
```

核心代码：

```java
public static OrderStatusHistory record(
        Long orderId,
        OrderStatus fromStatus,
        OrderStatus toStatus,
        String action
) {
    return new OrderStatusHistory(orderId, fromStatus, toStatus, action);
}
```

新增查询仓库：

```text
src/main/java/com/codexdemo/orderplatform/order/OrderStatusHistoryRepository.java
```

核心方法：

```java
List<OrderStatusHistory> findByOrderIdOrderByChangedAtAscIdAsc(Long orderId);
```

新增响应 DTO：

```text
src/main/java/com/codexdemo/orderplatform/order/OrderStatusHistoryResponse.java
```

新增服务方法：

```java
@Transactional(readOnly = true)
public List<OrderStatusHistoryResponse> getOrderHistory(Long orderId) {
    findOrder(orderId);
    return orderStatusHistoryRepository.findByOrderIdOrderByChangedAtAscIdAsc(orderId).stream()
            .map(OrderStatusHistoryResponse::from)
            .toList();
}
```

统一写历史：

```java
private void recordHistory(SalesOrder order, OrderStatus fromStatus, String action) {
    orderStatusHistoryRepository.save(
            OrderStatusHistory.record(order.getId(), fromStatus, order.getStatus(), action)
    );
}
```

新增 Controller 入口：

```java
@GetMapping("/{orderId}/history")
public List<OrderStatusHistoryResponse> getOrderHistory(@PathVariable Long orderId) {
    return orderApplicationService.getOrderHistory(orderId);
}
```

---

# 3. 单元测试记录

执行命令：

```powershell
mvn test
```

结果：

```text
BUILD SUCCESS
Compiling 36 source files
Found 5 JPA repository interfaces
Tests run: 14, Failures: 0, Errors: 0, Skipped: 0
Finished at: 2026-05-10T11:26:33+08:00
```

第六版新增测试：

```text
orderHistoryTracksFulfillmentTimeline
 -> 验证创建、支付、发货、完成的完整时间线

orderHistoryTracksCancelAndExpirationActions
 -> 验证手动取消和自动过期也会写不同 action
```

对应图片：

```text
../图片/02-maven-test-v6.png
```

---

# 4. 打包记录

执行命令：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
Built target\advanced-order-platform-0.1.0-SNAPSHOT.jar
Spring Boot repackage completed
Finished at: 2026-05-10T11:31:25+08:00
```

对应图片：

```text
../图片/03-maven-package-v6.png
```

---

# 5. HTTP 运行调试记录

临时启动命令等价于：

```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18086 `
  --order.expiration.enabled=false `
  --outbox.publisher.scan-delay-ms=1000
```

调试进程：

```text
PID: 19572
Port: 18086
Health: UP
```

调试链路：

```text
GET /actuator/health
 -> UP

GET /api/v1/products
 -> 返回 3 个演示商品

POST /api/v1/orders
 -> 创建订单 id=1

GET /api/v1/orders/1/history
 -> ORDER_CREATED

POST /api/v1/orders/1/pay
 -> PAID

POST /api/v1/orders/1/ship
 -> SHIPPED

POST /api/v1/orders/1/complete
 -> COMPLETED

GET /api/v1/orders/1/history
 -> 返回 4 条历史
```

最终历史：

```text
HistoryCount:
4

HistoryActions:
ORDER_CREATED,ORDER_PAID,ORDER_SHIPPED,ORDER_COMPLETED

HistoryFromStatuses:
null,CREATED,PAID,SHIPPED

HistoryToStatuses:
CREATED,PAID,SHIPPED,COMPLETED

ChangedAtPresent:
true
```

对应图片：

```text
../图片/04-http-history-smoke.png
```

---

# 6. Outbox 验证

目标订单同时产生 Outbox 事件：

```text
OrderCreated
OrderPaid
OrderShipped
OrderCompleted
```

发布器验证：

```text
TargetPublishedCount:
4

Publisher log:
event=outbox_publish published=4
```

这说明：

```text
状态历史负责内部审计查询
Outbox 继续负责外部事件发布准备
```

对应图片：

```text
../图片/05-outbox-and-cleanup-v6.png
```

---

# 7. 代码讲解记录

第六版新增讲解文件：

```text
代码讲解记录/10-version-6-status-history.md
```

同时更新：

```text
README.md
代码讲解记录/README.md
代码讲解记录/03-order-domain-api.md
代码讲解记录/04-order-application-flow.md
代码讲解记录/05-outbox-exception-tests.md
```

---

# 8. 清理记录

本轮清理结果：

```text
HTTP 调试进程 PID 19572
 -> 已停止

临时 stdout/stderr 日志
 -> 已删除

target 构建目录
 -> 已删除

a/6/图片
 -> 保留，作为第六版运行调试截图记录

a/6/解释/说明.md
 -> 保留，作为第六版运行调试文字记录
```

---

# 9. 当前结论

第六版完成后，项目不仅能完成订单状态流转：

```text
CREATED -> PAID -> SHIPPED -> COMPLETED
CREATED -> CANCELLED
CREATED -> EXPIRED(CANCELLED)
```

还可以查询每张订单的状态变化时间线：

```text
GET /api/v1/orders/{orderId}/history
```

一句话总结：第六版把项目从“能处理状态”推进到“能解释状态变化过程”，更适合真实系统排查问题、做审计和扩展运营后台。
