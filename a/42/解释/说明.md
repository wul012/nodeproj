# v42 归档说明

## 版本主题

v42 新增 `Promotion Release Archive Verification`：对 v41 的最终发布归档进行独立复验，复算 `releaseArchiveDigest`，并逐项校验 archive items。

## 关键代码

- `src/services/opsPromotionArchiveBundle.ts`
  - 新增 `OpsPromotionReleaseArchiveVerification`
  - 新增 `OpsPromotionReleaseArchiveVerificationItem`
  - 新增 `createOpsPromotionReleaseArchiveVerification`
  - 新增 `archiveReleaseArchiveVerificationNextActions`
  - 新增 `renderOpsPromotionReleaseArchiveVerificationMarkdown`
- `src/routes/opsSummaryRoutes.ts`
  - 新增 `GET /api/v1/ops/promotion-archive/release-archive/verification`
- `src/ui/dashboard.ts`
  - 新增 `Release Archive Verification`
  - 新增 `Release Archive Verification Report`
- `test/opsPromotionDecision.test.ts`
  - 新增 `verifies promotion release archive as JSON or Markdown`
- `README.md`
  - 补充能力说明和 API 列表
- `代码讲解记录/46-ops-promotion-release-archive-verification-v42.md`
  - 新增本版代码讲解

## 验证结果

- `npm run typecheck`：通过
- `npm test`：18 个测试文件通过，82 个测试通过
- `npm run build`：通过
- Node HTTP smoke：使用安全环境变量，仅启动 Node 项目，访问 release archive verification JSON 和 Markdown 均通过

## 清理说明

- 本版只操作 `D:\nodeproj\orderops-node`
- 未启动、停止、构建、测试或修改 Java 高并发项目
- 未启动、停止、构建、测试或修改 mini-kv
- 验证后清理 `dist`
- 未保留临时进程
