# V33 开发运行调试说明

## 本版目标

V33 在 V32 `promotion handoff certificate verification` 后继续收口，新增 `promotion handoff receipt`。

目标是把已验证的 certificate 整理成最终交接收据：

```text
handoff package
 -> handoff package verification
 -> handoff certificate
 -> handoff certificate verification
 -> handoff receipt
```

新增接口：

```text
GET /api/v1/ops/promotion-archive/handoff-receipt
GET /api/v1/ops/promotion-archive/handoff-receipt?format=markdown
```

receipt 保存：

```text
receiptDigest
certificateDigest
verifiedCertificateDigest
packageDigest
verifiedPackageDigest
sealDigest
milestones
```

## 关键代码

### 1. receipt milestone

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
export type OpsPromotionHandoffReceiptMilestoneName =
  | "handoff-package"
  | "verified-handoff-package"
  | "archive-seal"
  | "handoff-certificate"
  | "certificate-verification";
```

### 2. receipt 主体

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
export interface OpsPromotionHandoffReceipt {
  service: "orderops-node";
  generatedAt: string;
  receiptName: string;
  certificateName: string;
  packageName: string;
  archiveName: string;
  valid: boolean;
  state: OpsPromotionArchiveAttestationState;
  handoffReady: boolean;
  receiptDigest: {
    algorithm: "sha256";
    value: string;
    coveredFields: string[];
  };
```

### 3. receipt 创建入口

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
export function createOpsPromotionHandoffReceipt(input: {
  certificate: OpsPromotionHandoffCertificate;
  certificateVerification: OpsPromotionHandoffCertificateVerification;
}): OpsPromotionHandoffReceipt {
  const receiptName = `promotion-receipt-${input.certificateVerification.recomputedCertificateDigest.value.slice(0, 12)}`;
```

### 4. valid / handoffReady 判断

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
  const valid = input.certificate.valid
    && input.certificateVerification.valid
    && input.certificate.certificateDigest.value === input.certificateVerification.recomputedCertificateDigest.value
    && milestones.every((milestone) => milestone.valid);
  const handoffReady = valid && input.certificate.handoffReady && input.certificateVerification.handoffReady;
```

### 5. receiptDigest 覆盖字段

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
      coveredFields: [
        "receiptName",
        "certificateName",
        "packageName",
        "archiveName",
        "valid",
        "state",
        "handoffReady",
        "certificateDigest",
        "verifiedCertificateDigest",
        "packageDigest",
        "verifiedPackageDigest",
        "sealDigest",
        "decision",
        "verification",
        "milestones",
        "nextActions",
      ],
```

### 6. 路由链路

文件：`src/routes/opsSummaryRoutes.ts`

```ts
    const certificateVerification = createOpsPromotionHandoffCertificateVerification({
      handoffPackage,
      handoffPackageVerification,
      certificate,
    });
    const receipt = createOpsPromotionHandoffReceipt({
      certificate,
      certificateVerification,
    });

    if (request.query.format === "markdown") {
      reply.type("text/markdown; charset=utf-8");
      return renderOpsPromotionHandoffReceiptMarkdown(receipt);
    }
```

### 7. Dashboard 入口

文件：`src/ui/dashboard.ts`

```html
        <button data-action="opsPromotionHandoffReceipt">Handoff Receipt</button>
        <button data-action="opsPromotionHandoffReceiptReport">Handoff Receipt Report</button>
```

## 运行验证

正式验证命令：

```text
npm run typecheck -> passed
npm test          -> 18 files passed, 73 tests passed
npm run build    -> passed
```

本地 smoke 调试：

```text
HOST=127.0.0.1
PORT=4100
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
```

验证结果：

```text
/health -> ok
empty handoff receipt -> valid=true, state=not-started
blocked decision 后 handoff receipt -> valid=true, state=blocked
receiptDigest length -> 64
certificateDigest == verifiedCertificateDigest -> true
packageDigest == verifiedPackageDigest -> true
sealDigest length -> 64
milestoneCount -> 5
milestonesValid -> true
Markdown content-type -> text/markdown; charset=utf-8
Dashboard handoff receipt button -> present
Dashboard handoff receipt report button -> present
```

## 对其他项目的影响

本次只在 `D:\nodeproj\orderops-node` 内开发、构建、测试和启动本地 Node 服务。

没有进入、修改、启动、停止或调试：

```text
D:\javaproj\advanced-order-platform
D:\C\mini-kv
```

smoke 运行时显式设置：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
```

因此不会主动探测或操作 Java 高并发订单项目、C++ mini_kv 项目。

## 清理结果

```text
本地 Node 调试服务 PID 37420 -> 已停止
127.0.0.1:4100 -> 无监听
dist/ -> 已删除
临时测试目录 -> 未创建
playwright-report/ -> 未创建
test-output/ -> 未创建
```

## 本版结论

V33 已经把 verified certificate 收口成最终 `handoff receipt`：用 `receiptDigest` 封住交接摘要，并用五个 milestone 记录 package、verified package、seal、certificate、certificate verification 的关键指纹。
