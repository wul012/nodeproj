# v43 归档说明

## 版本主题

v43 新增 `Promotion Deployment Approval`：把 v42 已验证的 release archive 封装成部署审批记录，生成可归档的 `approvalDigest`。

## 关键代码

- `src/services/opsPromotionArchiveBundle.ts`
  - 新增 `OpsPromotionDeploymentApproval`
  - 新增 `OpsPromotionDeploymentApprovalItem`
  - 新增 `createOpsPromotionDeploymentApproval`
  - 新增 `archiveDeploymentApprovalItems`
  - 新增 `archiveDeploymentApprovalDigestPayload`
  - 新增 `renderOpsPromotionDeploymentApprovalMarkdown`
- `src/routes/opsSummaryRoutes.ts`
  - 新增 `GET /api/v1/ops/promotion-archive/deployment-approval`
- `src/ui/dashboard.ts`
  - 新增 `Deployment Approval`
  - 新增 `Deployment Approval Report`
- `test/opsPromotionDecision.test.ts`
  - 新增 `builds promotion deployment approval as JSON or Markdown`
- `README.md`
  - 补充能力说明和 API 列表
- `代码讲解记录/47-ops-promotion-deployment-approval-v43.md`
  - 新增本版代码讲解

## 验证结果

- `npm run typecheck`：通过
- `npm test`：18 个测试文件通过，83 个测试通过
- `npm run build`：通过
- Node HTTP smoke：使用安全环境变量，仅启动 Node 项目，访问 deployment approval JSON 和 Markdown 均通过

## 清理说明

- 本版只操作 `D:\nodeproj\orderops-node`
- 未启动、停止、构建、测试或修改 Java 高并发项目
- 未启动、停止、构建、测试或修改 mini-kv
- 验证后清理 `dist`
- 未保留临时进程
