# OrderOps Node V7 运行调试归档

## 本版目标

V7 在 V6 的 operation intent 上增加“操作事件时间线”：

- 创建 intent 时记录 `intent.created`。
- 被安全策略挡住时记录 `intent.blocked`。
- 等待确认时记录 `intent.awaiting_confirmation`。
- 确认成功时记录 `intent.confirmation.accepted`。
- 确认失败时记录 `intent.confirmation.rejected`。
- 超时过期时记录 `intent.expired`。

这版仍然只在 Node 内部记录事件，不执行 Java / mini-kv 上游动作。

## 关键代码

### 1. 事件类型

文件：`src/services/operationIntent.ts`

```ts
export type OperationIntentEventType =
  | "intent.created"
  | "intent.blocked"
  | "intent.awaiting_confirmation"
  | "intent.confirmation.accepted"
  | "intent.confirmation.rejected"
  | "intent.expired";
```

### 2. 事件结构

文件：`src/services/operationIntent.ts`

```ts
export interface OperationIntentEvent {
  id: string;
  intentId: string;
  type: OperationIntentEventType;
  at: string;
  action: ActionKey;
  target: ActionTarget;
  status: OperationIntentStatus;
  actor: {
    id: string;
    role: OperatorRole;
  };
  message: string;
  details?: Record<string, unknown>;
}
```

### 3. 创建 intent 时记录事件

文件：`src/services/operationIntent.ts`

```ts
this.recordEvent(intent, "intent.created", "Operation intent was created", {
  reason: intent.reason,
});
```

```ts
this.recordEvent(
  intent,
  status === "blocked" ? "intent.blocked" : "intent.awaiting_confirmation",
  policy.message,
  policy.blockedBy ? { blockedBy: policy.blockedBy, requiredRole: policy.requiredRole } : { requiredRole: policy.requiredRole },
);
```

### 4. 确认失败也记录事件

文件：`src/services/operationIntent.ts`

```ts
this.recordEvent(current, "intent.confirmation.rejected", "Confirmation was rejected because the intent is not confirmable", {
  attemptedBy: input.operatorId.trim(),
  status: current.status,
});
```

### 5. 事件流和 timeline 查询

文件：`src/services/operationIntent.ts`

```ts
listEvents(query: OperationIntentEventQuery = {}): OperationIntentEvent[] {
  this.expireStale(new Date());
  const safeLimit = Math.min(Math.max(Math.floor(query.limit ?? 50), 1), this.maxEvents);
  return this.events
    .filter((event) => query.intentId === undefined || event.intentId === query.intentId)
    .filter((event) => query.type === undefined || event.type === query.type)
    .slice(-safeLimit)
    .reverse()
    .map(cloneEvent);
}
```

```ts
getTimeline(intentId: string, limit = 50): { intent: OperationIntent; events: OperationIntentEvent[] } {
  const intent = this.get(intentId);
  return {
    intent,
    events: this.listEvents({ intentId, limit }).reverse(),
  };
}
```

### 6. 新路由

文件：`src/routes/operationIntentRoutes.ts`

```ts
app.get<{ Querystring: ListIntentEventQuery }>("/api/v1/operation-intent-events", ...)
```

```ts
app.get<{ Params: IntentParams; Querystring: ListIntentQuery }>("/api/v1/operation-intents/:intentId/timeline", ...)
```

### 7. Dashboard

文件：`src/ui/dashboard.ts`

```html
<button data-action="intentTimeline">Timeline</button>
<button data-action="intentEvents">Event Feed</button>
```

```js
if (action === "intentTimeline") {
  write(await api("/api/v1/operation-intents/" + encodeURIComponent($("intentId").value) + "/timeline?limit=30"));
}
```

```js
if (action === "intentEvents") {
  write(await api("/api/v1/operation-intent-events?limit=30"));
}
```

## 运行调试结果

- `npm run typecheck`：通过。
- `npm test`：7 个测试文件、24 个测试通过。
- `npm run build`：通过。
- `/health`：返回 `status: ok`。
- `/api/v1/runtime/config`：确认 `upstreamProbesEnabled=false`、`upstreamActionsEnabled=false`。
- `POST /api/v1/operation-intents`：
  - `kv-set` + `admin` 返回 `status=blocked`。
  - `policy.blockedBy=UPSTREAM_ACTIONS_ENABLED=false`。
- `GET /api/v1/operation-intent-events?limit=10`：
  - 创建后返回 `intent.blocked,intent.created`。
  - 确认 blocked intent 后返回 `intent.confirmation.rejected,intent.blocked,intent.created`。
- `GET /api/v1/operation-intents/:id/timeline?limit=10`：
  - 创建后返回 `intent.created,intent.blocked`。
  - 确认失败后返回 `intent.created,intent.blocked,intent.confirmation.rejected`。
- 首页 HTML 包含 `intentTimeline`、`intentEvents` 和 `/api/v1/operation-intent-events`。

## 对另外两个项目的影响

本次 V7 调试没有启动、停止、构建、测试或修改：

- `D:\javaproj\advanced-order-platform`
- `D:\C\mini-kv`

Node smoke 只访问了 `orderops-node` 自己的 intent/timeline 接口，没有访问真实代理路由。

## Cleanup

- 已删除本次 build 产生的 `dist`。
- 已停止本次 V7 smoke 使用的 `orderops-node` dev server。
- 结束时 `127.0.0.1:4100` 没有监听进程。

## 本版一句话

V7 把 operation intent 从“有状态”推进到“有历史”，后续做持久化审计、审批界面、执行队列都会更自然。
