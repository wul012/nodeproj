# v41 归档说明

## 版本主题

v41 新增 `Promotion Release Archive`：把 v39 的 release evidence 和 v40 的 release evidence verification 收束成最终发布归档清单，并生成稳定的 `releaseArchiveDigest`。

## 关键代码

- `src/services/opsPromotionArchiveBundle.ts`
  - 新增 `OpsPromotionReleaseArchive`、`OpsPromotionReleaseArchiveItem`
  - 新增 `createOpsPromotionReleaseArchive`
  - 新增 `archiveReleaseArchiveDigestPayload`
  - 新增 `archiveReleaseArchiveItems`
  - 新增 `renderOpsPromotionReleaseArchiveMarkdown`
- `src/routes/opsSummaryRoutes.ts`
  - 新增 `GET /api/v1/ops/promotion-archive/release-archive`
  - 新增 `createPromotionReleaseEvidenceArtifacts` helper，复用 release evidence 构建链路
- `src/ui/dashboard.ts`
  - 新增 `Release Archive`
  - 新增 `Release Archive Report`
- `test/opsPromotionDecision.test.ts`
  - 新增 `builds promotion release archive as JSON or Markdown`
- `README.md`
  - 补充能力说明和 API 列表
- `代码讲解记录/45-ops-promotion-release-archive-v41.md`
  - 新增本版代码讲解

## 验证结果

- `npm run typecheck`：通过
- `npm test`：18 个测试文件通过，81 个测试通过
- `npm run build`：通过
- Node HTTP smoke：使用安全环境变量，仅启动 Node 项目，访问 release archive JSON 和 Markdown 均通过

## 清理说明

- 本版只操作 `D:\nodeproj\orderops-node`
- 未启动、停止、构建、测试或修改 Java 高并发项目
- 未启动、停止、构建、测试或修改 mini-kv
- 验证后清理 `dist`
- 未保留临时进程
