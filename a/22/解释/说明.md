# OrderOps Node V22 运行调试归档

## 本版目标

V22 新增本地 promotion decision ledger integrity：

- `GET /api/v1/ops/promotion-decisions/integrity` 检查整本 promotion decision ledger。
- 按 sequence 从旧到新复算每条 decision digest。
- 检查当前保留记录的 sequence 是否连续。
- 逐条生成 `chainDigest`，最终得到 `rootDigest`。
- Dashboard 新增 `Ledger Integrity` 入口。
- 本功能只读取 Node 本地 ledger，不触碰 Java / mini-kv。

## 关键代码

### 1. ledger integrity 结构

文件：`src/services/opsPromotionDecision.ts`

```ts
export interface OpsPromotionDecisionLedgerIntegrity {
  service: "orderops-node";
  generatedAt: string;
  valid: boolean;
  totalRecords: number;
  oldestSequence?: number;
  newestSequence?: number;
  rootDigest: {
    algorithm: "sha256";
    value: string;
  };
```

### 2. 单条 integrity record

文件：`src/services/opsPromotionDecision.ts`

```ts
export interface OpsPromotionDecisionLedgerIntegrityRecord {
  id: string;
  sequence: number;
  outcome: OpsPromotionDecision;
  readyForPromotion: boolean;
  digestValid: boolean;
  storedDigest: OpsPromotionDecisionRecord["digest"];
  recomputedDigest: OpsPromotionDecisionRecord["digest"];
  previousChainDigest?: string;
  chainDigest: string;
}
```

### 3. integrity 主流程

文件：`src/services/opsPromotionDecision.ts`

```ts
const ordered = [...this.records.values()].sort((left, right) => left.sequence - right.sequence);
let previousChainDigest: string | undefined;
const records = ordered.map((record) => {
  const recomputedValue = digestRecord(record);
  const chainDigest = digestChainStep(previousChainDigest, record, recomputedValue);
```

### 4. 账本级 valid

文件：`src/services/opsPromotionDecision.ts`

```ts
const digestsValid = records.every((record) => record.digestValid);
const sequencesContiguous = hasContiguousSequences(ordered);
```

```ts
valid: digestsValid && sequencesContiguous,
```

### 5. root digest

文件：`src/services/opsPromotionDecision.ts`

```ts
rootDigest: {
  algorithm: "sha256",
  value: previousChainDigest ?? digestEmptyLedger(),
},
```

### 6. 每一步 chain digest

文件：`src/services/opsPromotionDecision.ts`

```ts
function digestChainStep(
  previousChainDigest: string | undefined,
  record: OpsPromotionDecisionRecord,
  recomputedDigest: string,
): string {
  return createHash("sha256")
    .update(stableJson({
      previousChainDigest: previousChainDigest ?? "genesis",
      id: record.id,
      sequence: record.sequence,
      storedDigest: record.digest.value,
      recomputedDigest,
    }))
    .digest("hex");
}
```

### 7. sequence 连续性

文件：`src/services/opsPromotionDecision.ts`

```ts
function hasContiguousSequences(records: OpsPromotionDecisionRecord[]): boolean {
  if (records.length === 0) {
    return true;
  }

  return records.every((record, index) => record.sequence === records[0].sequence + index);
}
```

### 8. 路由入口

文件：`src/routes/opsSummaryRoutes.ts`

```ts
app.get("/api/v1/ops/promotion-decisions/integrity", async () => deps.opsPromotionDecisions.integrity());
```

### 9. Dashboard 入口

文件：`src/ui/dashboard.ts`

```html
<button data-action="opsPromotionIntegrity">Ledger Integrity</button>
```

```js
if (action === "opsPromotionIntegrity") {
  write(await api("/api/v1/ops/promotion-decisions/integrity"));
}
```

## 运行调试结果

- `npm run typecheck`：通过。
- `npm test`：18 个测试文件、63 个测试通过。
- `npm run build`：通过。
- `/health`：返回 `status=ok`。
- 空 ledger integrity：
  - `valid=true`
  - `sequenceOrder=empty`
  - `rootDigest.value.length=64`
- 创建两条 promotion decision：
  - 第一条 `sequence=1`
  - 第二条 `sequence=2`
- 两条记录后的 ledger integrity：
  - `valid=true`
  - `totalRecords=2`
  - `digestsValid=true`
  - `sequencesContiguous=true`
  - `sequenceOrder=contiguous`
  - `rootDigest.value.length=64`
  - 最后一条 `chainDigest == rootDigest.value`
  - 第二条 `previousChainDigest == 第一条 chainDigest`
- 首页 HTML 包含：
  - `opsPromotionIntegrity`
  - `/api/v1/ops/promotion-decisions/integrity`

## 对另外两个项目的影响

本次 V22 调试没有启动、停止、构建、测试或修改：

- `D:\javaproj\advanced-order-platform`
- `D:\C\mini-kv`

Node smoke 只访问了 `orderops-node` 自己的 health、promotion-decisions、integrity 和 dashboard 接口，没有访问真实代理路由。

## Cleanup

已完成最终 cleanup：

- 已停止本次 V22 smoke 使用的 `orderops-node` dev server。
- 收尾检查时 `127.0.0.1:4100` 没有监听进程。
- 已删除本次 `npm run build` 生成的 `dist`。
- 保留源码、测试、讲解文档和 `a/22` 归档文件。

## 本版一句话

V22 把 promotion decision 从“单条可复核”推进到“整本账本可复核”：每条记录的 digest、sequence 顺序和最终 root digest 都能被检查。
