# V31 开发运行调试说明

## 本版目标

V31 在 V30 `promotion handoff package verification` 后面继续加一层 `promotion handoff certificate`。

目标不是再展开一个更大的包，而是把已经验证过的 handoff package 提炼成一份适合交接记录引用的证书：

```text
archive bundle
 -> manifest
 -> archive verification
 -> attestation
 -> attestation verification
 -> handoff package
 -> handoff package verification
 -> handoff certificate
```

新增接口：

```text
GET /api/v1/ops/promotion-archive/handoff-certificate
GET /api/v1/ops/promotion-archive/handoff-certificate?format=markdown
```

## 关键代码

### 1. 证书结构

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
export interface OpsPromotionHandoffCertificate {
  service: "orderops-node";
  generatedAt: string;
  certificateName: string;
  packageName: string;
  archiveName: string;
  valid: boolean;
  state: OpsPromotionArchiveAttestationState;
  handoffReady: boolean;
  certificateDigest: {
    algorithm: "sha256";
    value: string;
    coveredFields: string[];
  };
  packageDigest: {
    algorithm: "sha256";
    value: string;
  };
  verifiedPackageDigest: {
    algorithm: "sha256";
    value: string;
  };
  sealDigest: {
    algorithm: "sha256";
    value: string;
  };
```

`packageDigest` 来自 handoff package，`verifiedPackageDigest` 来自 package verification 重新计算的 digest，`certificateDigest` 是 V31 对证书摘要字段生成的新 digest。

### 2. 附件有效性必须经过 package 和 verification 双重认可

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
  const attachments = input.handoffPackage.attachments.map((attachment) => {
    const verificationAttachment = input.handoffPackageVerification.attachments.find((candidate) => candidate.name === attachment.name);

    return {
      name: attachment.name,
      valid: attachment.valid && verificationAttachment?.valid === true,
      source: attachment.source,
      digest: { ...attachment.digest },
    };
  });
```

### 3. 证书 ready 状态的收口

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
  const valid = input.handoffPackage.valid && input.handoffPackageVerification.valid && attachments.every((attachment) => attachment.valid);
  const handoffReady = valid && input.handoffPackage.handoffReady;
```

这保证了 certificate 只有在 package、verification、附件三层都成立时才可能 `handoffReady=true`。

### 4. certificateDigest 覆盖字段

文件：`src/services/opsPromotionArchiveBundle.ts`

```ts
    certificateDigest: {
      algorithm: "sha256",
      value: digestStable(digestPayload),
      coveredFields: [
        "certificateName",
        "packageName",
        "archiveName",
        "valid",
        "state",
        "handoffReady",
        "packageDigest",
        "verifiedPackageDigest",
        "sealDigest",
        "decision",
        "verification",
        "attachments",
        "nextActions",
      ],
    },
```

### 5. 路由链路

文件：`src/routes/opsSummaryRoutes.ts`

```ts
    const handoffPackageVerification = createOpsPromotionHandoffPackageVerification({
      bundle,
      manifest,
      verification: archiveVerification,
      attestation,
      attestationVerification,
      handoffPackage,
    });
    const certificate = createOpsPromotionHandoffCertificate({
      handoffPackage,
      handoffPackageVerification,
    });

    if (request.query.format === "markdown") {
      reply.type("text/markdown; charset=utf-8");
      return renderOpsPromotionHandoffCertificateMarkdown(certificate);
    }
```

### 6. Dashboard 入口

文件：`src/ui/dashboard.ts`

```html
        <button data-action="opsPromotionHandoffCertificate">Handoff Certificate</button>
        <button data-action="opsPromotionHandoffCertificateReport">Handoff Certificate Report</button>
```

```js
        if (action === "opsPromotionHandoffCertificate") {
          write(await api("/api/v1/ops/promotion-archive/handoff-certificate"));
        }
        if (action === "opsPromotionHandoffCertificateReport") {
          const response = await fetch("/api/v1/ops/promotion-archive/handoff-certificate?format=markdown");
```

## 运行验证

正式验证命令：

```text
npm run typecheck -> passed
npm test          -> 18 files passed, 71 tests passed
npm run build    -> passed
```

本地 smoke 调试：

```text
HOST=127.0.0.1
PORT=4100
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
```

验证结果：

```text
/health -> ok
/api/v1/ops/promotion-archive/handoff-certificate -> valid=true
empty state -> not-started
blocked decision 后 state -> blocked
certificateDigest length -> 64
packageDigest == verifiedPackageDigest -> true
sealDigest length -> 64
attachmentCount -> 5
attachmentsValid -> true
Markdown content-type -> text/markdown; charset=utf-8
Dashboard certificate button -> present
Dashboard certificate report button -> present
```

## 对其他项目的影响

本次只在 `D:\nodeproj\orderops-node` 内开发、构建、测试和启动本地 Node 服务。

没有进入、修改、启动、停止或调试：

```text
D:\javaproj\advanced-order-platform
D:\C\mini-kv
```

smoke 运行时显式设置：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
```

因此不会主动探测或操作 Java 高并发订单项目、C++ mini_kv 项目。

## 清理结果

```text
本地 Node 调试服务 PID 24480 -> 已停止
127.0.0.1:4100 -> 无监听
dist/ -> 已删除
临时测试目录 -> 未创建
playwright-report/ -> 未创建
test-output/ -> 未创建
```

## 本版结论

V31 已经比较成熟：它把 V30 验证后的交接包提炼成可分享的 `certificateDigest`，并用 JSON、Markdown、Dashboard、单元测试和本地 smoke 一起闭环验证。
