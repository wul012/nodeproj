# 第九十九版运行调试归档说明

## 计划依据

本轮推进前读取了最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\v95-production-gap-roadmap.md
```

计划要求 Node v99 做：

```text
production readiness summary v2
汇总 v96-v98 与 Java v46 / mini-kv v55 的新上游证据
把生产阻塞项按 audit / access-control / upstream-observability 分类
```

只读核对结果：

```text
Java: f1d0f1a v46 add replay audit evidence samples
mini-kv: 3e4f9fb v55 add restart recovery evidence sample
Node: v96-v98 已完成并已打 tag
```

因此本轮继续推进 Node v99。

## 本版目标

新增生产就绪度汇总 v2：

```text
GET /api/v1/production/readiness-summary-v2
GET /api/v1/production/readiness-summary-v2?format=markdown
```

v99 的核心判断：

```text
upstream-observability: ready=true
audit: ready=false
access-control: ready=false
execution-safety: ready=true
readyForProductionOperations=false
executionAllowed=false
```

也就是说，本版确认 Java / mini-kv 上游只读证据更完整，但 Node 仍不能称为 production-ready，因为审计持久化和访问控制仍是明确阻塞项。

## 代码改动

新增：

```text
src/services/productionReadinessSummaryV2.ts
test/productionReadinessSummaryV2.test.ts
fixtures/upstream-production-evidence/failed-event-replay-audit-approved.sample.json
fixtures/upstream-production-evidence/failed-event-replay-audit-blocked.sample.json
fixtures/upstream-production-evidence/mini-kv-restart-recovery-evidence.json
代码讲解记录/103-production-readiness-summary-v2-v99.md
docs/plans/v99-production-readiness-v2-roadmap.md
```

更新：

```text
src/config.ts
src/routes/statusRoutes.ts
test/config.test.ts
README.md
docs/plans/README.md
docs/plans/v95-production-gap-roadmap.md
代码讲解记录/README.md
```

## 验证记录

类型检查：

```text
npm run typecheck
```

结果：

```text
tsc -p tsconfig.json --noEmit
exit_code=0
```

聚焦测试：

```text
npm test -- productionReadinessSummaryV2 config
```

结果：

```text
Test Files  3 passed (3)
Tests       9 passed (9)
```

全量测试：

```text
npm test
```

结果：

```text
Test Files  48 passed (48)
Tests       187 passed (187)
```

构建：

```text
npm run build
```

结果：

```text
tsc -p tsconfig.json
exit_code=0
```

HTTP smoke：

```text
HOST=127.0.0.1
PORT=4199
LOG_LEVEL=silent
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
node dist/server.js
GET http://127.0.0.1:4199/health
GET http://127.0.0.1:4199/api/v1/production/readiness-summary-v2
GET http://127.0.0.1:4199/api/v1/production/readiness-summary-v2?format=markdown
```

结果摘要：

```text
healthStatus=ok
summaryVersion=production-readiness-summary.v2
readyForProductionOperations=false
executionAllowed=false
upstreamEvidenceReady=true
categoryCount=4
productionBlockerCount=12
```

smoke JSON 已保存：

```text
a/99/解释/production-readiness-summary-v2-smoke.json
```

## 图片记录

截图目录：

```text
a/99/图片/
```

已保存：

```text
43-production-readiness-summary-v2.png
```

截图使用本机 Chrome：

```text
C:\Users\吴林东\AppData\Local\Google\Chrome\Application\chrome.exe
```

说明：后续 Node 运行调试截图默认优先使用本机 Chrome / Chromium，不默认触发 Playwright 浏览器下载。

## 边界

本版不做：

```text
不启动 Java
不启动 mini-kv
不构建 Java
不构建 mini-kv
不执行 Java replay POST
不执行 mini-kv 写命令
不读取真实 secret
不连接真实数据库
不把 production readiness 改成 true
```

## 清理记录

本轮结束前需要清理：

```text
dist
output
Node smoke 进程 port 4199
```

## 一句话总结

v99 把 Node 的生产差距从“零散报告”收敛成四类明确判断：上游只读证据已经比较稳，但生产级仍卡在 audit durability 和 access-control 两个硬缺口上。
