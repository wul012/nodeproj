# v1459 candidate document profile section renderer split

v1459 migrates the candidate document request package section. The section keeps the same heading, readiness fields, counts, disabled flags, and digest text.

The route output remains stable while the ownership moves to the dedicated candidate document renderer.
