# v1387 candidate document submission precheck

v1387 adds the source-lineage submission checkpoint. It carries the v1386 source-lineage request item into submission precheck without accepting a real document.

The validator keeps missing, synthetic, and unreviewed material blocked before payload import.
