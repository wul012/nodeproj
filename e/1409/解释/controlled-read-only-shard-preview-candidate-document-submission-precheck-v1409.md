# v1409 candidate document submission precheck

v1409 adds the disabled-probe-state checkpoint. It records that smoke checks in disabled-probe mode must not assume the package state is ready.

This preserves the lesson from v1386 HTTP smoke.
