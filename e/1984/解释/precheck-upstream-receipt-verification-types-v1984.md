# v1984 types

This version moves the profile shape and supporting Java, mini-kv, evidence, snippet, message, and check contracts into `managedAuditManualSandboxConnectionPrecheckUpstreamReceiptVerificationTypes.ts`.

The type split makes downstream imports explicit and keeps runtime logic out of structural declarations.
