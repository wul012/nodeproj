# v1402 candidate document submission precheck

v1402 adds the request-instruction coverage checkpoint. It verifies every request item has a submission instruction before real intake exists.

The checkpoint is cross-cutting and covers all acceptance checks.
