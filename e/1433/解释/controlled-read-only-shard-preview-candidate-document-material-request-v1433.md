# v1433 candidate document material request

v1433 adds the document-source-uri material request. It asks for a stable source URI or archive handle for the future reviewed document.

The package does not fetch or dereference that URI.
