# v716 Controlled read-only shard preview operator header policy

v716 records the Node guarded-route header policy for the future live read-only window.

The section requires operator, role, verification, and approval-correlation headers for guarded Node reads, without storing secret values.

Cross-project status: Java and mini-kv are not blocked.

Verification: covered by the runbook package focused test.
