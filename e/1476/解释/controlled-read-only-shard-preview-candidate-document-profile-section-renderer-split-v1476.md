# v1476 candidate document profile section renderer split

v1476 documents the lack of live integration startup requirements. No Java, mini-kv, browser, or long-running preview process is required for the refactor.

HTTP smoke still uses a short-lived Node server after build and then stops it.
