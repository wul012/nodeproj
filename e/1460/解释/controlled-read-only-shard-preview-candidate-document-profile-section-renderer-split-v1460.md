# v1460 candidate document profile section renderer split

v1460 migrates the candidate document submission precheck section. It preserves checkpoint and validator counts, field counts, disabled execution flags, and source digest output.

The migration is rendering-only and does not change the submission precheck artifact.
