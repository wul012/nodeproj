# v1994 entrypoint

This version reduces the public service entrypoint to orchestration.

It loads source reports, creates references, computes checks, gathers policy messages, and delegates final profile assembly. Existing route and test imports continue to use the same service module path.
