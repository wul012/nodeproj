# Node v1317 signed approval artifact draft text package comparison acceptance precheck

Node v1317 adds the operator value handle acceptance checkpoint. It groups the two value-binding comparison lanes and matching controls so later compared package evidence can prove value-handle alignment without enabling value import.

The guard blocks acceptance when non-secret operator value handle evidence is absent, when approval-grant separation is not preserved, or when side-effect proof is missing.

Operator value supply, evidence import, runtime payload routing, and writes remain disabled. No actual draft text package is submitted or accepted.

Java and mini-kv can continue independently; no fresh sibling evidence is requested.
