# v1441 candidate document material request

v1441 adds the secret-absence-attestation material request. It asks for an explicit no-secret attestation.

The current artifact continues to expose `containsSecretValue=false`.
