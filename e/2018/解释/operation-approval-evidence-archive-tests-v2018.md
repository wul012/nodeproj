# v2018 archive tests

This version verifies `operationApprovalExecutionGateArchive.test.ts` after the split.

The archive route still consumes the approval evidence report and verification through the unchanged public module.
