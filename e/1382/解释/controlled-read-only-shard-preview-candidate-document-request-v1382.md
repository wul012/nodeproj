# v1382 candidate document request package

v1382 adds the missing-document rejection request item. It turns the v1371 missing-document guard behavior into an explicit acceptance check.

The package can be ready while still requiring a real document because missing material is rejected before import.
