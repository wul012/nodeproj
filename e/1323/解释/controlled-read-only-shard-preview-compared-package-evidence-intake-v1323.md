# Node v1323 compared package evidence intake

v1323 adds the manual submission reference evidence slot. It defines how a future evidence artifact must point to an operator-submitted draft text package source without allowing Node to create or submit package material itself.

The guard rejects missing or synthetic evidence and keeps acceptance, signed approval, runtime payloads, writes, and sibling mutation blocked.
