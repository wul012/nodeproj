# v1391 candidate document submission precheck

v1391 adds the identity-digest submission checkpoint. It carries identity and digest lineage into the precheck while keeping secret values absent.

The validator blocks runtime payloads and writes.
