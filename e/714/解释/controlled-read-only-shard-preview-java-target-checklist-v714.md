# v714 Controlled read-only shard preview Java target checklist

v714 records the Java target checklist.

The section maps to `JAVA_READ_TARGET_SPEC` and allows only Java read-only shard-readiness or health-style GET evidence. Node still does not start Java.

Cross-project status: Java can continue in parallel by keeping GET-only evidence clear.

Verification: covered by the runbook package focused test.
