# v2045 isolate Node v273 source adaptation in references so downstream checks do not know source profile internals

This version isolate Node v273 source adaptation in references so downstream checks do not know source profile internals.

It belongs to the v2043-v2057 Node-only refactor batch for the disabled candidate upstream echo verification split. Java and mini-kv remain read-only evidence inputs and can continue in parallel.
