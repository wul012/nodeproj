# v1372 candidate document request package

v1372 adds the source-lineage document request item. It asks for a real compared package evidence candidate document that includes durable upstream version and fixture lineage.

The request is ready as a contract, but it still reports zero real documents and blocks candidate document intake, payload import, evaluation, approval, execution, writes, and sibling mutation.
