# v2000 typecheck and build

This version records strict TypeScript and production build validation.

`npm run typecheck` passed after the split, and `npm run build` also passed. The split modules compile through the same NodeNext TypeScript configuration.
