# v1365 compared evidence candidate intake

v1365 adds the comparison-result intake slot. It preserves the offline comparison result requirements from the candidate blueprint and makes them part of the real-document intake checklist.

The guard blocks candidate evaluation until a real compared package evidence candidate document exists. The version is intentionally read-only: no runtime payload is accepted and no preview execution is allowed.
