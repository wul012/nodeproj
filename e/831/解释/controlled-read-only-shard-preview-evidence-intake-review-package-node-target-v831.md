# v831 Controlled read-only shard preview evidence intake review package Node target

v831 adds `INTAKE_REVIEW_NODE_TARGET`.

The control groups Node health and Node Markdown intake records into a Node target review. This gives future manual-entry work a compact Node target slice without changing the underlying ledger.

Cross-project status: Java and mini-kv can continue in parallel.

Verification: covered by the v812-v836 evidence intake review package focused test and final batch validation.
