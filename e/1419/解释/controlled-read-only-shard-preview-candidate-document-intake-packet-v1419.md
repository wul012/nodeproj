# v1419 candidate document intake packet

v1419 adds the runtime-payload-freeze intake slot. It binds runtime payload absence and import/evaluation freeze checks into the intake packet.

The version keeps imported and evaluated payload counts at zero.
