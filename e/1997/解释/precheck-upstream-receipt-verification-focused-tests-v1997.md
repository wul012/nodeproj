# v1997 focused tests

This version verifies the direct v247 service test after the split.

The focused test still covers normal readiness, forced historical fixture fallback, blocked upstream-actions config, and JSON/Markdown route behavior.
