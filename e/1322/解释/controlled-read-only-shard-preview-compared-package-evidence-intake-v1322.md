# Node v1322 compared package evidence intake

v1322 adds the source acceptance precheck evidence contract slot. The slot requires any future real manually compared package evidence artifact to cite the v1321 acceptance precheck digest and all ten source checkpoints before it can be evaluated.

This version remains read-only: no package is created, no approval is captured, and `realComparedPackageEvidenceCount` stays `0`.
