# v1452 candidate document material submission precheck

v1452 adds the comparison-binding checkpoint. It ties comparison-binding rows to the evidence-policy request item before any later candidate material can be reviewed.

The validator makes the evidence relationship explicit without evaluating candidate values. This keeps the system in a pre-submission state and avoids turning the precheck into an approval engine.
