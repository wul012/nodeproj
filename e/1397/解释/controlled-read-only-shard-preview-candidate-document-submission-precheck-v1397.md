# v1397 candidate document submission precheck

v1397 adds the missing-document submission checkpoint. It preserves fail-closed behavior when reviewed real material is absent.

Missing candidate documents are rejected before intake.
