# v722 Controlled read-only shard preview operator runbook order

v722 records the ordered runbook section list.

The future manual window must follow the section order instead of ad hoc command ordering.

Cross-project status: Java and mini-kv can continue in parallel.

Verification: covered by the runbook package focused test.
