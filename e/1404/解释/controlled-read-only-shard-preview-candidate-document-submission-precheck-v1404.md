# v1404 candidate document submission precheck

v1404 adds the document-envelope-shape checkpoint. It prepares a future envelope shape while keeping runtime payloads absent.

The precheck still does not accept a document.
