# v703 Controlled read-only shard preview window archive verification

v703 records the archive verification stage.

The stage is `WINDOW_ARCHIVE_VERIFICATION`: archive digest and section count must match the verified window evidence before closeout.

Cross-project status: no fresh sibling evidence is consumed in v703.

Verification: covered by focused stage ledger tests.
