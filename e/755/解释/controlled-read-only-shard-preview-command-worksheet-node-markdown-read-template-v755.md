# v755 Controlled read-only shard preview command worksheet Node Markdown read template

v755 adds the controlled shard preview Markdown read template.

It records `WORKSHEET_NODE_MARKDOWN_READ_TEMPLATE`, a manual request for the controlled shard preview route with `format=markdown`. This prepares archive-friendly human review without executing the command automatically.

Cross-project status: Java and mini-kv can continue in parallel.

Verification: covered by the v752-v771 command worksheet focused test and final batch validation.
