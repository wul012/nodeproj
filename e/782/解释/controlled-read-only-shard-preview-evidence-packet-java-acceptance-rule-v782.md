# v782 Controlled read-only shard preview evidence packet Java acceptance rule

v782 adds `EVIDENCE_JAVA_ACCEPTANCE_RULE`.

The record defines how Java evidence becomes accepted or triaged. Node does not import a Java runtime file in this version.

Cross-project status: Java can continue in parallel.

Verification: covered by the v772-v791 evidence packet focused test and final batch validation.
