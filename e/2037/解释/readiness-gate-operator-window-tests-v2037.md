# v2037 operator window tests

This version verifies the downstream operator window checklist.

The checklist still consumes the same Node v237 profile fields and gate digest from the public readiness gate module.
