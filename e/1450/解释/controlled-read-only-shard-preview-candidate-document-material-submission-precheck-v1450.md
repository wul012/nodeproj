# v1450 candidate document material submission precheck

v1450 adds the digest-and-canonical-body checkpoint. It carries the canonical markdown body and field-value table request items into the submission precheck shape.

This version keeps the body outside runtime import. It counts the required material fields and reports that the submission surface is known, while real reviewed material remains absent and blocked.
