# v2044 move profile, route, sibling evidence, and boundary-code constants into a dedicated constants module

This version move profile, route, sibling evidence, and boundary-code constants into a dedicated constants module.

It belongs to the v2043-v2057 Node-only refactor batch for the disabled candidate upstream echo verification split. Java and mini-kv remain read-only evidence inputs and can continue in parallel.
