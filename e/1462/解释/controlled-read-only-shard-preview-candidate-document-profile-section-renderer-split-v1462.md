# v1462 candidate document profile section renderer split

v1462 migrates the candidate document material request package section. It preserves the v1446 material request package version, 25 request items, 25 checks, 20 requested fields, and disabled intake flags.

This prepares the profile renderer for future real material work without changing current behavior.
