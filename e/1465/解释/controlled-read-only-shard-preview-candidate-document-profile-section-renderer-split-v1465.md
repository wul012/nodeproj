# v1465 candidate document profile section renderer split

v1465 adds focused renderer coverage. The test loads the controlled read-only shard preview fixture and renders only the extracted candidate document section group.

It verifies that the group contains exactly five headings and still exposes the material request and submission precheck fields.
