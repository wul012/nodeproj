# v2015 stable JSON

This version keeps stable JSON and primitive record helpers in the readers module for reuse by report digesting and verification comparisons.

The digest algorithm and covered field order remain unchanged.
