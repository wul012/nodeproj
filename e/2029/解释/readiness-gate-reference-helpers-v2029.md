# v2029 reference helpers

This version keeps evidence file hashing, snippet matching, JSON loading, record extraction, arrays, strings, booleans, and numbers inside the references module.

Those helpers are local mechanics for the Java and mini-kv evidence readers.
