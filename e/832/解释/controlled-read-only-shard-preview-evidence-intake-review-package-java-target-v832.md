# v832 Controlled read-only shard preview evidence intake review package Java target

v832 adds `INTAKE_REVIEW_JAVA_TARGET`.

The control groups Java readiness and Java acceptance intake records. It records that Java can continue in parallel and that Node does not own Java startup or cleanup.

Cross-project status: Java can continue in parallel; Node does not require fresh Java evidence.

Verification: covered by the v812-v836 evidence intake review package focused test and final batch validation.
