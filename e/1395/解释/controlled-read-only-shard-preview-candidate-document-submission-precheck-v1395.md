# v1395 candidate document submission precheck

v1395 adds the approval-separation submission checkpoint. It keeps candidate document submission separate from approval grant and signed approval.

The checkpoint is ready only as a precheck contract.
