# v1400 candidate document submission precheck

v1400 adds the import-evaluation-freeze submission checkpoint. It keeps import and evaluation frozen while the precheck is only a contract.

Candidate payload counts remain zero.
