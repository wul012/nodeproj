# v1377 candidate document request package

v1377 adds the signature-envelope document request item. It prepares a future signed approval review path without treating the envelope as signed approval capture.

The acceptance check blocks approval grant, signed approval, runtime payloads, writes, and sibling mutation.
