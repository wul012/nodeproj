# v1996 inventory

This version removes the split v247 service from the live large-file inventory.

The remaining large-file inventory now correctly focuses on `statusRoutes.ts`, `dashboard.ts`, and `opsPromotionArchiveRenderers.ts`, which are still intended future split targets.
