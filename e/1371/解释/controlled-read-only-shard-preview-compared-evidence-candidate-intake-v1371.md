# v1371 compared evidence candidate intake

v1371 adds the archive-closeout intake slot and completes the ten-slot intake preflight. The aggregate reports ten slots, ten guards, twenty required fields, thirty-six gates, and `waiting-for-real-compared-package-evidence-candidate-document`.

The version updates the profile route, review artifact barrel, type module catalog, focused tests, and historical fallback coverage. It is ready as a contract, but it still refuses real execution until a reviewed real compared package evidence candidate document exists.
