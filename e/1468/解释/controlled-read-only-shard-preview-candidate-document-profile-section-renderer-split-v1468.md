# v1468 candidate document profile section renderer split

v1468 records the new renderer in the type module catalog. The catalog adds `candidate-document-profile-section-renderer` as order 209.

The ownership entry says this module owns candidate document section headings and profile entry rendering.
