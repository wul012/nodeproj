# v2003 boundary

This version records the public approval evidence boundary.

Routes and downstream bundles still import `operationApprovalEvidence.js`; the entrypoint now re-exports the service, report builder, verification builder, renderers, and public types from focused modules.
