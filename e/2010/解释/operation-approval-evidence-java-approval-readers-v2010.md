# v2010 Java approval readers

This version moves Java approval-status detail readers into `operationApprovalEvidenceReaders.ts`.

The readers still extract approval evidence version, approval digest, replay eligibility digest, and approved-for-replay state from nested Java probe details.
