# v818 Controlled read-only shard preview evidence intake review package acceptance criteria

v818 adds `INTAKE_REVIEW_ACCEPTANCE_CRITERIA_PRESERVED`.

The control verifies that acceptance criteria stay attached to the intake review package. Missing acceptance criteria block operator review.

Cross-project status: Java and mini-kv can continue in parallel.

Verification: covered by the v812-v836 evidence intake review package focused test and final batch validation.
