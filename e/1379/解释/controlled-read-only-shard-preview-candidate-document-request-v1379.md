# v1379 candidate document request package

v1379 adds the policy-lock document request item. It requests proof that the future document cannot bypass write or execution controls.

The request item stays read-only and keeps write routing, service starts, and sibling mutation disabled.
