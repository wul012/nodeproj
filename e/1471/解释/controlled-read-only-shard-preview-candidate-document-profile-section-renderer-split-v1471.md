# v1471 candidate document profile section renderer split

v1471 preserves route Markdown output. The main profile renderer delegates the candidate document group through one imported function while keeping the same strings.

The existing route test continues to assert the v1456 material submission precheck text in the full profile output.
