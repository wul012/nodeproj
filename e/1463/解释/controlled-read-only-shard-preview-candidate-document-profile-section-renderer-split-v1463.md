# v1463 candidate document profile section renderer split

v1463 migrates the candidate document material submission precheck section. It preserves the v1456 version, ten checkpoints, ten validators, twenty submission fields, and material submission disabled flags.

The section still shows precheck readiness only when the source v1446 package is ready.
