# v1376 candidate document request package

v1376 adds the identity-digest document request item. It requests identity and digest lineage while avoiding secret reads.

The item is covered by read-only checks and contributes its candidate fields to the twenty-field request count.
