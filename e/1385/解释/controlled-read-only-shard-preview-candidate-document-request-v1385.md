# v1385 candidate document request package

v1385 adds the import and evaluation freeze request item. It proves the request package does not import, evaluate, accept, or reject candidate payloads.

The gate set keeps payload import, evaluation, and acceptance blocking visible as separate checks.
