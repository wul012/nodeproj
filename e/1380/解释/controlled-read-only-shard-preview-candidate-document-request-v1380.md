# v1380 candidate document request package

v1380 adds the approval-separation document request item. It asks for evidence that document presence remains separate from approval grant and signed approval.

This preserves the approval boundary needed before any real shard preview execution can be considered.
