# v717 Controlled read-only shard preview mini-kv command allowlist

v717 records the mini-kv command allowlist section.

Only `SHARDJSON` and `SHARDROUTEVERIFYREPORTJSON` are included in the operator runbook package.

Cross-project status: mini-kv can continue in parallel by preserving this read-only command boundary.

Verification: covered by the runbook package focused test.
