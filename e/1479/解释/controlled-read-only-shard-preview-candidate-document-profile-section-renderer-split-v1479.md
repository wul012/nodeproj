# v1479 candidate document profile section renderer split

v1479 records build and HTTP smoke coverage. The smoke check must still find the v1456 material submission precheck section and confirm disabled upstream probes keep the route fail-closed.

The smoke server is stopped immediately after the check.
