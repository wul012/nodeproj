# v713 Controlled read-only shard preview Node target checklist

v713 records the Node target checklist in the operator runbook package.

The section maps to `NODE_READ_TARGET_SPEC` and limits Node reads to health plus controlled preview JSON/Markdown evidence.

Cross-project status: Java and mini-kv are not blocked by this Node target checklist.

Verification: covered by the runbook package focused test.
