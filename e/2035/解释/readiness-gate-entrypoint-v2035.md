# v2035 entrypoint

This version leaves the public service file as orchestration only.

It loads source material, builds evidence references, builds the gate, computes checks and messages, then delegates final profile assembly.
