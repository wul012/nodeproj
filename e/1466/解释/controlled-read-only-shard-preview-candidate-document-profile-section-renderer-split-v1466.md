# v1466 candidate document profile section renderer split

v1466 locks the material request route-facing fields. The focused renderer test checks that `candidateDocumentMaterialRequestPackageVersion: Node v1446` remains visible.

This prevents the extraction from dropping the material request package section while the route still depends on it.
