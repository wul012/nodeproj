# v1470 candidate document profile section renderer split

v1470 updates catalog and validation versions to `Node v1481`. The module count increases from 209 to 210.

The catalog tests now verify the new version, module count, unique IDs, unique paths, and stable re-export count.
