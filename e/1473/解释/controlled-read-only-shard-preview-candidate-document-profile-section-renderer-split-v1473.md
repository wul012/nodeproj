# v1473 candidate document profile section renderer split

v1473 confirms the main renderer line-count reduction. The live-window renderer drops the candidate document helper block and becomes easier to review.

The new file stays focused on one group instead of spreading candidate document field rendering through the larger route renderer.
