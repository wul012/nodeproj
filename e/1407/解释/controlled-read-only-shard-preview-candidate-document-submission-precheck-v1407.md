# v1407 candidate document submission precheck

v1407 adds the secret-value-absence checkpoint. It keeps secret values out of the submission precheck and digest surface.

The validator blocks runtime and write paths.
