# v1464 candidate document profile section renderer split

v1464 preserves candidate document section ordering. The extracted renderer returns the same five headings in the same order as the original route profile.

Stable ordering matters because archive reviews and route smoke tests match these sections by text.
