# v1352 compared evidence candidate

v1352 adds the source-lineage candidate section. It binds `sourceIntakeDigest` and `digestLineage` as real-candidate requirements while keeping candidate values absent.

Java and mini-kv can continue in parallel; Node is defining the receiving blueprint, not requesting fresh sibling output yet.
