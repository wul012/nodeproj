# v1451 candidate document material submission precheck

v1451 adds the field-table checkpoint. It connects the field-value table and comparison-binding table requests so a future submission can prove the same twenty material fields are covered.

The package reports twenty required fields and twenty submission material fields. It does not accept field values yet, and it does not let placeholder or synthetic data satisfy the gate.
