# v2036 focused tests

This version verifies the direct readiness gate tests.

The tests still cover ready state, blocked upstream-actions config, JSON route output, Markdown route output, and no connection or credential reads.
