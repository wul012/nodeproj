# v1373 candidate document request package

v1373 adds the artifact-shape document request item. It carries the v1371 artifact-shape intake fields into a concrete request instruction and acceptance criterion.

The package keeps the document absent and rejects synthetic substitutes. This is request preparation, not evidence import.
