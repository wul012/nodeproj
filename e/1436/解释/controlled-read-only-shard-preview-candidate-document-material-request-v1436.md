# v1436 candidate document material request

v1436 adds the field-value-table material request. It asks for a future table covering all requested material fields.

The aggregate still reports only the requested field count, not field values.
