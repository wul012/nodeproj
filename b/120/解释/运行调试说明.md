# Node v120 运行调试说明

本次运行调试对象是 `managed audit adapter compliance harness`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
AUDIT_STORE_KIND=file
AUDIT_STORE_PATH=.tmp/v120-audit.jsonl
AUDIT_STORE_URL=managed-audit://contract-only
PORT=4136
```

HTTP smoke 使用 auditor header 访问 audit endpoint。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：69 个测试文件通过，250 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/audit/managed-adapter-compliance
GET /api/v1/audit/managed-adapter-compliance?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "managed-audit-adapter-compliance.v1",
  "adapterName": "InMemoryComplianceAuditAdapter",
  "appendOnlyWriteCovered": true,
  "queryByRequestIdCovered": true,
  "digestStableAfterRepeatRead": true,
  "backupRestoreMarkerCovered": true,
  "noDatabaseConnectionAttempted": true,
  "noAuditFileMigrationPerformed": true,
  "realManagedAdapterConnected": false,
  "upstreamActionsStillDisabled": true,
  "passedComplianceStepCount": 4,
  "productionBlockerCount": 1,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/120/图片/managed-audit-adapter-compliance.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v120 已用本地 compliance adapter 统一验收 `appendOnlyWrite`、`queryByRequestId`、`digest`、`backupRestoreMarker` 四个方法。本版没有连接真实数据库、没有迁移 audit 文件，真实 managed audit adapter 仍是生产阻塞。
