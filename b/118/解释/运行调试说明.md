# Node v118 运行调试说明

本次运行调试对象是 `IdP verifier boundary`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
ORDEROPS_AUTH_TOKEN_ISSUER=orderops-smoke
ORDEROPS_AUTH_TOKEN_SECRET=smoke-secret-v118
ORDEROPS_IDP_ISSUER=https://idp.example
ORDEROPS_IDP_AUDIENCE=orderops-node
ORDEROPS_IDP_JWKS_URL=https://idp.example/.well-known/jwks.json
ORDEROPS_IDP_CLOCK_SKEW_SECONDS=90
PORT=4132
```

HTTP smoke 使用 viewer header 访问 security endpoint。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：67 个测试文件通过，244 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/security/idp-verifier-boundary
GET /api/v1/security/idp-verifier-boundary?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "idp-verifier-boundary.v1",
  "issuerConfigured": true,
  "audienceConfigured": true,
  "jwksUrlUsesHttps": true,
  "clockSkewConfigured": true,
  "noJwksNetworkFetch": true,
  "noExternalIdpCall": true,
  "realIdpVerifierConnected": false,
  "upstreamActionsStillDisabled": true,
  "productionBlockerCount": 1,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/118/图片/idp-verifier-boundary.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v118 已定义真实 IdP verifier 的 OIDC/JWT 边界，检查 issuer、audience、JWKS URL、clock skew 等配置形状。本版不访问 JWKS、不调用外部 IdP、不把 token 变成真实放行依据，所以 `readyForProductionAuth=false`。
