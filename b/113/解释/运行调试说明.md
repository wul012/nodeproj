# Node v113 运行调试说明

本次运行调试对象是 `deployment environment readiness gate`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
ORDEROPS_AUTH_TOKEN_ISSUER=orderops-smoke
ORDEROPS_AUTH_TOKEN_SECRET=smoke-secret-v113
AUDIT_STORE_URL=managed-audit://contract-only
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
PORT=4123
```

HTTP smoke 使用 viewer header 访问，因为本次开启了 access guard enforcement rehearsal。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：62 个测试文件通过，229 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/deployment/environment-readiness
GET /api/v1/deployment/environment-readiness?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "gateVersion": "deployment-environment-readiness.v1",
  "readyForDeployment": false,
  "readyForProductionOperations": false,
  "signedTokenContractPasses": true,
  "managedAuditStoreUrlConfigured": true,
  "managedAuditStoreAdapterConnected": false,
  "upstreamActionsStillDisabled": true,
  "productionBlockerCount": 2,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/113/图片/deployment-environment-readiness.png
```

截图说明：

```text
Playwright screenshot CLI 不支持直接追加自定义 HTTP header。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全配置，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v113 已经把 token、audit、retention、backup、upstream action safety 汇总成部署前门禁。即使 rehearsal 配置齐全，仍然因为真实 IdP 和真实 managed audit adapter 未接入而保持 `readyForDeployment=false`。
