# Node v133 运行调试说明

本版目标：新增 `production connection archive verification`，把 v129 precheck、v130 dry-run change request、v132 approval ledger 打包成可复核 verification report。

## 验证命令

```text
npm run typecheck
npx vitest run test/productionConnectionArchiveVerification.test.ts
npm test
npm run build
Node HTTP smoke
本机 Chrome 截图
```

## 关键结果

```text
typecheck: passed
target test: 3 passed
npm test: 82 files passed, 289 tests passed
build: passed
HTTP smoke: passed
```

HTTP smoke 返回：

```text
profileVersion=production-connection-archive-verification.v1
readyForArchiveVerification=true
approvalDigestValid=true
approvalDigestMatchesChangeRequest=true
noRealConnectionAttempted=true
productionBlockerCount=0
archiveDigest.length=64
```

## 归档文件

```text
b/133/图片/production-connection-archive-verification.png
b/133/解释/运行调试说明.md
```

## 安全说明

本版只启动 Node 本地进程做 smoke 和截图；没有启动、停止、构建、测试或修改 Java 与 mini-kv。archive verification 只复核本地 dry-run 证据，不触发真实数据库、真实 IdP/JWKS 或上游动作。
