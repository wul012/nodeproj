# Node v132 运行调试说明

本版目标：新增 `production connection dry-run approval ledger`，记录 reviewer、decision、reason、changeRequestDigest 和 approval digest，但不执行真实连接。

## 验证命令

```text
npm run typecheck
npx vitest run test/productionConnectionDryRunApprovalLedger.test.ts
npm test
npm run build
Node HTTP smoke
本机 Chrome 截图
```

## 关键结果

```text
typecheck: passed
target test: 3 passed
npm test: 81 files passed, 286 tests passed
build: passed
HTTP smoke: passed
```

HTTP smoke 返回：

```text
approvalVersion=production-connection-dry-run-approval.v1
decision=approve
executable=false
realConnectionAttempted=false
readyForApprovalArchive=true
approvalDigest.length=64
ledgerReady=true
recordCount=1
```

## 归档文件

```text
b/132/图片/production-connection-dry-run-approval-ledger.png
b/132/解释/运行调试说明.md
```

## 安全说明

本版只启动 Node 本地进程做 smoke 和截图；没有启动、停止、构建、测试或修改 Java 与 mini-kv。审批记录只写入 Node 内存 ledger，不会触发真实 audit adapter、IdP/JWKS 或 upstream action。
