# Node v135 运行调试说明

本版目标：新增 `production live probe readiness contract`，定义 Java / mini-kv 只读 live probe 目标、skipped/pass/blocked 语义和禁写边界，但不实际探测上游。

## 验证命令

```text
npm run typecheck
npx vitest run test/productionLiveProbeReadinessContract.test.ts
npm test
npm run build
Node HTTP smoke
本机 Chrome 截图
```

## 关键结果

```text
typecheck: passed
target test: 3 passed
npm test: 84 files passed, 295 tests passed
build: passed
HTTP smoke: passed
```

HTTP smoke 返回：

```text
profileVersion=production-live-probe-readiness-contract.v1
readyForLiveProbePlanning=true
probeAttempted=false
plannedProbeCount=5
writeProbeCount=0
productionBlockerCount=0
```

## 归档文件

```text
b/135/图片/production-live-probe-readiness-contract.png
b/135/解释/运行调试说明.md
```

## 安全说明

本版只启动 Node 本地进程做 smoke 和截图；没有启动、停止、构建、测试或修改 Java 与 mini-kv。contract endpoint 只生成计划，不会对上游发起探测。
