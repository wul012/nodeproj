# Node v108 运行调试说明

本次运行调试对象是 `auth enforcement rehearsal middleware`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ORDEROPS_AUTH_MODE=rehearsal
ACCESS_GUARD_ENFORCEMENT_ENABLED=false
PORT=4118
```

默认运行时仍然只观察、不拒绝请求；显式打开 enforcement 的 401 / 403 / 200 行为由测试覆盖。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：57 个测试文件通过，214 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/security/auth-enforcement-rehearsal
GET /api/v1/security/auth-enforcement-rehearsal?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "auth-enforcement-rehearsal.v1",
  "authMode": "rehearsal",
  "enforcementEnabled": false,
  "rejectsRequests": false,
  "readyForProductionAuth": false,
  "executionAllowed": false,
  "sampleCount": 3,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/108/图片/auth-enforcement-rehearsal.png
```

## 结论

v108 已经有可切换的 enforcement rehearsal：测试环境可以显式验证 401、403、200；默认运行时继续保持 observe-only，不把 header-derived identity 当作生产认证。
