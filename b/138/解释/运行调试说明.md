# Node v138 运行调试说明

本版目标：

```text
新增 live probe evidence archive record。
把 v135 contract、v136 smoke harness、v137 summary v13 固化成 digest-backed archive record。
```

## 粒度说明

本版没有新增 production readiness summary。

原因：

```text
summary 后续只承担阶段总汇总；
v138 只是 live probe evidence 的归档记录，不为一个小检查新增 summary 版本。
```

## 验证结果

本版验证项：

```text
npm run typecheck
npx vitest run test/productionLiveProbeEvidenceArchive.test.ts
npm test
npm run build
HTTP smoke
本机 Chrome 截图
```

测试结果：

```text
target test: 1 file / 3 tests passed
full test: 87 files / 304 tests passed
build: passed
```

HTTP smoke 使用安全环境变量：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
```

实际 smoke 结果：

```text
profileVersion=production-live-probe-evidence-archive.v1
archiveDigest=78ce87b08f2357951e811e4a9d1cacd1b7df63dc1c40c29b5818e89310d6d157
readyForArchiveRecord=true
liveProbeEvidenceMode=skipped
plannedProbeCount=5
skippedProbeCount=5
writeProbeAttempted=false
readyForProductionOperations=false
productionBlockerCount=0
markdownStatus=200
```

这说明本版只归档 skipped live probe evidence，不访问 Java / mini-kv，也不把 skipped evidence 当作 production pass。

## 截图

```text
b/138/图片/production-live-probe-evidence-archive.png
```

## 清理

本版收口前清理：

```text
dist/
.tmp/
```

没有保留本版启动的后台 Node 进程。
