# Node v114 运行调试说明

本次运行调试对象是 `verified identity audit binding`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
ORDEROPS_AUTH_TOKEN_ISSUER=orderops-smoke
ORDEROPS_AUTH_TOKEN_SECRET=smoke-secret-v114
PORT=4124
```

HTTP smoke 使用 viewer / auditor header 访问，并额外发送 `Authorization: Bearer <local rehearsal token>`，用于验证 token subject、roles、issuer、verification result 是否能进入 audit context。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：63 个测试文件通过，232 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/security/verified-identity-audit-binding
GET /api/v1/security/verified-identity-audit-binding?format=markdown
GET /api/v1/security/access-policy
GET /api/v1/audit/events?limit=5
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "verified-identity-audit-binding.v1",
  "acceptedTokenBindsSubject": true,
  "rejectedTokenCapturesReason": true,
  "upstreamActionsStillDisabled": true,
  "productionBlockerCount": 1,
  "markdownStatus": 200,
  "markdownHasTitle": true,
  "auditTokenVerification": "accepted",
  "auditTokenSubject": "viewer-smoke-token"
}
```

截图文件：

```text
b/114/图片/verified-identity-audit-binding.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v114 已把 signed token contract 的 verified subject、roles、issuer、verification result 写入 audit context。当前仍是本地 HMAC rehearsal，不是生产 IdP，因此 `readyForProductionAuth=false`，真实执行继续保持关闭。
