# Node v110 运行调试说明

本次运行调试对象是 `production readiness summary v5`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
AUDIT_STORE_KIND=file
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
ACCESS_GUARD_ENFORCEMENT_ENABLED=false
PORT=4120
```

本版汇总 v108 auth enforcement rehearsal、v109 audit retention integrity evidence、v107 upstream boundary evidence。它仍然不放行生产操作。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：59 个测试文件通过，220 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/production/readiness-summary-v5
GET /api/v1/production/readiness-summary-v5?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "summaryVersion": "production-readiness-summary.v5",
  "readyForProductionOperations": false,
  "upstreamBoundaryReady": true,
  "authRehearsalReady": true,
  "authRejects": false,
  "auditRetentionReady": true,
  "managedAuditStoreReady": false,
  "executionAllowed": false,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/110/图片/production-readiness-summary-v5.png
```

## 结论

v110 说明项目进入更清晰的生产阻塞收敛状态：上游边界、auth rehearsal、audit retention evidence 都能汇总，但真实 signed auth 和 managed audit store 仍未完成，所以继续保持 `readyForProductionOperations=false`。
