# Node v137 运行调试说明

本版目标：

```text
新增 production readiness summary v13。
把 v12 dry-run review evidence、v135 live probe contract、v136 live probe smoke harness 汇总到新的生产就绪报告。
```

## 验证结果

本版验证项：

```text
npm run typecheck
npx vitest run test/productionReadinessSummaryV13.test.ts
npm test
npm run build
HTTP smoke
本机 Chrome 截图
```

测试结果：

```text
target test: 1 file / 3 tests passed
full test: 86 files / 301 tests passed
build: passed
```

HTTP smoke 使用安全环境变量：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
```

实际 smoke 结果：

```text
summaryVersion=production-readiness-summary.v13
summaryV12Ready=true
liveProbeContractReady=true
liveProbeSmokeEvidenceReady=true
liveProbeSmokeSkipped=true
readyForProductionOperations=false
productionBlockerCount=2
markdownStatus=200
```

这说明本版已经能把 live probe 准备证据汇总起来，但仍明确保留真实 managed audit adapter 和 real IdP verifier 两个生产 blocker。

## 截图

```text
b/137/图片/production-readiness-summary-v13.png
```

## 清理

本版收口前清理：

```text
dist/
.tmp/
```

没有保留本版启动的后台 Node 进程。
