# v152 运行调试说明：production pass evidence archive verification

## 本版目标

v152 消费 v150 archive，重算 archive digest，并引用 Java v49 / mini-kv v58 的只读 evidence 版本证据。

本版仍然不启动 Java / mini-kv，也不在 Node runtime 读取另外两个仓库文件。上游证据以已经只读核对过的 tag 和样本路径固化：

```text
Java: v49订单平台ops-read-only-evidence-sample
mini-kv: 第五十八版只读证据样本包
```

## 运行验证

已执行：

```text
npm run typecheck
npm test -- --run test/productionLiveProbeRealReadSmokeProductionPassEvidenceArchiveVerification.test.ts
npm test -- --run archive/capture/gate 相关测试
npm test
npm run build
Node HTTP smoke
Chrome screenshot
```

验证结果：

```text
typecheck: passed
target test: 1 file, 4 tests passed
related tests: 4 files, 15 tests passed
full test: 100 files, 347 tests passed
build: passed
HTTP smoke: passed
screenshot: passed
```

HTTP smoke 关键返回：

```text
verificationState=verified-non-pass-archive
readyForArchiveVerification=true
readyForProductionOperations=false
upstreamEvidenceReady=true
archiveDigestMatches=true
verificationDigestLength=64
```

## 截图

截图文件：

```text
b/152/图片/production-pass-evidence-archive-verification.png
```

截图覆盖：

```text
Verification
Checks
Archive artifact
Java evidence
mini-kv evidence
```

## 安全边界

本版没有启动、停止、构建、测试或修改 Java / mini-kv。

Node 临时 smoke / screenshot 服务已停止。

`UPSTREAM_ACTIONS_ENABLED=false`，没有执行 Java replay、订单写操作或 mini-kv 写命令。
