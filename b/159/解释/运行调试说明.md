# v159 运行调试说明：read-only capture release evidence review

## 本版目标

v159 把 Node v158 archive verification、Java v51 field guide、mini-kv v60 field guide 合成一次 release evidence review。

本版仍然只做 review，不授权生产执行：

```text
readyForProductionOperations=false
executionAllowed=false
UPSTREAM_ACTIONS_ENABLED=false
```

## 运行验证

已执行：

```text
npm run typecheck
npm test -- --run test/productionLiveProbeRealReadSmokeReadOnlyWindowCaptureReleaseEvidenceReview.test.ts test/productionLiveProbeRealReadSmokeReadOnlyWindowCaptureArchiveVerification.test.ts
npm test
npm run build
Node HTTP smoke
Chrome screenshot
```

验证结果：

```text
typecheck: passed
target/related tests: 2 files, 6 tests passed
full test: passed
build: passed
HTTP smoke: passed
screenshot: passed
```

HTTP smoke 关键返回：

```text
reviewState=reviewed-read-only-skipped-capture
readyForReadOnlyCaptureReleaseEvidenceReview=true
readyForProductionPassEvidence=false
readyForProductionOperations=false
javaV51FieldGuideReady=true
miniKvV60FieldGuideReady=true
skippedOrMixedNotProductionPass=true
releaseReviewDigestLength=64
```

## 截图

截图文件：

```text
b/159/图片/production-live-probe-real-read-smoke-read-only-window-capture-release-evidence-review.png
```

截图覆盖：

```text
Review
Checks
Archive Verification
Java Field Guide
mini-kv Field Guide
Summary
```

## 安全边界

本版没有启动、停止、构建、测试或修改 Java / mini-kv。

Java v51 和 mini-kv v60 只做只读 tag/status 核对。

Node 临时 smoke / screenshot 服务在验证后停止。

继续禁止：

```text
UPSTREAM_ACTIONS_ENABLED=true
Java replay POST
mini-kv SET / DEL / EXPIRE
Node 自动启动 Java / mini-kv
skipped/mixed evidence 冒充 production pass
```
