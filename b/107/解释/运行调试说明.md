# Node v107 运行调试说明

本次运行调试对象是 Node 项目 `orderops-node` 的 `production readiness summary v4`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
AUDIT_STORE_KIND=file
PORT=4117
```

没有启动、停止、构建、测试或修改 Java 高并发项目和 mini-kv。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：56 个测试文件通过，211 条测试通过
build：通过
```

## HTTP smoke

本次启动的是已构建产物：

```powershell
node dist/server.js
```

服务地址：

```text
http://127.0.0.1:4117
```

核心 smoke 结果：

```json
{
  "healthStatus": "ok",
  "summaryVersion": "production-readiness-summary.v4",
  "readyForProductionOperations": false,
  "javaReady": true,
  "miniKvReady": true,
  "accessDurable": true,
  "fileAuditReady": true,
  "executionAllowed": false,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

这说明 v107 能正确读取 Java v48 operator auth boundary、mini-kv v57 recovery retention boundary，并能在 file audit runtime 下证明 access/operator audit evidence 可恢复。

## 截图

截图使用本机 Chrome，目标是：

```text
GET /api/v1/production/readiness-summary-v4?format=markdown
```

截图文件：

```text
b/107/图片/production-readiness-summary-v4-viewport.png
```

## 结论

v107 的结果不是解除生产阻塞，而是更准确地说明生产阻塞在哪里：

```text
真实 auth middleware 未完成
access guard enforcement 未完成
managed audit store / retention / backup policy 未完成
```

因此本版继续保持：

```text
readyForProductionOperations=false
executionAllowed=false
```
