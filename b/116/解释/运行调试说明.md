# Node v116 运行调试说明

本次运行调试对象是 `production readiness summary v6`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
ORDEROPS_AUTH_TOKEN_ISSUER=orderops-smoke
ORDEROPS_AUTH_TOKEN_SECRET=smoke-secret-v116
AUDIT_STORE_KIND=file
AUDIT_STORE_PATH=.tmp/v116-audit.jsonl
AUDIT_STORE_URL=managed-audit://contract-only
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
PORT=4128
```

HTTP smoke 使用 viewer header 访问 production readiness endpoint。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：65 个测试文件通过，238 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/production/readiness-summary-v6
GET /api/v1/production/readiness-summary-v6?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "summaryVersion": "production-readiness-summary.v6",
  "verifiedIdentityAuditBindingReady": true,
  "managedAuditLocalEvidenceReady": true,
  "deploymentEnvironmentGateConfigured": true,
  "upstreamActionsStillDisabled": true,
  "readyForProductionOperations": false,
  "productionBlockerCount": 2,
  "blockedCategoryCount": 2,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/116/图片/production-readiness-summary-v6.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v116 已把 v114 verified identity audit binding、v115 managed audit readiness summary、v113 deployment environment readiness gate 合成 production readiness summary v6。当前本地 rehearsal evidence 已经连通，但真实 IdP 与真实 managed audit adapter 仍是硬阻塞，所以 `readyForProductionOperations=false`。
