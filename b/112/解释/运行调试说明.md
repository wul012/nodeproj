# Node v112 运行调试说明

本次运行调试对象是 `managed audit store adapter contract`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
PORT=4122
```

本版只定义 managed audit store contract，并用 fake adapter 覆盖能力。不接 PostgreSQL / Redis / S3 / 云审计，不迁移历史 audit 文件，不执行真实保留或删除。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：61 个测试文件通过，226 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/audit/managed-store-contract
GET /api/v1/audit/managed-store-contract?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "managed-audit-store-contract.v1",
  "readyForProductionAudit": false,
  "capabilityCount": 5,
  "coveredCapabilityCount": 5,
  "appendOnly": true,
  "queryByRequestId": true,
  "digest": true,
  "retentionMetadata": true,
  "backupMarker": true,
  "realManagedAdapterConnected": false,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/112/图片/managed-audit-store-contract.png
```

## 结论

v112 已经把 managed audit store 的最小 adapter contract 固化为 evidence；fake adapter 能证明能力形状，但真实 managed adapter 仍未接入，所以继续保持生产审计阻塞。
