# Node v115 运行调试说明

本次运行调试对象是 `managed audit readiness summary`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
AUDIT_STORE_KIND=file
AUDIT_STORE_PATH=.tmp/v115-audit.jsonl
AUDIT_STORE_URL=managed-audit://contract-only
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
PORT=4126
```

HTTP smoke 使用 auditor header 访问 audit endpoint。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：64 个测试文件通过，235 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/audit/managed-readiness-summary
GET /api/v1/audit/managed-readiness-summary?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "summaryVersion": "managed-audit-readiness-summary.v1",
  "fakeAdapterCapabilitiesCovered": true,
  "fileAuditRuntimeSelected": true,
  "fileAuditIntegrityStable": true,
  "managedStoreUrlConfigured": true,
  "realManagedAdapterConnected": false,
  "localEvidenceReadyForAdapterWork": true,
  "productionBlockerCount": 1,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/115/图片/managed-audit-readiness-summary.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v115 已把 managed audit store contract、file audit retention/integrity evidence、retention/backup 配置和 `AUDIT_STORE_URL` 合成一个审计生产准备 summary。本地证据已经足够支持下一步 adapter 工作，但真实 managed audit adapter 仍未接入，所以 `readyForProductionAudit=false`。
