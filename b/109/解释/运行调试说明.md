# Node v109 运行调试说明

本次运行调试对象是 `audit retention policy + file audit integrity evidence`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
AUDIT_STORE_KIND=file
AUDIT_RETENTION_DAYS=30
AUDIT_MAX_FILE_BYTES=1048576
AUDIT_ROTATION_ENABLED=true
AUDIT_BACKUP_ENABLED=true
PORT=4119
```

本版只生成证据，不删除、不轮转、不备份文件，也不接数据库或云审计服务。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：58 个测试文件通过，217 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/audit/retention-integrity-evidence
GET /api/v1/audit/retention-integrity-evidence?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "evidenceVersion": "audit-retention-integrity-evidence.v1",
  "readyForProductionAudit": false,
  "runtimeStoreKind": "file",
  "retentionDaysConfigured": true,
  "rotationPolicyConfigured": true,
  "backupPolicyConfigured": true,
  "integrityDigestStable": true,
  "productionBlockerCount": 1,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/109/图片/audit-retention-integrity-evidence.png
```

## 结论

v109 证明 file audit 的本地 retention knobs 和 digest evidence 可以被稳定读取；即使 retention / rotation / backup 都配置齐，仍然因为缺少 managed audit store 而保持 `readyForProductionAudit=false`。
