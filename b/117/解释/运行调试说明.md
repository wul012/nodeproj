# Node v117 运行调试说明

本次运行调试对象是 `managed audit adapter boundary`。

运行边界：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
AUDIT_STORE_KIND=file
AUDIT_STORE_PATH=.tmp/v117-audit.jsonl
AUDIT_STORE_URL=managed-audit://contract-only
PORT=4130
```

HTTP smoke 使用 auditor header 访问 audit endpoint。

## 验证命令

```powershell
npm run typecheck
npm test
npm run build
```

结果：

```text
typecheck：通过
test：66 个测试文件通过，241 条测试通过
build：通过
```

## HTTP smoke

目标：

```text
GET /api/v1/audit/managed-adapter-boundary
GET /api/v1/audit/managed-adapter-boundary?format=markdown
```

结果：

```json
{
  "healthStatus": "ok",
  "profileVersion": "managed-audit-adapter-boundary.v1",
  "selectedBoundaryRuntime": "file",
  "managedStoreUrlConfigured": true,
  "doesNotOpenNetworkConnection": true,
  "doesNotMigrateAuditFiles": true,
  "realManagedAdapterConnected": false,
  "backupRestoreDrillImplemented": false,
  "upstreamActionsStillDisabled": true,
  "productionBlockerCount": 2,
  "markdownStatus": 200,
  "markdownHasTitle": true
}
```

截图文件：

```text
b/117/图片/managed-audit-adapter-boundary.png
```

截图说明：

```text
Playwright 使用本机 Chrome channel 截图。
HTTP smoke 已在 ACCESS_GUARD_ENFORCEMENT_ENABLED=true 下完成。
截图阶段使用同一安全变量，但将 ACCESS_GUARD_ENFORCEMENT_ENABLED=false，仅用于渲染 Markdown 页面。
```

## 结论

v117 已定义真实 managed audit adapter 的接口边界，并把 `memory`、`file`、`managed-unimplemented` 三种 runtime 状态讲清楚。本版没有打开数据库连接、没有迁移 audit JSONL、没有执行 backup/restore drill，所以 `readyForProductionAudit=false`。
