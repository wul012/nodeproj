# Node v122 运行调试说明：production readiness summary v8

本版来源计划：

```text
docs/plans/v119-production-connection-roadmap.md
```

本版目标：

```text
把 v120 managed audit adapter compliance 与 v121 JWKS verifier fixture rehearsal
接入新的 production readiness summary v8。
```

## 运行环境

HTTP smoke 使用安全环境变量：

```text
HOST=127.0.0.1
PORT=4122
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
ACCESS_GUARD_ENFORCEMENT_ENABLED=true
ORDEROPS_IDP_ISSUER=https://idp.example
ORDEROPS_IDP_AUDIENCE=orderops-node
ORDEROPS_IDP_JWKS_URL=https://idp.example/.well-known/jwks.json
AUDIT_STORE_KIND=file
AUDIT_STORE_URL=managed-audit://contract-only
```

HTTP smoke 请求带了 viewer header：

```text
x-orderops-operator-id=viewer-1
x-orderops-roles=viewer
```

截图使用本机 Chrome / Chromium。由于 `npx playwright screenshot` 不支持直接追加自定义 header，本版截图阶段使用 `ACCESS_GUARD_ENFORCEMENT_ENABLED=false` 的 observe-only 模式打开同一 Markdown endpoint；HTTP smoke 已在 enforcement=true 下通过。

没有启动、停止、构建、测试或修改 Java / mini-kv。

## 验证命令

本版已运行：

```text
npm run typecheck
npm test
npm run build
Node HTTP smoke
本机 Chrome 截图
```

验证结果：

```text
typecheck: passed
test: 71 个测试文件通过，256 条测试通过
build: passed
HTTP smoke: /api/v1/production/readiness-summary-v8 JSON 和 Markdown 均返回 200
```

HTTP smoke 关键结果：

```text
summaryVersion=production-readiness-summary.v8
managedAuditComplianceHarnessPasses=true
jwksFixtureVerifierPasses=true
readyForProductionOperations=false
productionBlockers=REAL_MANAGED_AUDIT_ADAPTER_MISSING,REAL_IDP_VERIFIER_NOT_CONNECTED
```

## 截图

截图文件：

```text
b/122/图片/production-readiness-summary-v8.png
```

截图内容为 Markdown 版：

```text
GET /api/v1/production/readiness-summary-v8?format=markdown
```

## 后续计划

本版收口后另起新计划：

```text
docs/plans/v122-production-connection-candidate-roadmap.md
```

后续建议版本：

```text
Node v123：managed audit adapter harness runner
Node v124：JWKS cache contract rehearsal
Node v125：production readiness summary v9
```

## 清理

本版 smoke 启动的 Node 进程已停止。

临时目录：

```text
.tmp/
```

会在版本收口前清理。
