# v158 运行调试说明：read-only window capture archive verification

## 本版目标

v158 复核 v157 capture archive。

本版验证内容：

```text
复算 captureArchiveDigest
校验 v157 archive profileVersion
校验 v156 live capture profileVersion
校验 v155 readiness packet profileVersion
校验 Java v50 / mini-kv v59 tag 引用
确认 skipped/mixed 不进入 production pass
确认 readyForProductionOperations=false
```

## 运行验证

已执行：

```text
npm run typecheck
npm test -- --run test/productionLiveProbeRealReadSmokeReadOnlyWindowCaptureArchiveVerification.test.ts test/productionLiveProbeRealReadSmokeReadOnlyWindowCaptureArchive.test.ts
npm test
npm run build
Node HTTP smoke
Chrome screenshot
```

验证结果：

```text
typecheck: passed
target/related tests: 2 files, 6 tests passed
full test: passed
build: passed
HTTP smoke: passed
screenshot: passed
```

HTTP smoke 关键返回：

```text
verificationState=verified-read-only-skipped-capture-archive
readyForReadOnlyCaptureArchiveVerification=true
readyForProductionOperations=false
captureArchiveDigestMatches=true
skippedOrMixedNotProductionPass=true
verificationDigestLength=64
```

## 截图

截图文件：

```text
b/158/图片/production-live-probe-real-read-smoke-read-only-window-capture-archive-verification.png
```

截图覆盖：

```text
Verification
Checks
Archive
Live Capture
Readiness Packet
Java Reference
mini-kv Reference
```

## 安全边界

本版没有启动、停止、构建、测试或修改 Java / mini-kv。

Node 临时 smoke / screenshot 服务在验证后停止。

继续禁止：

```text
UPSTREAM_ACTIONS_ENABLED=true
Java replay POST
mini-kv SET / DEL / EXPIRE
Node 自动启动 Java / mini-kv
skipped/mixed evidence 冒充 production pass
```
