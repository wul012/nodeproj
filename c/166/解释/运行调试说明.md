# v166 运行调试说明：rollback window readiness checklist

## 本版目标

v166 按最新全局计划推进 Node 自己负责的版本：

```text
Node v166：rollback window readiness checklist
```

前置条件已经只读核对完成：

```text
Java v57：rollback approval handoff sample 已完成并推送
mini-kv v66：restore compatibility handoff sample 已完成并推送
```

本版只消费两边 handoff 证据，生成人工回滚窗口 checklist，不启动、不停止、不构建、不测试、不修改 Java / mini-kv。

## 本版结果

新增：

```text
src/services/rollbackWindowReadinessChecklist.ts
test/rollbackWindowReadinessChecklist.test.ts
```

改造：

```text
src/routes/statusRoutes.ts
```

新增接口：

```text
GET /api/v1/production/rollback-window-readiness-checklist
GET /api/v1/production/rollback-window-readiness-checklist?format=markdown
```

核心输出：

```text
checklistState=ready-for-manual-window-review
readyForRollbackWindowReadinessChecklist=true
readyForProductionRollback=false
readyForProductionOperations=false
executionAllowed=false
checklistCheckCount=26
passedChecklistCheckCount=26
```

## 运行验证

已执行：

```text
npm run typecheck
npm test -- --run test/rollbackWindowReadinessChecklist.test.ts
npm test
npm run build
Node HTTP smoke
Chrome screenshot
```

验证结果：

```text
typecheck: passed
target test: 1 file, 3 tests passed
full test: 113 files, 390 tests passed
build: passed
HTTP smoke: passed
screenshot: passed
```

HTTP smoke 核心断言：

```text
checklistState=ready-for-manual-window-review
readyForRollbackWindowReadinessChecklist=true
java=Java v57
miniKv=mini-kv v66
checklistCheckCount=26
passedChecklistCheckCount=26
Markdown title rendered
START_POST_V166_PLAN rendered
```

## 截图

截图文件：

```text
c/166/图片/rollback-window-readiness-checklist.png
```

截图覆盖：

```text
Rollback window readiness checklist
Checklist
Checks
Previous Release Bundle Gate
Java Rollback Approval Handoff
mini-kv Restore Compatibility Handoff
Node Checklist Envelope
Checklist Steps
Forbidden Operations
Next Actions
```

截图使用本机 Chrome：

```text
npx playwright screenshot --channel chrome
```

## 安全边界

本版 HTTP smoke 和截图只启动 Node 本地服务，验证后已停止。

运行环境保持：

```text
UPSTREAM_PROBES_ENABLED=false
UPSTREAM_ACTIONS_ENABLED=false
```

本版没有启动、停止、构建、测试或修改 Java / mini-kv。

Node v166 明确不允许：

```text
Trigger Java rollback from Node
Execute database rollback SQL
Read production secret values
Execute mini-kv LOAD / COMPACT / SETNXEX
Execute mini-kv restore or admin commands
Treat mini-kv restore as Java order authority
Treat checklist as production rollback approval
```

## 计划变化

更新：

```text
docs/plans/v163-post-rollback-readiness-roadmap.md
docs/plans/README.md
```

新增：

```text
docs/plans/v166-post-rollback-window-roadmap.md
```

计划状态变成：

```text
v163 衍生生产交付硬化计划已收口
Node v166 已完成
下一步进入 post-v166 真实生产化缺口收敛计划
下一步推荐并行：Java v58 + mini-kv v67
```

## 一句话结论

v166 把 Java v57 与 mini-kv v66 的人工 handoff 证据合成了一个只读回滚窗口 readiness checklist，完成了 v163 衍生的 release bundle / handoff / rollback window 生产交付硬化阶段，但仍不授权真实回滚或恢复执行。
