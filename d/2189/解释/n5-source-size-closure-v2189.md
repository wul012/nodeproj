# v2189 N5 源文件体积治理收口说明

本版只做内部结构治理，不增加功能。v2187 开始时重新扫描出的真实债务是 16 个超过 800 行的 `src/` 文件，v2187、v2188 已分别把数量降到 10 和 4；v2189 处理最后的仪表盘脚本、状态路由、人工沙箱连接演练 guard、三项目真实读 runtime packet，最终由 `node scripts/source-size-census.mjs --json` 复现为 1234 个源文件、0 个超限文件、0 个豁免。

`dashboardClientScript.ts` 继续作为唯一公开组合入口，三个片段严格按 core、operations、actions 顺序拼接。与 v2188 原始字符串相比，长度仍是 51041，SHA-256 仍是 `5771c499a2987853de06731340221b5ad800930431da59f1b80439f4645c7f3f`。状态路由把四条生产连接 dry-run approval 注册语句移到已有的 deployment 路由模块，调用位置不变，AST 对比确认四条语句一致。guard 只下沉历史证据读取和格式化，runtime packet 只下沉结果记录、错误分类与单条记录渲染；两个原服务仍保留 loader、决策、摘要和公开类型入口。

源文件 baseline 已收紧为 `maxOversized: 0` 与空 remediation 列表。今后任何新超限文件都会直接让 ratchet 失败，不能靠登记新债务悄悄绕过。服务与路由文件数仍是 1125/80，没有抬高治理基线。Java 和 mini-kv 可以并行继续，本版没有启动、停止、构建、测试或修改它们，也没有改变跨项目合同与生产执行权限。

浏览器检查使用受控 Node PID 25716、端口 4190 和 Playwright MCP。1440×1000 页面没有横向溢出，仪表盘脚本正常装载，截图存放在独立的 `d/2189/图片/`。服务随后按 PID 精确停止，端口已释放。最终全量分片、文档质量门和 CI 结果以 evidence JSON 的验证项为准；在这些机械证据完成前，本说明不把外部 N5 评审写成通过。
