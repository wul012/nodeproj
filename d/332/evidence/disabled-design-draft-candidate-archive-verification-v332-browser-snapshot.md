# Browser snapshot note

Playwright MCP was attempted first for the local `file://` archive page and returned:

```text
Access to "file:" protocol is blocked.
```

The v332 renderable archive page was therefore verified with local Chrome headless, producing:

```text
d/332/图片/disabled-design-draft-candidate-archive-verification-v332.png
```

The HTTP Markdown route snapshot is stored separately at:

```text
d/332/evidence/disabled-design-draft-candidate-archive-verification-v332-snapshot.md
```
