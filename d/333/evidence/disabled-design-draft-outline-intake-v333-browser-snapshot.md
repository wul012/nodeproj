# Browser snapshot note

Playwright MCP was attempted first for the local `file://` archive page and returned:

```text
Access to "file:" protocol is blocked.
```

The v333 renderable archive page was therefore verified with local Chrome headless, producing:

```text
d/333/图片/disabled-design-draft-outline-intake-v333.png
```

The HTTP Markdown route snapshot is stored separately at:

```text
d/333/evidence/disabled-design-draft-outline-intake-v333-snapshot.md
```
