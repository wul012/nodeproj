# Node v2207 Approval Echo Hotspot Repair

Date: 2026-07-19
Owner: Codex
State: complete; commit cd1cac35, tag v2207, and Node Evidence run 29712753670 passed

## Objective and stop condition

Remove the current highest maintainability hotspot without changing one public
module path, profile field, object-key order, digest, JSON/Markdown route body,
fixture, or safety decision. The version stops only when the 788-line file and its
187-line / complexity-137 `createChecks` function disappear from the shrink-only
baseline, no replacement debt appears, and portable frozen-fallback output parity
plus both local and historical evidence paths pass.

## Step-0 reconciliation and parallel projects

- Node started clean at pushed commit `4a9471ce`, tag `v2206`; cumulative Node
  Evidence run `29693804255` subsequently passed checkout, typecheck, lint, all
  static gates, the complete test suite, build, and safe smoke.
- Java is committed/pushed at `64fee530`, tag
  `v1871-order-platform-exact-spotbugs-waivers`, with an active uncommitted v1872
  immutable-DTO boundary slice. Java is recommended parallel; Node reads neither
  that dirty tree nor fresh Java evidence.
- mini-kv is committed/pushed at `44301a2e`, tag `v1667`, with only its exhibition
  brief modified. mini-kv is recommended parallel and is not blocked by Node.
- No live capstone, sibling process, write permission, or execution authority is
  required. Existing Java v116 and mini-kv v122 evidence remains read-only input.

## Necessity proof

- Blocker: the target service owns three exact baseline debts at once: 788 file
  lines, 187 `createChecks` lines, and branch complexity 137. It is the highest
  current file hotspot and mixes evidence acquisition, policy assessment, message
  creation, summary calculation, and public profile assembly.
- Consumer: the unchanged public loader is consumed by the v283 plan draft, the
  v284 echo chain, and the audit JSON/Markdown route table.
- Existing abstractions are reused: historical evidence utilities, report-count
  utilities, the existing types/renderer, and the shrink-only maintainability gate.
  No new report, route, service, schema, fixture, or governance chain is created.
- Growth stops when exactly the three named debt keys are removed. A second source
  family, another service wrapper, or unrelated rename is outside this version.

## Requirement-evidence matrix

| Requirement | Implementation | Mechanical evidence | State |
|---|---|---|---|
| Freeze public output first | normalize repository roots/path separators and hash fixed-time fallback JSON + Markdown | oracle passes before extraction and after it | passed; 27742/13925 bytes and both SHA-256 values stable |
| Preserve evidence modes | leave local sibling test and forced historical fallback test unchanged | focused existing tests | passed in target 5/5 |
| Separate data from behavior | move Java/mini-kv path parsing to `approvalReadinessEchoSources.ts`; move pure assessment to `approvalReadinessEchoChecks.ts` | import graph + focused parity | passed; 158/256/482-line split and cycles remain 2 |
| Remove long/complex check builder | compose ordered source, mini-kv, alignment, and runtime check groups | maintainability census | passed; 89/121/238 -> 88/120/237 |
| Keep hard file ceilings | add no service/route file; remain exactly 1125 services and 80 routes | governance growth ratchet | passed at 1125/80 |
| Tighten debt only | remove three stale baseline entries and add/grow none | baseline diff + census | passed; exact three stale before refresh, ready after |
| Close safely | focused, typecheck, lint, static gates, batched complete suite, build, docs, commit/tag/push/CI | reproducible commands and archive | local acceptance passed; remote closeout pending |

## New-file design note

- `approvalReadinessEchoSources`: data acquisition only; owns Java/mini-kv paths,
  frozen boundary/artifact catalogs, and parsed historical references.
- `approvalReadinessEchoChecks`: behavior only; owns ordered check groups, echo
  assessment, blockers, warnings, recommendations, and summary.
- The long service remains the public facade and owns Node-v281 service loading,
  profile identity, route links, digest assembly, and final response shape.
- Runtime dependencies flow services -> evidence; evidence modules import service
  contracts as types only. No runtime evidence -> service cycle is allowed.

## Execution sequence

1. Add the portable fixed-time fallback JSON/Markdown oracle to the existing test,
   make it fail with placeholder values, then record current values and prove green.
2. Extract source parsing and pure assessment without changing existing test
   expectations. Split the check object in its original key order.
3. Run the target test under local sibling resolution and forced historical fallback;
   run both downstream consumers and route parity.
4. Run maintainability before editing the baseline: it must report exactly three
   stale keys and no added/grown debt. Remove only those keys and rerun ready.
5. Verify typecheck, lint, service/route growth, all static gates, build, and one
   complete suite at the next batch boundary. Write Chinese explanation/walkthrough
   before final verification, then commit/tag/push and inspect Node Evidence.

## Fail conditions

- Updating an existing business expectation, fixture, route, renderer output, profile
  order, or safety boolean fails the version.
- Adding a service/route file, moving the public module, creating a runtime import
  cycle, raising a baseline, or producing any new/grown debt fails the version.
- A digest oracle that embeds a machine-specific repository root or Windows-only
  separator fails portability and must be redesigned rather than waived.
- If v2206 remote CI turns red, v2207 pauses before commit and repairs that failure.

## Executed evidence before final batch

- Oracle red: placeholder run failed exactly the four new byte/hash assertions while
  the four existing tests passed. Migration-before and migration-after runs passed
  1 file / 5 tests with JSON 27,742 bytes and Markdown 13,925 bytes unchanged.
- A copied Java walkthrough directory typo caused 4/5 tests to fail and both hashes
  to drift. The path was restored from the v2206 Git object; no expectation changed.
- Target, v283/v284 consumers, maintainability adversarial test, and governance
  growth ratchet passed 5 files / 24 tests. Typecheck and zero-warning lint passed.
- Maintainability failed before baseline refresh with exactly three stale entries and
  no added/grown debt, then passed ready at `88 / 120 / 237 / 2`. Service/route files
  remain `1125 / 80`; elegance debt stays 4,537; tracked families stay 52.
- Elegance, family logic, source size, renderer, security/config, and archive budget
  passed. The walkthrough is 100% compliant at 3,266 Chinese characters, 11 H2
  sections, 372-character largest section, and zero repetition/oversize signals.
- Final documents passed 11 files / 42 tests after closeout updates and the source
  tree built. A tracked
  safe process on port 31207 returned 200 for health (92 bytes), target JSON (28,596),
  and Markdown (13,925); it stopped and the port returned to zero listeners.
- The complete suite passed all 16 shards at 567 files / 1,730 tests with two
  workers per shard. A separate `vitest list --json` run independently reproduced
  567 files / 1,730 tests. Commit `cd1cac35`, tag `v2207`, push, and Node Evidence
  run `29712753670` completed successfully, including build and safe smoke cleanup.
